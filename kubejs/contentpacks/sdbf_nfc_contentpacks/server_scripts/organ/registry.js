// priority: 501
// [merged] domain/organ/registry/organ/nature/mammary_gland.js, domain/organ/registry/organ/ender/basic_organ.js, domain/organ/registry/organ/ender/dragon_blood.js, domain/organ/registry/organ/ender/dragon_blood_heart.js, domain/organ/registry/organ/ender/ender_eye.js, domain/organ/registry/organ/ender/ender_golem_line.js, domain/organ/registry/organ/ender/endermaptera_shell.js, domain/organ/registry/organ/ender/endermite_gland.js, domain/organ/registry/organ/ender/phantom_wing.js, domain/organ/registry/organ/food/baguette_bone.js, domain/organ/registry/organ/food/beer_gland.js, domain/organ/registry/organ/food/gluten_muscle.js, domain/organ/registry/organ/food/greedy_throat.js, domain/organ/registry/organ/food/gula_beacon.js, domain/organ/registry/organ/food/gula_worm_nest.js, domain/organ/registry/organ/food/jar_of_mystery.js, domain/organ/registry/organ/food/jar_of_vacuum.js, domain/organ/registry/organ/food/king_of_stomach.js, domain/organ/registry/organ/food/living_beef_wellington.js, domain/organ/registry/organ/food/mashed_potato_pancreas.js, domain/organ/registry/organ/food/parasitism_stomach.js, domain/organ/registry/organ/food/sweet_heart.js, domain/organ/registry/organ/food/sweets_gland.js, domain/organ/registry/organ/food/tasty_gland.js, domain/organ/registry/organ/food/void_stomach_pouch.js, domain/organ/registry/organ/food/worm_larva.js, domain/organ/registry/organ/food/worm_of_gula.js, domain/organ/registry/organ/food/worm_of_taste.js, domain/organ/registry/organ/infected/basic_organs.js, domain/organ/registry/organ/infected/bone_cage.js, domain/organ/registry/organ/infected/bone_meal_bag.js, domain/organ/registry/organ/infected/devour_teeth.js, domain/organ/registry/organ/infected/malignant_neuron_tumor.js, domain/organ/registry/organ/infected/mutation_intestine.js, domain/organ/registry/organ/infected/nightmare_core.js, domain/organ/registry/organ/infected/primal_bone_cage.js, domain/organ/registry/organ/infected/primal_heart.js, domain/organ/registry/organ/infected/soul_core.js, domain/organ/registry/organ/infected/witch_fibroma.js, domain/organ/registry/organ/infected/wither_bone_cage.js, domain/organ/registry/organ/infected/worm_neuron.js, domain/organ/registry/organ/legend/prismarine_crown.js, domain/organ/registry/organ/machine/basic_organs.js, domain/organ/registry/organ/machine/blaze_pressurizer.js, domain/organ/registry/organ/machine/cyborgization_device.js, domain/organ/registry/organ/machine/energy_bottle_red.js, domain/organ/registry/organ/machine/fiery_core.js, domain/organ/registry/organ/machine/furnace_core.js, domain/organ/registry/organ/machine/iron_repair_device.js, domain/organ/registry/organ/machine/lava_life_cycle_system.js, domain/organ/registry/organ/machine/programmable_automatic_core.js, domain/organ/registry/organ/machine/prowler_rotating_shaft.js, domain/organ/registry/organ/machine/revolution_bell.js, domain/organ/registry/organ/machine/revolution_cable.js, domain/organ/registry/organ/machine/revolution_delay.js, domain/organ/registry/organ/machine/revolution_relay.js, domain/organ/registry/organ/machine/small_acid_tank.js, domain/organ/registry/organ/machine/telescopic_arm.js, domain/organ/registry/organ/machine/thermometer.js, domain/organ/registry/organ/machine/watcher_probe.js, domain/organ/registry/organ/magic/amethyst_core.js, domain/organ/registry/organ/magic/animted_soul.js, domain/organ/registry/organ/magic/aptrgangr_soul.js, domain/organ/registry/organ/magic/bookwyrm_star_gem.js, domain/organ/registry/organ/magic/calamity_bone_spur.js, domain/organ/registry/organ/magic/coral_armor.js, domain/organ/registry/organ/magic/deepling_ectoplasm.js, domain/organ/registry/organ/magic/deepling_star_gem.js, domain/organ/registry/organ/magic/draugr_skull.js, domain/organ/registry/organ/magic/drygmy_star_gem.js, domain/organ/registry/organ/magic/exhausted_source_focus_crystal.js, domain/organ/registry/organ/magic/heal_star_gem.js, domain/organ/registry/organ/magic/hippocamtus_scale.js, domain/organ/registry/organ/magic/koboleton.js, domain/organ/registry/organ/magic/potion_skin.js, domain/organ/registry/organ/magic/shulker_eye.js, domain/organ/registry/organ/magic/soul_cage.js, domain/organ/registry/organ/magic/soul_wing.js, domain/organ/registry/organ/magic/starbuncle_star_gem.js, domain/organ/registry/organ/magic/whirlisprig_star_gem.js, domain/organ/registry/organ/magic/wither_skull.js, domain/organ/registry/organ/nature/allay_wing.js, domain/organ/registry/organ/nature/armadillo_shell.js, domain/organ/registry/organ/nature/axolotl_gill.js, domain/organ/registry/organ/nature/basic_organs.js, domain/organ/registry/organ/nature/bat_throat.js, domain/organ/registry/organ/nature/bear_paw.js, domain/organ/registry/organ/nature/bee_honey_stomach.js, domain/organ/registry/organ/nature/bison_horn.js, domain/organ/registry/organ/nature/boar_nose.js, domain/organ/registry/organ/nature/camel_hump.js, domain/organ/registry/organ/nature/cassowary_muscle.js, domain/organ/registry/organ/nature/chimera_heart.js, domain/organ/registry/organ/nature/cindaria_umbrella.js, domain/organ/registry/organ/nature/cornucopia.js, domain/organ/registry/organ/nature/deer_horn.js, domain/organ/registry/organ/nature/dog_tail.js, domain/organ/registry/organ/nature/dolphin_fin.js, domain/organ/registry/organ/nature/dragonfly_wing.js, domain/organ/registry/organ/nature/dumbo_octopus_hat.js, domain/organ/registry/organ/nature/elder_guardian_core.js, domain/organ/registry/organ/nature/explosion_bag.js, domain/organ/registry/organ/nature/ferret_tail.js, domain/organ/registry/organ/nature/flamingo_beak.js, domain/organ/registry/organ/nature/fox_tail.js, domain/organ/registry/organ/nature/frog_tongue.js, domain/organ/registry/organ/nature/giant_claw.js, domain/organ/registry/organ/nature/glow_gland.js, domain/organ/registry/organ/nature/guardian_core.js, domain/organ/registry/organ/nature/hatching_chamber.js, domain/organ/registry/organ/nature/hedgehog_thorn.js, domain/organ/registry/organ/nature/horse_tendon.js, domain/organ/registry/organ/nature/jumping_spider_leg.js, domain/organ/registry/organ/nature/koi_fish_scale.js, domain/organ/registry/organ/nature/leaf_insect_exoskeleton.js, domain/organ/registry/organ/nature/leech_mouthpart.js, domain/organ/registry/organ/nature/llama_gland.js, domain/organ/registry/organ/nature/minisheep_gland.js, domain/organ/registry/organ/nature/moew_nose.js, domain/organ/registry/organ/nature/otter_tooth.js, domain/organ/registry/organ/nature/owl_vertebrae.js, domain/organ/registry/organ/nature/parrot_beak.js, domain/organ/registry/organ/nature/pelican_larynx.js, domain/organ/registry/organ/nature/penguin_flipper.js, domain/organ/registry/organ/nature/pig_stomach.js, domain/organ/registry/organ/nature/pillager_gland.js, domain/organ/registry/organ/nature/pufferfish_liver.js, domain/organ/registry/organ/nature/pumpkin_shooter.js, domain/organ/registry/organ/nature/rabbit_foot.js, domain/organ/registry/organ/nature/raccoon_paw.js, domain/organ/registry/organ/nature/rat_ear.js, domain/organ/registry/organ/nature/ravager_hoof.js, domain/organ/registry/organ/nature/red_panda_tail.js, domain/organ/registry/organ/nature/red_wolf_beard.js, domain/organ/registry/organ/nature/refill_agreement.js, domain/organ/registry/organ/nature/rib_blade.js, domain/organ/registry/organ/nature/scry_stomach_pouch.js, domain/organ/registry/organ/nature/sea_bunny_skin.js, domain/organ/registry/organ/nature/shima_enaga_feather.js, domain/organ/registry/organ/nature/silverfish_gland.js, domain/organ/registry/organ/nature/slime_colloid.js, domain/organ/registry/organ/nature/spider_leg.js, domain/organ/registry/organ/nature/squirrel_tail.js, domain/organ/registry/organ/nature/sulfur_cube_colloid.js, domain/organ/registry/organ/nature/swim_bladder.js, domain/organ/registry/organ/nature/symbiocto_arm.js, domain/organ/registry/organ/nature/tentacles_harvester.js, domain/organ/registry/organ/nature/turkey_gizzard.js, domain/organ/registry/organ/nature/turtle_shell.js, domain/organ/registry/organ/nature/urchinkin_stinger.js, domain/organ/registry/organ/nature/wilden_heart.js, domain/organ/registry/organ/nature/wolf_fang.js, domain/organ/registry/organ/nether/basic_organs.js, domain/organ/registry/organ/nether/blaze_spine.js, domain/organ/registry/organ/nether/explosion_cavity.js, domain/organ/registry/organ/nether/ghast_sac.js, domain/organ/registry/organ/nether/golden_stone.js, domain/organ/registry/organ/nether/hoglin_stomach.js, domain/organ/registry/organ/nether/ignited_armour.js, domain/organ/registry/organ/nether/magam_colloid.js, domain/organ/registry/organ/plants/cherry_bone.js, domain/organ/registry/organ/plants/crimson_brain.js, domain/organ/registry/organ/plants/foliaath_stem.js, domain/organ/registry/organ/plants/hop_kidney.js, domain/organ/registry/organ/plants/lily_pad_lung.js, domain/organ/registry/organ/plants/pitcher_stomach.js, domain/organ/registry/organ/plants/rootling_ectoplasm.js, domain/organ/registry/organ/plants/twisting_weeping_intestine.js, domain/organ/registry/organ/plants/vita_berry.js, domain/organ/registry/organ/plants/vita_rose_liver.js, domain/organ/registry/organ/plants/vita_sunflower.js, domain/organ/registry/organ/relics/dying_heart.js, domain/organ/registry/organ/relics/ender_guardian_spine.js, domain/organ/registry/organ/relics/harbinger_lung.js, domain/organ/registry/organ/relics/immortal_volcanic_rock.js, domain/organ/registry/organ/relics/leviathan_rib.js, domain/organ/registry/organ/relics/maledictus_wing.js, domain/organ/registry/organ/relics/netherite_muscle.js, domain/organ/registry/organ/relics/remnant_heart.js, domain/organ/registry/organ/relics/scylla_star_gem.js, domain/organ/registry/organ/relics/warden_core.js, domain/organ/registry/organ/rose/rose_quartz_dialyzer.js, domain/organ/registry/organ/rose/rose_quartz_heart.js, domain/organ/registry/organ/rose/rose_quartz_liver.js, domain/organ/registry/organ/rose/rose_quartz_muscle.js, domain/organ/registry/organ/rose/rose_quartz_rib.js

// ===== mammary_gland.js =====
RegistryOrgan('kubejs:mammary_gland')
    .addScore('chestcavity:breath_capacity', 1)
    .addScore('chestcavity:knockback_resistant', -1)
    .setCanSpawn(true)
    
const MammaryGlandRecipeMap = new Map()
/**
 * 
 * @param {String} entityTypeId 
 * @param {ItemStack} oriItem 
 * @param {ItemStack} outputItem 
 * @param {number} damage 
 */
function registryMammaryGlandRecipe(entityTypeId, oriItem, outputItem, damage) {
    MammaryGlandRecipeMap.set(entityTypeId, {
        outputItem: outputItem,
        damage: damage,
        oriItem: oriItem
    })
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function MammaryGlandEntityBeInteracted(customData, event, organItem, organIndex, slotType) {
    const item = event.item
    /**@type {Internal.ServerPlayer} */
    const entity = event.entity
    const target = event.target
    const targetType = target.getType()
    if (item.isEmpty()) return
    if (targetType == 'minecraft:cow') return
    if (target.invulnerableTime > 0) target.invulnerableTime = 0
    let recipe = MammaryGlandRecipeMap.has(String(targetType)) ? MammaryGlandRecipeMap.get(String(targetType)) : MammaryGlandRecipeMap.get('default')

    if (!item.is(recipe['oriItem'])) return
    let bucketItem = recipe['outputItem']
    if (recipe['damage']) {
        target.attack(entity.damageSources().magic(), recipe['damage'])
    }

    if (!entity.isCreative()) item.shrink(1)

    entity.playSound('item.bucket.fill_milk')
    entity.give(bucketItem.withName(Text.translate('item_name.kubejs.mammary_gland_bucket.name', target.getName())))
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:mammary_gland')
        .addOnlyStrategy('entity_be_interacted', MammaryGlandEntityBeInteracted)
)

registryMammaryGlandRecipe('default', Item.of('minecraft:bucket'), Item.of('minecraft:milk_bucket'), 2)
registryMammaryGlandRecipe('minecraft:bee', Item.of('minecraft:glass_bottle'), Item.of('minecraft:honey_bottle'), 4)
registryMammaryGlandRecipe('minecraft:panda', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:strength"}'), 4)
registryMammaryGlandRecipe('minecraft:frog', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:leaping"}'), 4)
registryMammaryGlandRecipe('minecraft:bat', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:night_vision"}'), 4)
registryMammaryGlandRecipe('minecraft:glow_squid', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:night_vision"}'), 4)
registryMammaryGlandRecipe('minecraft:ghast', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:regeneration"}'), 6)
registryMammaryGlandRecipe('minecraft:axolotl', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:regeneration"}'), 6)
registryMammaryGlandRecipe('minecraft:shulker', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:slow_falling"}'), 4)
registryMammaryGlandRecipe('minecraft:polar_bear', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:strength"}'), 4)
registryMammaryGlandRecipe('minecraft:ravager', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:strength"}'), 4)
registryMammaryGlandRecipe('minecraft:zoglin', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:strength"}'), 4)
registryMammaryGlandRecipe('minecraft:strider', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:fire_resistance"}'), 4)
registryMammaryGlandRecipe('minecraft:dolphin', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:fox', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:horse', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:donkey', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:mule', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:llama', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:trader_llama', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:ocelot', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:swiftness"}'), 4)
registryMammaryGlandRecipe('minecraft:turtle', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:turtle_master"}'), 4)
registryMammaryGlandRecipe('minecraft:squid', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:water_breathing"}'), 4)
registryMammaryGlandRecipe('minecraft:pufferfish', Item.of('minecraft:glass_bottle'), Item.of('minecraft:potion', '{Potion:"minecraft:water_breathing"}'), 4)
registryMammaryGlandRecipe('minecraft:zombified_piglin', Item.of('minecraft:bucket'), Item.of('kubejs:molten_gold_fluid_bucket'), 15)
registryMammaryGlandRecipe('minecraft:drowned', Item.of('minecraft:bucket'), Item.of('kubejs:molten_copper_fluid_bucket'), 15)
registryMammaryGlandRecipe('minecraft:iron_golem', Item.of('minecraft:bucket'), Item.of('kubejs:molten_iron_fluid_bucket'), 25)
registryMammaryGlandRecipe('minecraft:magma_cube', Item.of('minecraft:slime_ball'), Item.of('minecraft:magma_cream'), 1)
registryMammaryGlandRecipe('minecraft:villager', Item.of('minecraft:bucket'), Item.of('kubejs:molten_emerald_fluid_bucket'), 15)
registryMammaryGlandRecipe('minecraft:ender_dragon', Item.of('minecraft:glass_bottle'), Item.of('minecraft:dragon_breath'), 10)
// registryMammaryGlandRecipe('dracolotl:dracolotl', Item.of('minecraft:glass_bottle'), Item.of('minecraft:dragon_breath'), 1)

// ===== basic_organ.js =====
RegistryOrgan('kubejs:ender_breath')
    .addScore('chestcavity:health', 1)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:ender_ectoplasm')
    .addScore('chestcavity:nerves', 1)
    .addScore('chestcavity:defense', 1)
    .setCanSpawn(true)

// ===== dragon_blood.js =====
RegistryOrgan('kubejs:dragon_blood_liver')
    .addScore('chestcavity:detoxification', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_lung')
    .addScore('chestcavity:breath_capacity', 1)
    .addScore('chestcavity:breath_recovery', 1)
    .addScore('chestcavity:endurance', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_muscle')
    .addScore('chestcavity:strength', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_rib')
    .addScore('chestcavity:defense', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_kidney')
    .addScore('chestcavity:filtration', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_spine')
    .addScore('chestcavity:nerves', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_stomach')
    .addScore('chestcavity:digestion', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:dragon_blood_intestine')
    .addScore('chestcavity:nutrition', 2)
    .addScore('kubejs:dragon_blood', 1)
    .setCanSpawn(true)

// ===== dragon_blood_heart.js =====
// RegistryOrgan('kubejs:dragon_blood_heart')
    // .addScore('chestcavity:health', 2)
    // .addScore('kubejs:dragon_blood', 1)
    // .setCanSpawn(true)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DragonBloodHeartChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:advance_dragon_breath', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DragonBloodHeartTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:advance_dragon_breath')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:dragon_blood_heart')
        // .addOnlyStrategy('chest_cavity_update', DragonBloodHeartChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', DragonBloodHeartTakeOff)
// )

// ===== ender_eye.js =====
// RegistryOrgan('kubejs:ender_eye')
    // .addScore('chestcavity:luck', 1)
    // .addScore('chestcavity:nerves', 1)
    // .setCanSpawn(true)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function EnderEyeChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'irons_spellbooks:teleport', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function EnderEyeTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'irons_spellbooks:teleport')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:ender_eye')
        // .addOnlyStrategy('chest_cavity_update', EnderEyeChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', EnderEyeTakeOff)
// )

// ===== ender_golem_line.js =====
RegistryOrgan('kubejs:ender_golem_line')
    .addScore('kubejs:extreme_fitness', 1)
    .addScore('chestcavity:nerves', 0.5)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function EnderGolemLineKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    let playerPos = player.blockPosition()
    let entityList = GetLivingWithinRadius(level, playerPos, 10, (level, entity) => {
        return !entity.equals(player)
    })
    entityList.forEach(entity => {
        let entityPos = entity.position()
        let diff = playerPos.subtract(entityPos)
        diff = diff.normalize().scale(1)
        entity.setDeltaMovement(diff)
        for (let i = 0; i < 3; i++) {
            level.spawnParticles($ParticleTypes.PORTAL, true, entityPos.x() + Math.random(), entityPos.y() + Math.random(), entityPos.z() + Math.random(), 0.2, 0.3, 0.2, 5, 0)
        }
    })
    player.addItemCooldown(organItem, 20 * 3)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:ender_golem_line')
        .addOnlyStrategy('key_active', EnderGolemLineKeyActive)
)

// ===== endermaptera_shell.js =====
RegistryOrgan('kubejs:endermaptera_shell')
    .addScore('chestcavity:defense', 1)
    .setCanSpawn(true)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function EndermapteraShellEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = entity.level
    if (Math.random() > 0.1) return
    let entityPos = entity.position()
    level.playSound(null, entityPos.x(), entityPos.y(), entityPos.z(), 'entity.enderman.teleport', entity.getSoundSource(), 1, 1)
    level.spawnParticles($ParticleTypes.PORTAL, true, entityPos.x() + Math.random(), entityPos.y() + Math.random(), entityPos.z() + Math.random(), 0.2, 0.3, 0.2, 10, 0)
    event.amount = 0
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:endermaptera_shell')
        .addOnlyStrategy('entity_be_hurt', EndermapteraShellEntityBeHurt)
)

// ===== endermite_gland.js =====
RegistryOrgan('kubejs:endermite_gland')
    .addScore('chestcavity:endurance', 1)
    .addScore('chestcavity:filtration', 1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function EndermiteGlandEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    /**@type {Internal.PathfinderMob} */
    let entities = GetLivingWithinRadiusVec3d(level, entity.position(), 16, (pLevel, pEntity) => {
        if (!(pEntity instanceof $PathfinderMob)) return false
        if (!pEntity.isMonster()) return false
        if (pEntity.getTarget() && pEntity.getTarget().equals(entity)) return false
        return true
    })
    entities.forEach((pEntity) => {
        pEntity.setTarget(entity)
    })
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:endermite_gland')
    .addOnlyStrategy('entity_tick', EndermiteGlandEntityTick)
)

// ===== phantom_wing.js =====
RegistryOrgan('kubejs:phantom_wing')
    .addScore('kubejs:light_weight', 2)
    .setCanSpawn(true)

// ===== baguette_bone.js =====
RegistryOrgan('kubejs:baguette_bone')
    .addScore('chestcavity:defense', 1)
    .addScore('chestcavity:digestion', 1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BaguetteBoneChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const entity = event.entity
    
    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.isEdible()) continue
        let foodProperties = pItem.getFoodProperties(entity)
        let foodHunger = foodProperties.getNutrition()
        let foodSaturation = foodHunger * foodProperties.getSaturationModifier() * 2
        value += foodSaturation
    }
    if (value != 0) {
        customData.armor.addAttributeModifier(value / 8, 'addition', 'base')
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:baguette_bone')
        .addStrategy('chest_cavity_update', BaguetteBoneChestCavityUpdate)
)

// ===== beer_gland.js =====
RegistryOrgan('kubejs:beer_gland')
    .addScore('chestcavity:filtration', 1)
    .addScore('chestcavity:fire_resistant', -1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BeerGlandChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    if (slotType == DigestSlot) {
        SetCustomData(customData, 'hasBeerGland', 2)
    } else {
        SetCustomData(customData, 'hasBeerGland', 1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:beer_gland')
        .addOnlyStrategy('chest_cavity_update', BeerGlandChestCavityUpdate, 10)
)

// ===== gluten_muscle.js =====
RegistryOrgan('kubejs:gluten_muscle')
    .addScore('chestcavity:defense', 1.5)
    .addScore('chestcavity:digestion', 1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function GlutenMuscleChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const entity = event.entity
    
    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, FourDiagonalDirectionOffset)
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.isEdible()) continue
        let foodProperties = pItem.getFoodProperties(entity)
        let foodHunger = foodProperties.getNutrition()
        let foodSaturation = foodHunger * foodProperties.getSaturationModifier() * 2
        value += foodSaturation
    }
    if (value != 0) {
        customData.attackDamage.addAttributeModifier(value / 2, 'addition', 'base')
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:gluten_muscle')
        .addStrategy('chest_cavity_update', GlutenMuscleChestCavityUpdate)
)

// ===== greedy_throat.js =====
RegistryOrgan('kubejs:greedy_throat')
    .addScore('chestcavity:breath_capacity', 1)
    .addScore('chestcavity:knockback_resistant', -1)



/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.FoodEatenEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function GreedyThroatFoodEaten(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    if (!player) return
    const chestCavity = player.chestCavityInstance
    const ccInv = chestCavity.inventory
    const foodItem = event.item
    if (!foodItem) return
    if (OrganItemCoolDownSlience(player, organItem)) return
    let itemCopy = foodItem.copyWithCount(1)
    let nbt = itemCopy.getOrCreateTag()
    if (nbt.contains('greedyThroatUUID')) {
        let greedyUuid = nbt.getUUID('greedyThroatUUID')
        if (greedyUuid.equals(player.getUuid())) return
    }
    nbt.putUUID('greedyThroatUUID', player.getUuid())

    let canSetSlotList = []
    for (let i = 0; i < ccInv.getContainerSize(); i++) {
        if (ccInv.getItem(i).isEmpty()) {
            canSetSlotList.push(i)
        }
    }

    let targetIndex = -1
    if (canSetSlotList.length > 0) {
        targetIndex = RandomGet(canSetSlotList)
    } else {
        return
    }
    let targetSlotType = chestCavity.inventoryTypeData.getSlotType(targetIndex)
    SetChestCavityOrgan(customData, chestCavity, itemCopy, targetIndex, targetSlotType, true)
    if (player instanceof $ServerPlayer) {
        player.addItemCooldown(organItem, 20 * 10)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:greedy_throat')
        .addOnlyStrategy('food_eaten', GreedyThroatFoodEaten)
)

// ===== gula_beacon.js =====
RegistryOrgan('kubejs:gula_beacon')
    .addScore('chestcavity:health', 1)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function GulaBeaconChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.isPlayer()) return
    if (!(entity instanceof $PathfinderMob)) return
    RemoveCustomGoalByName(entity.goalSelector, 'gula_challenge')
    entity.goalSelector.addGoal(0, NewGulaChallengeGoal(entity))
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function GulaBeaconChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.isPlayer()) return
    if (!(entity instanceof $PathfinderMob)) return
    RemoveCustomGoalByName(entity.goalSelector, 'gula_challenge')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:gula_beacon')
        .addOnlyStrategy('organ_take_on', GulaBeaconChestCavityTakeOn)
        .addOnlyStrategy('organ_take_off', GulaBeaconChestCavityTakeOff)
)

// ===== gula_worm_nest.js =====
RegistryOrgan('kubejs:gula_worm_nest')
    .addScore('chestcavity:health', 0.1)
    .addScore('chestcavity:filtration', 0.1)
    .addScore('chestcavity:nerves', 0.1)
    .addScore('chestcavity:digestion', 0.1)
    .addScore('chestcavity:nutrition', 0.1)
    .addScore('chestcavity:endurance', -0.5)
    .addScore('chestcavity:breath_capacity', 0.1)
    .addScore('chestcavity:breath_recovery', 0.1)

RegistryOrgan('kubejs:deserted_gula_worm_nest')
    .addScore('chestcavity:health', 0.1)
    .addScore('chestcavity:filtration', 0.1)
    .addScore('chestcavity:nerves', 0.1)
    .addScore('chestcavity:digestion', 0.1)
    .addScore('chestcavity:nutrition', 0.1)
    .addScore('chestcavity:breath_capacity', 0.1)
    .addScore('chestcavity:breath_recovery', 0.1)

// ===== jar_of_mystery.js =====
RegistryOrgan('kubejs:jar_of_mystery')
    .addScore('chestcavity:endurance', -1)
    .addScore('chestcavity:digestion', -1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.FoodEatenEventJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function JarOfMysteryFoodEaten(customData, event, organItem, organIndex, slotType) {
    const player = event.getPlayer()
    if (!player) return
    let curDamage = organItem.getDamageValue() + 1
    player.potionEffects.add('minecraft:absorption', 20 * 20, 0)
    if (curDamage >= organItem.getMaxDamage()) {
        // [maa禁用] MAAUtils.onKubeTaskFinish('jar_of_mystery_grow', player, (pTask, pPlayer, pTeamData) => {
        //     pTeamData.addProgress(pTask, 1)
        // })
        let replaceItem = Item.of('kubejs:worm_larva')
        replaceItem.setDamageValue(replaceItem.getMaxDamage())
        SetChestCavityOrgan(customData, player.chestCavityInstance, replaceItem, organIndex, slotType, true)
    } else {
        organItem.setDamageValue(curDamage)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:jar_of_mystery')
        .addOnlyStrategy('food_eaten', JarOfMysteryFoodEaten)
)

// ===== jar_of_vacuum.js =====
RegistryOrgan('kubejs:jar_of_vacuum')
    .addScore('chestcavity:health', -0.5)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function JarOfVacuumChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory

    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let curItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (curItem.isEmpty()) continue
        if (curItem.hasTag('kubejs:food')) {
            value += 1
        }
    }
    if (value != 0) {
        customData.attackDamage.addAttributeModifier(value, 'addition', 'base')
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:jar_of_vacuum')
        .addStrategy('chest_cavity_update', JarOfVacuumChestCavityUpdate)
)

// ===== king_of_stomach.js =====
RegistryOrgan('kubejs:king_of_stomach')
    .addScore('chestcavity:endurance', -3)
    .addScore('chestcavity:nutrition', -1)
    .addScore('chestcavity:metabolism', 1)

const CakeFoodProperties = { 'minecraft:cake': { 'nutrition': 14, 'saturation': 2.8 }, 'bakery:strawberry_cake': { 'nutrition': 20, 'saturation': 14 }, 'bakery:sweetberry_cake': { 'nutrition': 20, 'saturation': 14 }, 'bakery:chocolate_cake': { 'nutrition': 20, 'saturation': 14 }, 'bakery:chocolate_gateau': { 'nutrition': 20, 'saturation': 14 }, 'bakery:bundt_cake': { 'nutrition': 20, 'saturation': 14 }, 'bakery:linzer_tart': { 'nutrition': 20, 'saturation': 14 }, 'bakery:apple_pie': { 'nutrition': 20, 'saturation': 14 }, 'bakery:glowberry_tart': { 'nutrition': 20, 'saturation': 14 }, 'bakery:chocolate_tart': { 'nutrition': 20, 'saturation': 14 }, 'bakery:pudding': { 'nutrition': 20, 'saturation': 14 }}

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function KingOfStomachChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    const inventory = chestCavity.inventory
    const level = event.level
    let onlySet = new Set()
    let healthUp = 0
    let attackUp = 0
    const sweetsGlandEffect = GetCustomDataOrDefault(customData, 'hasSweetsGland', 0)
    const beerGlandEffect = GetCustomDataOrDefault(customData, 'hasBeerGland', 0)

    inventory.allItems.forEach(item => {
        if (item.isEmpty()) return
        let itemId = String(item.getId())
        if (!onlySet.has(itemId)) {
            let isGreedy = false
            if (item.hasNBT()) {
                let nbt = item.getNbt()
                if (nbt.contains('greedyThroatUUID')) isGreedy = true
            }
            if (item.hasTag('kubejs:cake') && sweetsGlandEffect == 2) {
                let foodPro = CakeFoodProperties[item.getId().toString()]
                if (!foodPro) return

                attackUp = attackUp + foodPro.saturation * 2
                healthUp = healthUp + foodPro.nutrition / 4
            } else if (item.hasTag('kubejs:sweets') && sweetsGlandEffect >= 1) {
                let foodPro = item.getFoodProperties(entity)
                if (!foodPro) return
                let nutrition = foodPro.getNutrition() + (isGreedy ? 2 : 0)
                let staturation = foodPro.getSaturationModifier() * nutrition * 2

                attackUp = attackUp + staturation * 0.25
                healthUp = healthUp + nutrition * 0.5
            } else if (item.hasTag('kubejs:beer') && beerGlandEffect >= 1) {
                let foodPro = item.getFoodProperties(entity)
                if (!foodPro) return
                let nutrition = foodPro.getNutrition() + (isGreedy ? 2 : 0)
                let staturation = foodPro.getSaturationModifier() * nutrition * 2
                attackUp = attackUp + staturation * 1.25 + nutrition * 1.25
                if (beerGlandEffect == 2) {
                    // 允许重复放置
                    return
                }
            // } else if (item.hasTag('kubejs:wine') && beerGlandEffect == 2) {
            //     // let age = $WineYears.getWineAge(item, level)
            //     let age = 2
            //     attackUp = attackUp + 1 * age
            } else {
                let foodPro = item.getFoodProperties(entity)
                if (!foodPro) return
                let nutrition = foodPro.getNutrition() + (isGreedy ? 2 : 0)

                let staturation = foodPro.getSaturationModifier() * nutrition * 2
                attackUp = attackUp + staturation * 0.1
                healthUp = healthUp + nutrition * 0.25

            }
            onlySet.add(itemId)
        }

    })
    if (slotType == GulaSlot) {
        attackUp = attackUp * 2
        healthUp = healthUp * 2
    }
    customData.attackDamage.addAttributeModifier(attackUp, 'addition', 'base')
    customData.maxHealth.addAttributeModifier(healthUp, 'addition', 'base')
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function KingOfStomachTakeOn(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    if (slotType == GulaSlot && entity instanceof $ServerPlayer) {
        entity.foodData.setNoAddExhaustion(true)
    }
    OrganSkinAdd(entity, 'chest', 'secret/chestcavity/gula_body')
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function KingOfStomachTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    if (slotType == GulaSlot && entity instanceof $ServerPlayer) {
        entity.foodData.setNoAddExhaustion(false)
    }
    OrganSkinRemove(entity, 'chest', 'secret/chestcavity/gula_body')
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:king_of_stomach')
        .addOnlyStrategy('organ_take_on', KingOfStomachTakeOn)
        .addOnlyStrategy('organ_take_off', KingOfStomachTakeOff)
        .addOnlyStrategy('chest_cavity_update', KingOfStomachChestCavityUpdate)
)

// ===== living_beef_wellington.js =====
RegistryOrgan('kubejs:living_beef_wellington')
    .addScore('chestcavity:strength', 1)
    .addScore('kubejs:extreme_strength', -1)
    .addScore('chestcavity:defense', -1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function LivingBeefWellingtonChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const entity = event.entity

    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, [[0, -1], [0, -2], [0, -3], [0, -4]])
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.isEdible()) continue
        let foodProperties = pItem.getFoodProperties(entity)
        if (foodProperties.getSaturationModifier() * 2 >= 1) continue
        value += 1
    }
    if (value != 0) {
        chestCavity.setOrganScore('kubejs:extreme_strength', chestCavity.getOrganScore('kubejs:extreme_strength') + value)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:living_beef_wellington')
        .addStrategy('chest_cavity_update', LivingBeefWellingtonChestCavityUpdate)
)

// ===== mashed_potato_pancreas.js =====
RegistryOrgan('kubejs:mashed_potato_pancreas')
    .addScore('chestcavity:digestion', 1.5)
    .addScore('kubejs:extreme_fitness', -1)
    .addScore('chestcavity:defense', -1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MashedPotatoPancreasChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory

    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, [[-1, 0], [1, 0], [-2, 0], [2, 0]])
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.hasTag('kubejs:food')) continue
        if (pItem.is('kubejs:mashed_potato_pancreas')) continue
        value += 1
    }
    if (value != 0) {
        chestCavity.setOrganScore('kubejs:extreme_fitness', chestCavity.getOrganScore('kubejs:extreme_fitness') + value)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:mashed_potato_pancreas')
        .addStrategy('chest_cavity_update', MashedPotatoPancreasChestCavityUpdate)
)

// ===== parasitism_stomach.js =====
RegistryOrgan('kubejs:parasitism_stomach')
    .addScore('chestcavity:endurance', 0.5)
    .addScore('chestcavity:nutrition', 1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.FoodEatenEventJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ParasitismStomachFoodEaten(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    if (!player) return
    if (OrganItemCoolDown(player, organItem)) return
    const chestCavity = player.chestCavityInstance
    const ccInv = chestCavity.inventory
    const foodItem = event.item
    if (!foodItem) return
    let foodPro = foodItem.getFoodProperties(player)
    if (!foodPro) return
    let nutrition = foodPro.getNutrition()
    let staturation = foodPro.getSaturationModifier() * nutrition * 2
    let organItemCount = ccInv.count(foodItem)
    let attackUp = (staturation + nutrition) * organItemCount * 0.2
    SetCustomDataMap(chestCavity, 'parasitismStomachCounter', 3)
    SetCustomDataMap(chestCavity, 'parasitismStomachAttackUp', attackUp)
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText('3')
    SetOrganEffect(chestCavity, organEffect)
    player.addItemCooldown(organItem, 20 * 3)
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ParasitismStomachTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'parasitismStomachCounter', 0)
    SetCustomDataMap(chestCavity, 'parasitismStomachAttackUp', 0)
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:parasitism_stomach')
    }
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ParasitismStomachDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance
    let counter = GetCustomDataMap(chestCavity, 'parasitismStomachCounter', 0)
    if (counter == 0) return
    event.amount = event.amount + GetCustomDataMap(chestCavity, 'parasitismStomachAttackUp', 0)
    let curCounter = counter - 1
    SetCustomDataMap(chestCavity, 'parasitismStomachCounter', curCounter)
    if (curCounter == 0) {
        RemoveOrganEffect(chestCavity, 'kubejs:parasitism_stomach')
    } else {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText(curCounter.toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:parasitism_stomach')
        .addOnlyStrategy('food_eaten', ParasitismStomachFoodEaten)
        .addOnlyStrategy('organ_take_off', ParasitismStomachTakeOff)
        .addOnlyStrategy('entity_do_damage', ParasitismStomachDoDamage)
)

// ===== sweet_heart.js =====
RegistryOrgan('kubejs:sweet_heart')
    .addScore('chestcavity:health', 1.5)
    .addScore('chestcavity:nutrition', 1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SweetHeartChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const entity = event.entity
    
    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.isEdible()) continue
        let foodProperties = pItem.getFoodProperties(entity)
        let foodHunger = foodProperties.getNutrition()
        value += foodHunger
    }
    if (value != 0) {
        chestCavity.setOrganScore('chestcavity:nhealth', chestCavity.getOrganScore('chestcavity:nutrition') + value)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:sweet_heart')
        .addStrategy('chest_cavity_update', SweetHeartChestCavityUpdate)
)

// ===== sweets_gland.js =====
RegistryOrgan('kubejs:sweets_gland')
    .addScore('chestcavity:endurance', 1)
    .addScore('chestcavity:impact_resistant', -1)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SweetsGlandChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    if (slotType == DigestSlot) {
        SetCustomData(customData, 'hasSweetsGland', 2)
    } else {
        SetCustomData(customData, 'hasSweetsGland', 1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:sweets_gland')
        .addOnlyStrategy('chest_cavity_update', SweetsGlandChestCavityUpdate, 10)
)

// ===== tasty_gland.js =====
RegistryOrgan('kubejs:tasty_gland')
    .addScore('chestcavity:endurance', -1.5)
    .addScore('chestcavity:digestion', 1)
    .setCanSpawn(true)


/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TastyGlandEntityBeInteracted(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    /**@type {Internal.PathfinderMob} */
    const target = event.target
    if (!target.isLiving()) return
    const level = event.level
    if (event.getHand() != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    let recoverAmount = Clamp(target.getHealth() * 0.5, 0, 40)
    let leftAmount = RecoverPlayerHungerAndSaturation(player, recoverAmount)
    recoverAmount = recoverAmount - leftAmount
    target.attack(player.damageSources().magic(), recoverAmount)
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 0.5, 1)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:tasty_gland')
        .addOnlyStrategy('entity_be_interacted', TastyGlandEntityBeInteracted)
)

// ===== void_stomach_pouch.js =====
RegistryOrgan('kubejs:void_stomach_pouch')
    .addScore('chestcavity:digestion', 2)
    .addScore('chestcavity:nutrition', -0.5)
    .addScore('chestcavity:endurance', -3)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.FoodEatenEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function VoidStomachPouchFoodEaten(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const foodItem = event.item
    const level = event.level
    // if (AStages.serverHasStage(FTBFinalTimerStart, event.server)) return
    if (!foodItem) return
    let foodItemId = String(foodItem.id)
    if (!organItem.hasNBT()) organItem.setNbt(new $CompoundTag())
    let nbt = organItem.getNbt()
    if (organItem.getDamageValue() <= 0 && !nbt.getBoolean('start')) nbt.putBoolean('start', true)
    let foodTypeMap = nbt.getCompound('foodTypeMap')
    let eatCount = foodTypeMap.getInt(foodItemId) + 1
    foodTypeMap.putInt(foodItemId, eatCount)
    let comboList = nbt.getList('comboList', TAG_STRING)
    if (comboList.contains(NBT.stringTag(foodItemId))) {
        comboList.clear()
    }
    comboList.add(NBT.stringTag(foodItemId))
    nbt.put('comboList', comboList)
    let statusMsg = Text.translatable('status_msg.kubejs.void_stomach_pouch.item_info', Text.translatable(foodItem.getDescriptionId())).gold()
    let comboCount = comboList.size()
    statusMsg.append(Text.translatable('status_msg.kubejs.void_stomach_pouch.combo_info', comboCount.toFixed(0)).gold())

    let sizeIncr = Math.floor(comboCount / 2)
    if (sizeIncr > 0) {
        nbt.putInt('sizeIncr', nbt.getInt('sizeIncr') + sizeIncr)
        statusMsg.append(Text.translatable('status_msg.kubejs.void_stomach_pouch.size_info', sizeIncr.toFixed(0)).aqua())
    }
    player.setStatusMessage(statusMsg)
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'block.amethyst_block.break', player.getSoundSource(), 1, 1)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function VoidStomachPouchEntityTick(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const player = event.entity
    const level = event.level
    if (!player.isPlayer()) return
    if (!organItem.hasNBT()) return
    let nbt = organItem.getNbt()
    if (!nbt.getBoolean('start')) return
    let curDamage = organItem.getDamageValue() + 1
    if (curDamage >= organItem.getMaxDamage()) {
        // if (AStages.serverHasStage(FTBFinalTimerStart, event.server)) return
        let dimNet = DimensionsNet.getPrimaryNetFromPlayer(player)
        if (!dimNet) {
            player.setStatusMessage(Text.translatable('status_msg.kubejs.void_stomach_pouch.no_dimnet'))
            RemoveChestCavityOrgan(customData, event.chestCavity, organIndex, slotType, true)
            return
        }
        let unifiedStorage = dimNet.getUnifiedStorage()
        let sizeIncr = nbt.getInt('sizeIncr')
        unifiedStorage.setSlotMaxSize(unifiedStorage.slotMaxSize + sizeIncr)
        player.setStatusMessage(Text.translatable('status_msg.kubejs.void_stomach_pouch.summary', sizeIncr.toFixed(0)).gold())
        RemoveChestCavityOrgan(customData, event.chestCavity, organIndex, slotType, true)
        // todo 要是有粒子效果会更好，优先级靠后
        level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 1, 1)
        player.addItemCooldown(organItem, 20 * 300)
    } else {
        organItem.setDamageValue(curDamage)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:void_stomach_pouch')
        .addOnlyStrategy('food_eaten', VoidStomachPouchFoodEaten)
        .addOnlyStrategy('entity_tick', VoidStomachPouchEntityTick)
)

// ===== worm_larva.js =====
RegistryOrgan('kubejs:worm_larva')
    .addScore('chestcavity:filtration', 1)
    .addScore('chestcavity:endurance', -1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.FoodEatenEventJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ParasiteLarvaFoodEaten(customData, event, organItem, organIndex, slotType) {
    const player = event.getPlayer()
    if (!player) return
    const item = event.getItem()
    const level = event.getLevel()
    if (!organItem.hasNBT()) {
        organItem.setNbt(new $CompoundTag())
    }
    let nbt = organItem.getNbt()
    let saturation = nbt.getFloat('saturation')

    let foodProperties = item.getFoodProperties(player)
    let foodHunger = foodProperties.getNutrition()
    let foodSaturation = foodProperties.getSaturationModifier() * foodHunger * 2
    let curSaturation = saturation + foodSaturation
    nbt.putFloat('saturation', curSaturation)
    let curDamage = organItem.getDamageValue() - foodHunger
    if (curDamage <= 0) {
        let ratio = curSaturation / (organItem.getMaxDamage() - curDamage)
        if (ratio >= 1) {
            let chestCavity = player.getChestCavityInstance()
            if (!chestCavity) return
            let invTypeData = chestCavity.getInventoryTypeData()
            let ccInv = chestCavity.inventory
            let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
            let emptyItemCount = 0
            for (let slotDefinition of aroundRelativeSlots) {
                let pItem = ccInv.getStackInSlot(slotDefinition.getId())
                if (!pItem || pItem.isEmpty()) {
                    emptyItemCount++
                    continue
                }
                let replaceItem
                if (pItem.hasTag('kubejs:heart')) {
                    replaceItem = Item.of('kubejs:king_of_stomach')
                } else if (pItem.hasTag('kubejs:bone')) {
                    replaceItem = Item.of('kubejs:greedy_throat')
                } else if (pItem.hasTag('kubejs:liver')) {
                    replaceItem = Item.of('kubejs:beer_gland')
                } else if (pItem.hasTag('kubejs:kidney')) {
                    replaceItem = Item.of('kubejs:sweets_gland')
                } else if (pItem.hasTag('kubejs:stomach')) {
                    replaceItem = Item.of('kubejs:parasitism_stomach')
                }
                if (!replaceItem) continue
                RemoveChestCavityOrgan(customData, player.chestCavityInstance, organIndex, slotType, true)
                SetChestCavityOrgan(customData, player.chestCavityInstance, replaceItem, slotDefinition.getId(), slotDefinition.getType(), true)
                level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 1, 1)
                return
            }
            // 虚空胃袋转化
            if (emptyItemCount >= aroundRelativeSlots.length) {
                SetChestCavityOrgan(customData, player.chestCavityInstance, Item.of('kubejs:void_stomach_pouch'), organIndex, slotType, true)
                level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 1, 1)
                return
            }
        }
        // 美味虫
        let replaceItem = Item.of('kubejs:worm_of_taste')
        SetChestCavityOrgan(customData, player.chestCavityInstance, replaceItem, organIndex, slotType, true)
    } else {
        organItem.setDamageValue(curDamage)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:worm_larva')
        .addOnlyStrategy('food_eaten', ParasiteLarvaFoodEaten)
)

// ===== worm_of_gula.js =====
RegistryOrgan('kubejs:worm_of_gula')
    .addScore('chestcavity:digestion', 1)
    .addScore('chestcavity:nutrition', 1)
    .addScore('chestcavity:endurance', 1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function WormOfGulaEntityTick(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const player = event.entity
    if (player.age % 200 != 0) return
    const level = event.level
    if (!player.isPlayer()) return
    const foodData = player.getFoodData()
    if (foodData.getFoodLevel() < 18) return
    const chestCavity = event.chestCavity
    let inventory = chestCavity.inventory
    if (inventory.canAddItem(Item.of('kubejs:gula_worm_nest'))) {
        inventory.addItem(Item.of('kubejs:gula_worm_nest'))
        foodData.setFoodLevel(4)
        foodData.setSaturation(0)
        level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 0.5, 1)
        player.potionEffects.add('minecraft:nausea', 100, 0, false, true)
    }

    if (!inventory.hasAnyMatching((ctx) => !ctx.is('kubejs:gula_worm_nest') && !ctx.is('kubejs:worm_of_gula'))) {
        chestCavity.setInventoryType('kubejs:cc_inventory_types/gula')
        level.playSound(null, player.getX(), player.getY(), player.getZ(), 'ui.toast.challenge_complete', player.getSoundSource(), 0.25, 1)
        inventory = chestCavity.inventory
        inventory.clear()
        for (let i = 0; i < inventory.getContainerSize(); i++) {
            inventory.setStackInSlot(i, Item.of('kubejs:deserted_gula_worm_nest'))
        }
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:worm_of_gula')
        .addOnlyStrategy('entity_tick', WormOfGulaEntityTick)
)

// ===== worm_of_taste.js =====
RegistryOrgan('kubejs:worm_of_taste')
    .addScore('chestcavity:filtration', 2)
    .addScore('chestcavity:endurance', 1)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function WormOfTasteChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const invTypeData = chestCavity.getInventoryTypeData()

    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
    let value = 0
    for (let slotDefinition of aroundRelativeSlots) {
        let pItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (pItem.isEmpty() || !pItem.isEdible()) continue
        let foodProperties = pItem.getFoodProperties(entity)
        let foodHunger = foodProperties.getNutrition()
        value += foodHunger
    }
    customData.attackDamage.addAttributeModifier(value / 4, 'addition', 'base')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:worm_of_taste')
        .addStrategy('chest_cavity_update', WormOfTasteChestCavityUpdate)
)

// ===== basic_organs.js =====
RegistryOrgan('kubejs:rotten_appendix').addScore('chestcavity:luck', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_intestine').addScore('chestcavity:nutrition', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_heart').addScore('chestcavity:health', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_kidney').addScore('chestcavity:filtration', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_liver').addScore('chestcavity:detoxification', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_lung').addScore('chestcavity:breath_recovery', 0.75).addScore('chestcavity:breath_capacity', 0.75).addScore('chestcavity:endurance', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_muscle').addScore('chestcavity:strength', 1.5).addScore('chestcavity:speed', -0.25).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_rib').addScore('chestcavity:defense', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_spine').addScore('chestcavity:nerves', 0.75).addScore('chestcavity:speed', -0.25).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_spleen').addScore('chestcavity:metabolism', 0.75).setCanSpawn(true)
RegistryOrgan('kubejs:rotten_stomach').addScore('chestcavity:digestion', 0.75).setCanSpawn(true)

// ===== bone_cage.js =====
RegistryOrgan('kubejs:bone_cage')
    .addScore('chestcavity:defense', 3)
    .addScore('chestcavity:breath_capacity', -1)
    .addScore('chestcavity:endurance', -0.5)
    .setCanSpawn(true)

// ===== bone_meal_bag.js =====
RegistryOrgan('kubejs:bone_meal_bag')
    .addScore('chestcavity:digestion', 2)
    .addScore('chestcavity:detoxification', -0.5)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BoneMealBagEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.age % 200 != 0) return
    AddItemIntoBundle(organItem, Item.of('minecraft:bone_meal'), 64, (pStack) => pStack.getMaxStackSize() / 64)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.NetworkEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BoneMealBagKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    let playerPos = player.position()
    let itemList = GetBundleContents(organItem)
    if (itemList.length <= 0) return
    let gunpowderItem = itemList[0]
    if (!gunpowderItem.is('minecraft:bone_meal')) return
    let boneMealCount = gunpowderItem.getCount()
    /**@type {Internal.LivingEntity[]} */
    let entityList = GetLivingWithinRadiusVec3d(level, playerPos, 5, (plevel, pEntity) => true)
    entityList.forEach(pEntity => pEntity.potionEffects.add('minecraft:blindness', 20 * boneMealCount, 0, false, true))
    RemoveBundleItem(organItem, 0, boneMealCount)
    level.createExplosion(playerPos.x(), playerPos.y(), playerPos.z()).explode()
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:bone_meal_bag')
        .addOnlyStrategy('entity_tick', BoneMealBagEntityTick)
        .addOnlyStrategy('key_active', BoneMealBagKeyActive)
)

// ===== devour_teeth.js =====
// RegistryOrgan('kubejs:devour_teeth')
    // .addScore('chestcavity:strength', 1)
    // .addScore('chestcavity:defense', 1)
    // .setCanSpawn(true)
// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DevourTeethChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:advance_devour', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DevourTeethTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:advance_devour')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:devour_teeth')
        // .addOnlyStrategy('chest_cavity_update', DevourTeethChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', DevourTeethTakeOff)
// )

// ===== malignant_neuron_tumor.js =====
RegistryOrgan('kubejs:malignant_neuron_tumor')
    .addScore('chestcavity:digestion', -10)
    .addScore('chestcavity:nutrition', -10)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MalignantNeuronTumorEntityTick(customData, event, organItem, organIndex, slotType) {
    if (organItem.getDamageValue() <= 0) return
    organItem.setDamageValue(organItem.getDamageValue() - 1)
}
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MalignantNeuronTumorEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const entity = event.entity
    const chestCavity = entity.chestCavityInstance
    let maxDamage = organItem.getMaxDamage()
    let newDamageValue = organItem.getDamageValue() + event.getAmount()
    if (newDamageValue >= maxDamage) {
        SetChestCavityOrgan(customData, chestCavity, Item.of('kubejs:inactivated_neuron_tumor'), organIndex, slotType, true)
        return
    } else {
        organItem.setDamageValue(newDamageValue)
    }
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:malignant_neuron_tumor')
        .addOnlyStrategy('entity_tick', MalignantNeuronTumorEntityTick)
        .addOnlyStrategy('entity_be_hurt', MalignantNeuronTumorEntityBeHurt)
)

// ===== mutation_intestine.js =====
RegistryOrgan('kubejs:mutation_intestine')
    .addScore('chestcavity:detoxification', 1)
    .addScore('chestcavity:nutrition', -1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OrganAddStatusEffectJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MutationIntestineOrganAddStatusEffect(customData, event, organItem, organIndex, slotType) {
    const effectInstance = event.effect
    if (effectInstance.getEffect().equals($MobEffects.HUNGER)) {
        event.setEffect(new $MobEffectInstance('minecraft:regeneration', effectInstance.getDuration(), effectInstance.getAmplifier()))
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:mutation_intestine')
        .addOnlyStrategy('organ_add_status_effect', MutationIntestineOrganAddStatusEffect)
)

// ===== nightmare_core.js =====
RegistryOrgan('kubejs:nightmare_core')
    .addScore('chestcavity:health', 1.5)
    .addScore('chestcavity:defense', -0.5)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.PlayerSpawnPhantomsEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function NightmareCorePlayerSpawnPhantoms(customData, event, organItem, organIndex, slotType) {
    event.cancel()
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:nightmare_core')
        .addOnlyStrategy('player_spawn_phantoms', NightmareCorePlayerSpawnPhantoms)
)

// ===== primal_bone_cage.js =====
RegistryOrgan('kubejs:primal_bone_cage')
    .addScore('chestcavity:defense', 3)
    .addScore('chestcavity:health', -1)

// ===== primal_heart.js =====
RegistryOrgan('kubejs:primal_heart')
    .addScore('chestcavity:health', 1)

// ===== soul_core.js =====
RegistryOrgan('kubejs:soul_core')
    .addScore('chestcavity:health', 1.5)
    .addScore('chestcavity:defense', -0.5)
    .setCanSpawn(true)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SoulCoreEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = entity.level
    if (entity.isFullyFrozen()) {
        entity.invulnerableTime = 0
        entity.attack(level.damageSources().freeze(), event.amount / 2)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:soul_core')
        .addOnlyStrategy('entity_do_damage', SoulCoreEntityDoDamage)
)

// ===== witch_fibroma.js =====
RegistryOrgan('kubejs:witch_fibroma')
    .addScore('chestcavity:digestion', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function WitchStomachKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    let potionsStack = GetBundleContents(organItem)
    if (potionsStack.length <= 0) return
    let potionItem = potionsStack[0]
    let potion = $PotionUtils.getPotion(potionItem)
    if (potion.effects.isEmpty()) return
    let effects = potion.getEffects()
    for (let effect of effects) {
        player.addEffect(effect)
    }
    player.addItemCooldown(organItem, 20 * 120)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:witch_fibroma')
        .addOnlyStrategy('key_active', WitchStomachKeyActive)
)

// ===== wither_bone_cage.js =====
RegistryOrgan('kubejs:wither_bone_cage')
    .addScore('chestcavity:defense', 2)
    .addScore('chestcavity:breath_capacity', -0.5)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function WitherBoneCageEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (RandomWithPlayerLuck(entity) <= 0.8) return
    let amplifier = 0
    let duration = 200
    if (entity.hasEffect('minecraft:wither')) {
        let witherEffect = entity.getEffect('minecraft:wither')
        amplifier = Math.min(witherEffect.getAmplifier() + 1, 4)
    }
    entity.potionEffects.add('minecraft:wither', duration, amplifier)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:wither_bone_cage')
        .addOnlyStrategy('entity_do_damage', WitherBoneCageEntityDoDamage)
)

// ===== worm_neuron.js =====
RegistryOrgan('kubejs:worm_neuron')
    .addScore('chestcavity:nerves', 1.5)
    .addScore('chestcavity:metabolism', 1)
    .addScore('chestcavity:endurance', -1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function WormNeuronEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity.age % 1200 != 0) return

    // 视作用默认培养液培养
    let tumorConfigModel = UnformedTumorFluidConfigMap.get('kubejs:nutrients_fluid')
    let organData = tumorConfigModel.genOrganData()
    let potentialOrganData = tumorConfigModel.genPotentialOrganData()

    let nbt = new $CompoundTag()
    // 故意预先存储，后续处理可以不依据该潜藏属性
    nbt.put('potentialOrganData', potentialOrganData)
    nbt.put('organData', organData)
    let canSetSlotList = []
    for (let i = 0; i < chestCavity.inventory.getContainerSize(); i++) {
        if (chestCavity.inventory.getItem(i).isEmpty()) {
            canSetSlotList.push(i)
        }
    }

    if (canSetSlotList.length == 0) {
        SetChestCavityOrgan(customData, chestCavity, Item.of('kubejs:malignant_neuron_tumor'), organIndex, slotType, true)
        return
    }
    let targetIndex = RandomGet(canSetSlotList)

    let targetSlotType = chestCavity.inventoryTypeData.getSlotType(targetIndex)
    SetChestCavityOrgan(customData, chestCavity, Item.of('kubejs:unformed_tumor', nbt), targetIndex, targetSlotType, true)
}




RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:worm_neuron')
        .addOnlyStrategy('entity_tick', WormNeuronEntityTick)
)

// ===== prismarine_crown.js =====
RegistryOrgan('kubejs:prismarine_crown')
    .addScore('chestcavity:water_breath', 1)
    .addScore('chestcavity:swim_speed', 1)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PrismarineCrownChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const { chestCavity } = event
    chestCavity.organScores.forEach((key, value) => {
        if (value < 0) {
            chestCavity.setOrganScore(key, 0)
        }
        if (value > 3) {
            chestCavity.setOrganScore(key, 3)
        }
    })
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:prismarine_crown')
        .addOnlyStrategy('chest_cavity_update', PrismarineCrownChestCavityUpdate, 1)
)

// ===== basic_organs.js =====
RegistryOrgan('kubejs:inner_furnace').addScore('chestcavity:health', 2).setCanSpawn(true)
RegistryOrgan('kubejs:golem_cable').addScore('chestcavity:strength', 0.5).addScore('chestcavity:nerves', 1).setCanSpawn(true)
RegistryOrgan('kubejs:golem_plating').addScore('chestcavity:defense', 1.5).addScore('chestcavity:speed', -0.5).setCanSpawn(true)
RegistryOrgan('kubejs:piston_muscle').addScore('chestcavity:strength', 2).addScore('chestcavity:nerves', -0.5).setCanSpawn(true)

// ===== blaze_pressurizer.js =====
RegistryOrgan('kubejs:blaze_pressurizer')
    .addScore('kubejs:extreme_strength', 1)
    .addScore('chestcavity:defense', -1)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BlazePressurizerChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'blazePressurizerCounter', 0)
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:blaze_pressurizer')
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BlazePressurizerKeyActiveOnly(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const chestCavity = player.chestCavityInstance
    let counter = BlazePressurizerActive(chestCavity, slotType)
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText(counter.toFixed(0))
    SetOrganEffect(chestCavity, organEffect)
    player.addItemCooldown(organItem, 20 * 30)
}

/**
 * 
 * @param {Internal.ChestCavityInstance} chestCavity 
 * @param {string} slotType 
 * @returns {number}
 */
function BlazePressurizerActive(chestCavity, slotType) {
    let counter = 3
    switch (slotType) {
        case MachinaryLubricant:
            counter = counter * 4
            break
        default:
            break
    }
    SetCustomDataMap(chestCavity, 'blazePressurizerCounter', counter)
    return counter
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:blaze_pressurizer')
        .addOnlyStrategy('organ_take_off', BlazePressurizerChestCavityTakeOffOnly)
        .addOnlyStrategy('key_active', BlazePressurizerKeyActiveOnly)
)

// ===== cyborgization_device.js =====
RegistryOrgan('kubejs:cyborgization_device')
    .addScore('chestcavity:health', 1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function CyborgizationDeviceEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    const level = event.level
    if (!entity.isPlayer()) return
    let fireTicks = entity.getRemainingFireTicks()
    if (fireTicks < 3580) return
    let damageValue = organItem.getDamageValue() + 1
    let maxDamageValue = organItem.getMaxDamage()
    if (damageValue >= maxDamageValue) {
        RemoveChestCavityOrgan(customData, chestCavity, organIndex, slotType, true)
        chestCavity.setInventoryType('kubejs:cc_inventory_types/revolution_machine')
        entity.setRemainingFireTicks(fireTicks - 3580)
        level.playSound(null, entity.getX(), entity.getY(), entity.getZ(), 'ui.toast.challenge_complete', entity.getSoundSource(), 0.25, 1)
    } else {
        organItem.setDamageValue(damageValue)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:cyborgization_device')
        .addOnlyStrategy('entity_tick', CyborgizationDeviceEntityTick)
)

// ===== energy_bottle_red.js =====
RegistryOrgan('kubejs:energy_bottle_red')
    .addScore('chestcavity:nerves', -0.5)
    .addScore('chestcavity:strength', 1.5)



/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function EnergyBottleRedEntityTick(customData, event, organItem, organIndex, slotType) {
    let repairValue = 1
    switch (slotType) {
        case MachinaryLubricant:
            repairValue = repairValue * 4
            break
        default:
            break
    }
    if (organItem.getDamageValue() > 0) {
        organItem.setDamageValue(Math.max(organItem.getDamageValue() - repairValue, 0))
    }
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function EnergyBottleRedDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance

    if (chestCavity.inventory.find('kubejs:burning_heart') > 0) {
        switch (slotType) {
            case RevolutionFlame:
                if (organItem.getDamageValue() + 10 <= organItem.getMaxDamage()) {
                    let damageRate = FloorFix(organItem.getMaxDamage() - organItem.getDamageValue() / organItem.getMaxDamage(), 2)
                    let value = GetCustomDataOrDefault(customData, 'burningItemMultiplierBoost', 0)
                    SetCustomData(customData, 'burningItemMultiplierBoost', value + damageRate * 2)
                    organItem.setDamageValue(organItem.getDamageValue() + 10)
                }
                break
            case MachinaryLubricant:
                if (organItem.getDamageValue() + 10 <= organItem.getMaxDamage()) {
                    let damageValue = organItem.getMaxDamage() - organItem.getDamageValue()
                    let value = GetCustomDataOrDefault(customData, 'burningItemDamageBoost', 0)
                    SetCustomData(customData, 'burningItemDamageBoost', value + damageValue * 4)
                    organItem.setDamageValue(organItem.getDamageValue() + 10)
                }
                break
            default:
                if (organItem.getDamageValue() + 10 <= organItem.getMaxDamage()) {
                    let damageValue = organItem.getMaxDamage() - organItem.getDamageValue()
                    let value = GetCustomDataOrDefault(customData, 'burningItemDamageBoost', 0)
                    SetCustomData(customData, 'burningItemDamageBoost', value + damageValue)
                    organItem.setDamageValue(organItem.getDamageValue() + 10)
                }
                break
        }

    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:energy_bottle_red')
        .addStrategy('entity_tick', EnergyBottleRedEntityTick)
        .addStrategy('entity_do_damage', EnergyBottleRedDoDamage)
)

// ===== fiery_core.js =====
RegistryOrgan('kubejs:fiery_core')
    .addScore('chestcavity:health', 0.5)
    .addScore('chestcavity:defense', -1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FieryCoreOrganTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    entity.setNoFireDamage(true)
    if (entity.isPlayer()) {
        let data = new $CompoundTag()
        data.putBoolean('noFireRender', true)
        EnqueueSendData(entity, 'fire_render', data)
    }
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FieryCoreOrganTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    entity.setNoFireDamage(false)
    if (entity.isPlayer()) {
        let data = new $CompoundTag()
        data.putBoolean('noFireRender', false)
        EnqueueSendData(entity, 'fire_render', data)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:fiery_core')
        .addOnlyStrategy('organ_take_on', FieryCoreOrganTakeOn)
        .addOnlyStrategy('organ_take_off', FieryCoreOrganTakeOff)
)

// ===== furnace_core.js =====
RegistryOrgan('kubejs:furnace_core')
    .addScore('chestcavity:knockback_resistant', 0.5)
    .addScore('chestcavity:strength', -3)
    .addScore('chestcavity:health', 1.5)

RegistryOrgan('kubejs:burning_heart')
    .addScore('chestcavity:knockback_resistant', 0.5)
    .addScore('chestcavity:strength', -3)
    .addScore('chestcavity:health', 1.5)

const FurnaceCoreTempAttackUpUUID = UUID.fromString('858D6175-7E75-41BC-9E70-A42AAFE7A337')
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function FurnaceCoreEntityTickDefer(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    let attributeInstance = event.entity.getAttribute('minecraft:generic.attack_damage')
    /**@type {number} */
    let value = 0
    if (!attributeInstance) return
    if (organItem.getDamageValue() + 1 <= organItem.getMaxDamage()) {
        organItem.setDamageValue(organItem.getDamageValue() + 1)
        value = 1
        let oldModifier = attributeInstance.getModifier(FurnaceCoreTempAttackUpUUID)
        if (oldModifier) value = oldModifier.amount + value
        if (slotType != RevolutionFlame) value = Math.min(value, 100)

        let attributeModifier = new $AttributeModifier(FurnaceCoreTempAttackUpUUID, 'FurnaceCoreTempAttackUp', value, $Operation.ADDITION)

        attributeInstance.removeModifier(FurnaceCoreTempAttackUpUUID)
        attributeInstance.addPermanentModifier(attributeModifier)
    } else {
        value = -3
        let oldModifier = attributeInstance.getModifier(FurnaceCoreTempAttackUpUUID)
        if (oldModifier) value = Math.max(oldModifier.amount + value, 0)

        attributeInstance.removeModifier(FurnaceCoreTempAttackUpUUID)
        if (value <= 0) return
        let attributeModifier = new $AttributeModifier(FurnaceCoreTempAttackUpUUID, 'FurnaceCoreTempAttackUp', value, $Operation.ADDITION)
        attributeInstance.addPermanentModifier(attributeModifier)
    }
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function FurnaceCoreDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance
    let damageValue = 2 + GetCustomDataMap(chestCavity, 'furnaceCoreRelay', 0)
    // 烈焰槽位解除耐久消耗提升限制
    if (slotType != RevolutionFlame) {
        damageValue = Math.min(damageValue, 5)
    }
    if (organItem.getDamageValue() > damageValue) {
        organItem.setDamageValue(organItem.getDamageValue() - damageValue)
        if (sourceEntity instanceof $ServerPlayer) {
            let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
            SetOrganEffect(chestCavity, organEffect)
        }
    } else {
        // 熔火 -> 心火
        let replaceItem = Item.of('kubejs:burning_heart')
        SetChestCavityOrgan(customData, chestCavity, replaceItem, organIndex, slotType, false)
        // 革命之钟触发烈焰加压器
        if (chestCavity.inventory.find('kubejs:revolution_bell') > 0) {
            if (sourceEntity instanceof $ServerPlayer) {
                if (!sourceEntity.getCooldowns().isOnCooldown('kubejs:blaze_pressurizer')) {
                    let counter = BlazePressurizerActive(chestCavity, slotType)
                    let organEffect = new OragnEffectModel(Item.of('kubejs:blaze_pressurizer')).setPriority(organIndex).setCustomText(counter.toFixed(0))
                    SetOrganEffect(chestCavity, organEffect)
                    sourceEntity.addItemCooldown('kubejs:blaze_pressurizer', 20 * 30)
                }
            } else {
                // 非玩家直接触发
                BlazePressurizerActive(chestCavity, slotType)
            }
        }

        if (sourceEntity instanceof $ServerPlayer) {
            let organEffect = new OragnEffectModel(replaceItem).setPriority(organIndex).setCustomText((replaceItem.getMaxDamage() - replaceItem.getDamageValue()).toFixed(0))
            SetOrganEffect(chestCavity, organEffect)
            RemoveOrganEffect(chestCavity, 'kubejs:furnace_core')
            CommonDingNotice(sourceEntity.level, sourceEntity)
        }
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function FurnaceCoreTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    let attributeInstance = entity.getAttribute('minecraft:generic.attack_damage')
    if (!attributeInstance) return
    attributeInstance.removeModifier(FurnaceCoreTempAttackUpUUID)
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:furnace_core')
    }
    OrganSkinRemove(entity, 'chest', 'secret/chestcavity/burning_heart_arms')
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function FurnaceCoreTakeOn(customData, event, organItem, organIndex, slotType) {
    const { chestCavity, entity } = event
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
    OrganSkinAdd(entity, 'chest', 'secret/chestcavity/burning_heart_arms')
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:furnace_core')
        .addOnlyStrategy('entity_tick', FurnaceCoreEntityTickDefer, -1)
        .addOnlyStrategy('organ_take_off', FurnaceCoreTakeOff)
        .addOnlyStrategy('entity_do_damage', FurnaceCoreDoDamage)
        .addOnlyStrategy('organ_take_on', FurnaceCoreTakeOn)
)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BurningHeartEntityTickDefer(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    let attributeInstance = event.entity.getAttribute('minecraft:generic.attack_damage')
    let damageValue = Math.max(10 - GetCustomDataMap(chestCavity, 'burningHeartDelay', 0), 0)
    // 烈焰槽位解除耐久消耗降低限制
    if (slotType != RevolutionFlame) {
        damageValue = Math.max(damageValue, 5)
    }

    if (!attributeInstance) return
    if (organItem.getDamageValue() + damageValue <= organItem.getMaxDamage()) {
        organItem.setDamageValue(organItem.getDamageValue() + damageValue)
        if (entity instanceof $ServerPlayer) {
            let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
            SetOrganEffect(chestCavity, organEffect)
        }
    } else {
        // 心火 -> 熔火
        let replaceItem = Item.of('kubejs:furnace_core')
        let damageValue = replaceItem.getMaxDamage()
        /**@type {number} */
        let blazerCount = GetCustomDataMap(chestCavity, 'blazePressurizerCounter', 0)
        if (blazerCount > 0) {
            damageValue = Math.max(damageValue - blazerCount * 10, 0)
            SetCustomDataMap(chestCavity, 'blazePressurizerCounter', 0)
            if (entity instanceof $ServerPlayer) {
                RemoveOrganEffect(chestCavity, 'kubejs:blaze_pressurizer')
            }
        }
        replaceItem.setDamageValue(damageValue)
        RemoveOrganEffect(chestCavity, 'kubejs:burning_heart')
        SetChestCavityOrgan(customData, event.chestCavity, replaceItem, organIndex, slotType, false)

        if (entity instanceof $ServerPlayer) {
            let organEffect = new OragnEffectModel(replaceItem).setPriority(organIndex).setCustomText((replaceItem.getMaxDamage() - replaceItem.getDamageValue()).toFixed(0))
            SetOrganEffect(chestCavity, organEffect)
            CommonDingNotice(entity.level, entity)
        }
    }
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BurningHeartDoDamageDefer(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance
    let blazerBoost = 1
    /**@type {number} */
    let blazerCount = GetCustomDataMap(chestCavity, 'blazePressurizerCounter', 0)
    let damageBoost = GetCustomDataOrDefault(customData, 'burningItemDamageBoost', 0)
    let multiplierBoost = GetCustomDataOrDefault(customData, 'burningItemMultiplierBoost', 0)
    if (blazerCount > 0 && (multiplierBoost > 0 || damageBoost > 0)) {
        blazerBoost = 2
        blazerCount = blazerCount - 1
        SetCustomDataMap(chestCavity, 'blazePressurizerCounter', blazerCount)
        if (sourceEntity instanceof $ServerPlayer) {
            if (blazerCount <= 0) {
                RemoveOrganEffect(chestCavity, 'kubejs:blaze_pressurizer')
            } else {
                let organEffect = new OragnEffectModel(Item.of('kubejs:blaze_pressurizer')).setPriority(organIndex).setCustomText(blazerCount.toFixed(0))
                SetOrganEffect(chestCavity, organEffect)
            }
        }
    }
    damageBoost = damageBoost * blazerBoost
    multiplierBoost = multiplierBoost * blazerBoost
    event.amount = (event.amount + damageBoost) * (2 + multiplierBoost)
}



/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BurningHeartTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    let attributeInstance = entity.getAttribute('minecraft:generic.attack_damage')
    if (!attributeInstance) return
    attributeInstance.removeModifier(FurnaceCoreTempAttackUpUUID)
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:burning_heart')
    }
    OrganSkinRemove(entity, 'chest', 'secret/chestcavity/burning_heart_arms')
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BurningHeartTakeOn(customData, event, organItem, organIndex, slotType) {
    const { chestCavity, entity } = event
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
    OrganSkinAdd(entity, 'chest', 'secret/chestcavity/burning_heart_arms')
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:burning_heart')
        .addOnlyStrategy('entity_tick', BurningHeartEntityTickDefer, -1)
        .addOnlyStrategy('organ_take_off', BurningHeartTakeOff)
        .addOnlyStrategy('entity_do_damage', BurningHeartDoDamageDefer, -1)
        .addOnlyStrategy('organ_take_on', BurningHeartTakeOn)
)

// ===== iron_repair_device.js =====
RegistryOrgan('kubejs:iron_repair_device')
    .addScore('chestcavity:fire_resistant', -2)
    .addScore('chestcavity:defense', -1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.ItemClickedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function IronRepairDeviceItemRightClicked(customData, event, organItem, organIndex, slotType) {
    const item = event.item
    const player = event.player
    if (OrganItemCoolDown(player, organItem)) return
    if (!item.is('minecraft:iron_ingot')) return
    player.heal(player.getMaxHealth() * 0.1)
    RecoverPlayerHungerAndSaturation(player, 4)
    item.shrink(1)
    player.addItemCooldown(organItem, 20 * 3)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:iron_repair_device')
        .addOnlyStrategy('item_right_clicked', IronRepairDeviceItemRightClicked)
)

// ===== lava_life_cycle_system.js =====
RegistryOrgan('kubejs:lava_life_cycle_system')
    .addScore('chestcavity:health', 1.5)
    .addScore('chestcavity:digestion', 0.5)

/**
 * 
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function LavaLifeCycleSystemEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    let fireTick = entity.getRemainingFireTicks()
    let mult = (slotType == MachinaryLubricant) ? 4 : 1
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    const invTypeData = chestCavity.getInventoryTypeData()
    let aroundRelativeSlots = GetDirectionRelativeSlotByParam(invTypeData, organIndex, FourDirectionOffset)
    for (let slotDefinition of aroundRelativeSlots) {
        let curItem = ccInv.getStackInSlot(slotDefinition.getId())
        if (!curItem || curItem.isEmpty()) continue
        let handler = GetItemFluidHandler(curItem)
        if (!handler) continue
        let fluid = handler.getFluid()
        if (fluid.getAmount() <= 0) continue
        if (fluid.containsFluid('minecraft:lava')) {
            if (fireTick + 40 * mult > 1200 * mult) continue
            handler.drain(Fluid.of('minecraft:lava', 1), 'execute')
            fireTick = fireTick + 40 * mult
            break
        }
        if (fluid.containsFluid('thermal:refined_fuel')) {
            if (fireTick + 20 * mult > 12000 * mult) continue
            handler.drain(Fluid.of('thermal:refined_fuel', 1), 'execute')
            fireTick = fireTick + 20 * mult
            break
        }
    }
    entity.setRemainingFireTicks(fireTick)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:lava_life_cycle_system')
        .addOnlyStrategy('entity_tick', LavaLifeCycleSystemEntityTick)
)

// ===== programmable_automatic_core.js =====
RegistryOrgan('kubejs:programmable_automatic_core')
    .addScore('chestcavity:filtration', 1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ProgrammableAutomaticCoreEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    entity.heal(1)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:programmable_automatic_core')
        .addOnlyStrategy('entity_tick', ProgrammableAutomaticCoreEntityTick)
)

// ===== prowler_rotating_shaft.js =====
RegistryOrgan('kubejs:prowler_rotating_shaft')
    .addScore('chestcavity:nerves', 0.5)
    .addScore('chestcavity:defense', -1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ProwlerRotatingShaftEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (!entity.isPlayer()) return
    if (event.amount <= 1) return
    const level = entity.level
    let tamedLivingList = GetTamedEntityWithinRadius(level, entity, 16)

    if (tamedLivingList.length <= 0) return
    let sharedAmount = event.amount / (tamedLivingList.length + 1)

    for (let target of tamedLivingList) {
        if (target.invulnerableTime > 0) continue
        target.attack(event.source, sharedAmount)
        event.amount = event.amount - sharedAmount
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:prowler_rotating_shaft')
        .addOnlyStrategy('entity_be_hurt', ProwlerRotatingShaftEntityBeHurt)
)

// ===== revolution_bell.js =====
RegistryOrgan('kubejs:revolution_bell')
    .addScore('chestcavity:endurance', 1.5)
    .addScore('chestcavity:metabolism', -0.5)

// ===== revolution_cable.js =====
RegistryOrgan('kubejs:revolution_cable')
    .addScore('chestcavity:endurance', 1.5)
    .addScore('chestcavity:nerves', 0.5)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionCableChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let machineCount = chestCavity.inventory.count('#kubejs:machine')
    let revolutionCount = chestCavity.inventory.count('#kubejs:revolution')
    let total = revolutionCount + machineCount
    switch (slotType) {
        case MachinaryLubricant:
            customData.maxHealth.addAttributeModifier(total * 4, 'addition', 'base')
            break
        default:
            customData.maxHealth.addAttributeModifier(total, 'addition', 'base')
            break
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:revolution_cable')
        .addOnlyStrategy('chest_cavity_update', RevolutionCableChestCavityUpdate)
)

// ===== revolution_delay.js =====
RegistryOrgan('kubejs:revolution_delay')
    .addScore('chestcavity:nerves', 0.5)
    
/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionDelayChestCavityUpdateOnly(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'burningHeartDelay', 0)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionDelayChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let num = 1
    switch (slotType) {
        case MachinaryLubricant:
            num = num * 4
            break
        default:
            break
    }
    let value = GetCustomDataMap(chestCavity, 'burningHeartDelay', 0)
    SetCustomDataMap(chestCavity, 'burningHeartDelay', value + num)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionDelayChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'burningHeartDelay', 0)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:revolution_delay')
        .addOnlyStrategy('chest_cavity_update', RevolutionDelayChestCavityUpdateOnly, 1)
        .addStrategy('chest_cavity_update', RevolutionDelayChestCavityUpdate)
        .addOnlyStrategy('organ_take_off', RevolutionDelayChestCavityTakeOffOnly)
)

// ===== revolution_relay.js =====
RegistryOrgan('kubejs:revolution_relay')
    .addScore('chestcavity:nerves', 0.5)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionRelayChestCavityUpdateOnly(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'furnaceCoreRelay', 0)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionRelayChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let num = 1
    switch (slotType) {
        case MachinaryLubricant:
            num = num * 4
            break
        default:
            break
    }
    let value = GetCustomDataMap(chestCavity, 'furnaceCoreRelay', 0)
    SetCustomDataMap(chestCavity, 'furnaceCoreRelay', value + num)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RevolutionRelayChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'furnaceCoreRelay', 0)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:revolution_relay')
        .addOnlyStrategy('chest_cavity_update', RevolutionRelayChestCavityUpdateOnly, 1)
        .addStrategy('chest_cavity_update', RevolutionRelayChestCavityUpdate)
        .addOnlyStrategy('organ_take_off', RevolutionRelayChestCavityTakeOffOnly)
)

// ===== small_acid_tank.js =====
RegistryOrgan('kubejs:small_acid_tank')
    .addScore('chestcavity:digestion', -3)
    .addScore('chestcavity:nutrition', -3)
    .addScore('chestcavity:health', -1)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SmallAcidTankEntityTick(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const entity = event.entity

    let damage = organItem.getDamageValue()
    let maxDamage = organItem.getMaxDamage()

    if (damage + 1 >= maxDamage) {
        RemoveChestCavityOrgan(customData, chestCavity, organIndex, slotType, true)
        if (chestCavity.getInventoryType() == 'kubejs:cc_inventory_types/player_17') {
            chestCavity.setInventoryType('kubejs:cc_inventory_types/player_21')
        }
        return
    }
    if (entity.isMoving()) {
        entity.attack(entity.damageSources().thorns(entity), 1)
    }
    organItem.setDamageValue(damage + 1)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:small_acid_tank')
        .addOnlyStrategy('entity_tick', SmallAcidTankEntityTick)
)

// ===== telescopic_arm.js =====
RegistryOrgan('kubejs:telescopic_arm')
    .addScore('chestcavity:nerves', 0.5)
    .addScore('chestcavity:knockback_resistant', 1)

RegistryOrgan('kubejs:telescopic_attack_arm')
    .addScore('chestcavity:strength', 1)
    .addScore('chestcavity:knockback_resistant', 1)


/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TelescopicArmChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    let blockReachUp = 1
    if (slotType == MachinaryLubricant) {
        blockReachUp = 4
    }
    customData.blockReach.addAttributeModifier(blockReachUp, 'addition', 'base')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:telescopic_arm')
        .addOnlyStrategy('chest_cavity_update', TelescopicArmChestCavityUpdate)
)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:telescopic_attack_arm')
        .addOnlyStrategy('chest_cavity_update', TelescopicArmChestCavityUpdate)
)

// ===== thermometer.js =====
RegistryOrgan('kubejs:thermometer')
    .addScore('chestcavity:fire_resistant', -2)
    .addScore('chestcavity:defense', -1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ThermometerEntityTickDefer(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity.isPlayer()) {
        let fireSeconds = Math.floor(entity.getRemainingFireTicks() / 20)
        let organEffect = new OragnEffectModel(organItem)
            .setPriority(organIndex)
            .setCustomText(Math.max(fireSeconds, 0).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function ThermometerOrganTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity.isPlayer()) {
        RemoveOrganEffect(chestCavity, 'kubejs:thermometer')
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:thermometer')
        .addOnlyStrategy('organ_take_off', ThermometerOrganTakeOff)
        .addOnlyStrategy('entity_tick', ThermometerEntityTickDefer, -1)
)

// ===== watcher_probe.js =====
RegistryOrgan('kubejs:watcher_probe')
    .addScore('kubejs:crit_chance', 1)
    .addScore('kubejs:crit_damage', 1)
    .addScore('chestcavity:fire_resistant', 1)
    .setCanSpawn(true)

// ===== amethyst_core.js =====
RegistryOrgan('kubejs:amethyst_core')
    .addScore('kubejs:magic_capacity', 1)
    .addScore('chestcavity:health', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function AmethystCoreEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const entity = event.source.actual
    
    if (!entity.isPlayer()) return
    
    let magicData = entity.getMagicData()
    let curMana = magicData.getMana()
    if (curMana <= 10) return
    let bonusDamage = curMana / 10
    magicData.setMana(curMana - bonusDamage)
    event.amount += bonusDamage
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:amethyst_core')
        .addOnlyStrategy('entity_do_damage', AmethystCoreEntityDoDamage)
)

// ===== animted_soul.js =====
// RegistryOrgan('kubejs:animted_soul')
//     .addScore('chestcavity:health', 1)
//     .addScore('chestcavity:nerves', 1)

// /**
// * @param {OrganChestCavityUpdateStrategyCustomData} customData
// * @param {Internal.NetworkEventJS} event
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function AnimtedSoulKeyActive(customData, event, organItem, organIndex, slotType) {
//     const player = event.player
//     const level = event.level
//     let playerPos = player.blockPosition()
//     /**@type {Internal.LivingEntity[]} */
//     let playerList = GetLivingWithinRadius(level, playerPos, 10, (level, entity) => {
//         return entity.isPlayer()
//     })
//     let playerUuid = player.getUuid()
//     let target = player.lastHurtMob
//     playerList.forEach(entity => {
//         let nbt = new $CompoundTag()
//         nbt.putString('SkullOwner', entity.getName().getString())
//         let headSummon = new $AnimHeadSummon(level, Blocks.PLAYER_HEAD.defaultBlockState(), nbt)
//         headSummon.setColor(entity.getTeamColor())
//         headSummon.setPos(player.blockPosition())
//         headSummon.setMaxHealth(Math.ceil(entity.getMaxHealth() / 2))
//         headSummon.heal(headSummon.maxHealth)
//         headSummon.setTicksLeft(20 * 60)
//         headSummon.setAggressive(true)
//         headSummon.setTarget(target)
//         headSummon.setOwnerID(playerUuid)
//         level.addFreshEntity(headSummon)
//     })
//     player.addItemCooldown(organItem, 20 * 30)
// }


// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:animted_soul')
//         .addOnlyStrategy('key_active', AnimtedSoulKeyActive)
// )

// ===== aptrgangr_soul.js =====
RegistryOrgan('kubejs:aptrgangr_soul')
    .addScore('chestcavity:health', 1)
    .addScore('chestcavity:defense', 1.5)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function AptrgangrSoulEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.hasEffect('minecraft:resistance')) return

    if (!entity.isPlayer()) {
        entity.potionEffects.add('minecraft:resistance', 3 * 20)
    } else {
        let cooldowns = entity.getCooldowns()
        if (cooldowns.isOnCooldown(organItem)) return
        entity.potionEffects.add('minecraft:resistance', 3 * 20)
        entity.addItemCooldown(organItem, 10 * 20)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:aptrgangr_soul')
        .addOnlyStrategy('entity_be_hurt', AptrgangrSoulEntityBeHurt)
)

// ===== bookwyrm_star_gem.js =====
// RegistryOrgan('kubejs:bookwyrm_star_gem')
//     .addScore('kubejs:magic_capacity', 1)
//     .addScore('chestcavity:nerves', 1)
//     .setCanSpawn(true)
// /**
//  * 
//  * @param {any} customData 
//  * @param {Internal.EffectResolveEvent} event 
//  * @param {Internal.ItemStack} organItem 
//  * @param {number} organIndex 
//  * @param {string} slotType 
//  */
// function BookwyrmStarGemEffectResolvePost(customData, event, organItem, organIndex, slotType) {
//     if (!(event.resolveEffect instanceof $EffectSmelt)) return
//     const level = event.world
//     const rayTraceResult = event.rayTraceResult
//     if (!(rayTraceResult instanceof $BlockHitResult)) return
//     const spellStats = event.spellStats
//     let itemEntities = level.getEntitiesOfClass($ItemEntity, AABB.ofBlock(rayTraceResult.getBlockPos()).inflate(spellStats.getAoeMultiplier() + 1.0))
//     /**@type {Internal.ItemEntity[]} */
//     let enchantItemEntityList = []
//     /**@type {Internal.Map<string, number>[]} */
//     let enchantList = []
//     itemEntities.forEach(pItemEntity => {
//         let pItem = pItemEntity.getItem()
//         let enchants = EnchantmentHelper.getEnchantments(pItem)
//         if (!enchants || enchants.size() <= 0) return
//         enchantItemEntityList.push(pItemEntity)
//         enchantList.push(enchants)
//     })
//     if (enchantItemEntityList.length == 0) return
//     /**@type {Internal.Map<string, number>} */
//     let targetEnchant = RandomGet(enchantList)
//     enchantItemEntityList.forEach(pItemEntity => {
//         let pItem = pItemEntity.getItem()
//         if (!pItem.hasNBT()) return
//         let nbt = pItem.getNbt()
//         pItem.is('enchanted_book') ? nbt.remove('StoredEnchantments') : nbt.remove('Enchantments')
//         EnchantmentHelper.setEnchantments(targetEnchant, pItem)
//     })
// }

// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:bookwyrm_star_gem')
//         .addOnlyStrategy('ars_effect_resolve_post', BookwyrmStarGemEffectResolvePost)
// )

// ===== calamity_bone_spur.js =====
// RegistryOrgan('kubejs:calamity_bone_spur')
    // .addScore('chestcavity:defense', 2)
    // .addScore('kubejs:magic_capacity', 1)
    // .setCanSpawn(true)
// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function CalamityBoneSpurChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:advance_fang_strike', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function CalamityBoneSpurTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:advance_fang_strike')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:calamity_bone_spur')
        // .addOnlyStrategy('chest_cavity_update', CalamityBoneSpurChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', CalamityBoneSpurTakeOff)
// )

// ===== coral_armor.js =====
RegistryOrgan('kubejs:coral_armor')
    .addScore('chestcavity:defense', 1.5)
    .addScore('kubejs:extreme_fitness', 0.5)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function CoralArmorEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const entity = event.entity
    if (!entity.isPlayer()) return
    entity.getMagicData().addMana(event.amount)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:coral_armor')
        .addOnlyStrategy('entity_be_hurt', CoralArmorEntityBeHurt)
)

// ===== deepling_ectoplasm.js =====
RegistryOrgan('kubejs:deepling_ectoplasm')
    .addScore('kubejs:knockback', 1)
    .addScore('kubejs:extreme_strength', 1)
    .addScore('chestcavity:nerves', 1.5)
    .setCanSpawn(true)

// ===== deepling_star_gem.js =====
RegistryOrgan('kubejs:deepling_star_gem')
    .addScore('chestcavity:health', 1)
    .addScore('kubejs:magic_capacity', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.EffectResolveEvent$Post} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function DeeplingStarGemEffectResolvePost(customData, event, organItem, organIndex, slotType) {
    const hitResult = event.rayTraceResult
    if (!(hitResult instanceof $EntityHitResult)) return
    /**@type {Internal.LivingEntity } */
    let entity = hitResult.getEntity()
    entity.setTicksFrozen(400)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:deepling_star_gem')
        .addOnlyStrategy('ars_effect_resolve_post', DeeplingStarGemEffectResolvePost)
)

// ===== draugr_skull.js =====
RegistryOrgan('kubejs:draugr_skull')
    .addScore('chestcavity:defense', 1)
    .addScore('kubejs:magic_capacity', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function DraugrSkullEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const source = event.source.actual
    if (!source) return
    let sourceFrozenTicks = source.getTicksFrozen()
    source.setTicksFrozen(sourceFrozenTicks + 100)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:draugr_skull')
        .addOnlyStrategy('entity_be_hurt', DraugrSkullEntityBeHurt)
)

// ===== drygmy_star_gem.js =====
// RegistryOrgan('kubejs:drygmy_star_gem')
//     .addScore('chestcavity:nerves', 1)
//     .addScore('kubejs:magic_capacity', 1)
//     .setCanSpawn(true)
// /**
//  * 
//  * @param {any} customData 
//  * @param {Internal.EffectResolveEvent} event 
//  * @param {Internal.ItemStack} organItem 
//  * @param {number} organIndex 
//  * @param {string} slotType 
//  */
// function DrygmyStarGemEffectResolvePost(customData, event, organItem, organIndex, slotType) {
//     const resolveEffect = event.resolveEffect
//     if (resolveEffect.getRegistryName() != 'ars_nouveau:glyph_harvest') return
//     /**@type {Internal.EntityHitResult} */
//     const hitResult = event.rayTraceResult
//     if (!(hitResult instanceof $EntityHitResult)) return
//     let hitEntity = hitResult.getEntity()
//     if (!(hitEntity instanceof $LivingEntity)) return
//     if (hitEntity.entityType.is(DrygmyBlacklistTag)) return
//     let lootTable = hitEntity.getLootTable()
//     let itemList = Utils.rollChestLoot(lootTable).toArray()
//     SpawnLootAtLocation(hitEntity.level, hitEntity.blockPosition(), itemList)
// }

// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:drygmy_star_gem')
//         .addOnlyStrategy('ars_effect_resolve_post', DrygmyStarGemEffectResolvePost)
// )

// ===== exhausted_source_focus_crystal.js =====
RegistryOrgan('kubejs:exhausted_source_focus_crystal')
    .addScore('kubejs:magic_capacity', 1)
    .addScore('chestcavity:luck', 1)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.OpenedEntityTickJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SourceResonatorEntityTick(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const entity = event.entity
    if (!entity.isPlayer()) return
    const chestCavity = event.chestCavity
    let magicData = entity.getMagicData()
    let curMana = magicData.getMana()
    if (curMana <= 50) return
    let damageValue = curMana / 50 + organItem.getDamageValue()
    let maxDamageValue = organItem.getMaxDamage()
    if (damageValue >= maxDamageValue) {
        SetChestCavityOrgan(customData, chestCavity, Item.of('kubejs:source_focus_crystal'), organIndex, slotType, true)
    } else {
        organItem.setDamageValue(damageValue)
    }
    magicData.setMana(0)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:exhausted_source_focus_crystal')
        .addOnlyStrategy('entity_tick', SourceResonatorEntityTick)
)

// ===== heal_star_gem.js =====
// RegistryOrgan('kubejs:heal_star_gem')
//     .addScore('chestcavity:luck', 1)
//     .addScore('kubejs:magic_capacity', 1)


// /**
// * @param {OrganEventCustomData} customData
// * @param {Internal.EffectResolveEvent$Post} event
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function HealStarGemArsEffectResolvePost(customData, event, organItem, organIndex, slotType) {
//     const resolveEffect = event.resolveEffect
//     if (resolveEffect.getRegistryName() != 'ars_nouveau:glyph_heal') return
//     /**@type {Internal.EntityHitResult} */
//     const hitResult = event.rayTraceResult
//     const spellStats = event.spellStats
//     if (!(hitResult instanceof $EntityHitResult)) return
//     /**@type {Internal.LivingEntity } */
//     let entity = hitResult.getEntity()
//     let duration = Math.max(60, 20 * (spellStats.getAmpMultiplier() + 1) * 3)
//     if (entity.hasEffect('minecraft:regeneration')) {
//         duration = duration + entity.getEffect('minecraft:regeneration').getDuration()
//     }
//     entity.potionEffects.add('minecraft:regeneration', duration, 0)
// }

// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:heal_star_gem')
//         .addOnlyStrategy('ars_effect_resolve_post', HealStarGemArsEffectResolvePost)
// )

// ===== hippocamtus_scale.js =====
RegistryOrgan('kubejs:hippocamtus_scale')
    .addScore('kubejs:magic_capacity', 1)
    .addScore('chestcavity:defense', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.ShieldBlockEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HippocamtusScaleShieldBlock(customData, event, organItem, organIndex, slotType) {
    const source = event.damageSource.actual
    if (!source) return
    let forzenTicks = source.getTicksFrozen()
    source.setTicksFrozen(forzenTicks + 60)
    if (forzenTicks <= 0) return
    customData.thornsDamage = customData.thornsDamage + forzenTicks / 10
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:hippocamtus_scale')
        .addOnlyStrategy('shield_block', HippocamtusScaleShieldBlock)
)

// ===== koboleton.js =====
RegistryOrgan('kubejs:koboleton_spine')
    .addScore('chestcavity:nerves', 1.5)
    .addScore('kubejs:magic_capacity', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:koboleton_rib')
    .addScore('chestcavity:defense', 1.5)
    .addScore('kubejs:magic_capacity', 1)
    .setCanSpawn(true)

RegistryOrgan('kubejs:koboleton_coccyx')
    .addScore('chestcavity:knockback_resistant', 1)
    .addScore('kubejs:magic_capacity', 2.5)
    .setCanSpawn(true)

// ===== potion_skin.js =====
// RegistryOrgan('kubejs:potion_skin')
    // .addScore('chestcavity:defense', 3)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function PotionSkinChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'irons_spellbooks:oakskin', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function PotionSkinTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'irons_spellbooks:oakskin')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:potion_skin')
        // .addOnlyStrategy('chest_cavity_update', PotionSkinChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', PotionSkinTakeOff)
// )

// ===== shulker_eye.js =====
// RegistryOrgan('kubejs:shulker_eye')
    // .addScore('chestcavity:nerves', 1)
    // .setCanSpawn(true)
// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function ShulkerEyeChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'irons_spellbooks:pocket_dimension', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function ShulkerEyeTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'irons_spellbooks:pocket_dimension')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:shulker_eye')
        // .addOnlyStrategy('chest_cavity_update', ShulkerEyeChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', ShulkerEyeTakeOff)
// )

// ===== soul_cage.js =====
RegistryOrgan('kubejs:soul_cage')
    .addScore('chestcavity:endurance', 1)
    .addScore('chestcavity:nerves', 1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDeathEvent} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SoulCageEntityKill(customData, event, organItem, organIndex, slotType) {
    let damageValue = organItem.getDamageValue()
    if (damageValue == 0) return
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance
    const entity = event.entity
    const level = entity.level
    const difficulty = level.getDifficulty().getId()
    let recoverValue = difficulty
    if (damageValue > recoverValue) {
        organItem.setDamageValue(damageValue - recoverValue)
    } else {
        organItem.setDamageValue(0) 
    }
    if (sourceEntity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SoulCageChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:soul_cage')
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SoulCageEntityTick(customData, event, organItem, organIndex, slotType) {
    let damageValue = organItem.getDamageValue()
    if (damageValue == organItem.getMaxDamage()) return
    const entity = event.entity
    if (entity.getMaxHealth() - entity.getHealth() == 0) return
    entity.heal(2)
    organItem.setDamageValue(damageValue + 1)
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(entity.chestCavityInstance, organEffect)
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:soul_cage')
        .addOnlyStrategy('entity_kill', SoulCageEntityKill)
        .addOnlyStrategy('organ_take_off', SoulCageChestCavityTakeOffOnly)
       .addOnlyStrategy('entity_tick', SoulCageEntityTick)
)

// ===== soul_wing.js =====
RegistryOrgan('kubejs:soul_wing')
    .addScore('kubejs:creative_flight', 1)

// ===== starbuncle_star_gem.js =====
RegistryOrgan('kubejs:starbuncle_star_gem')
    .addScore('kubejs:magic_capacity', 2)
    .setCanSpawn(true)
/**
 * 
 * @param {any} customData 
 * @param {Internal.EffectResolveEvent$Pre} event 
 * @param {Internal.ItemStack} organItem 
 * @param {number} organIndex 
 * @param {string} slotType 
 */
function StarbuncleStarGemEffectResolvePre(customData, event, organItem, organIndex, slotType) {
    const spellStats = event.spellStats
    const shooter = event.shooter
    const chestCavity = shooter.chestCavityInstance
    let magicCapacity = chestCavity.getOrganScore('kubejs:magic_capacity')
    spellStats.setAmpMultiplier(spellStats.getAmpMultiplier() + Math.max(magicCapacity, 0))
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:starbuncle_star_gem')
        .addOnlyStrategy('ars_effect_resolve_pre', StarbuncleStarGemEffectResolvePre)
)

// ===== whirlisprig_star_gem.js =====
RegistryOrgan('kubejs:whirlisprig_star_gem')
    .addScore('chestcavity:defense', 1)
    .addScore('kubejs:magic_capacity', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function WhirlisprigStarGemChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    SetCustomDataMap(chestCavity, 'hasWhirlisprigStarGem', 1)
}

// /**
// * @param {OrganEventCustomData} customData
// * @param {Internal.EvaluateChestCavityJS} event
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function WhirlisprigStarGemChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
//     const chestCavity = event.chestCavity
//     RemoveCustomDataMap(chestCavity, 'hasWhirlisprigStarGem')
// }


// /**
// * @param {OrganEventCustomData} customData
// * @param {Internal.EffectResolveEvent$Post} event
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function WhirlisprigStarGemArsEffectResolvePost(customData, event, organItem, organIndex, slotType) {
//     const resolveEffect = event.resolveEffect
//     if (resolveEffect.getRegistryName() != 'ars_nouveau:glyph_harvest') return
//     /**@type {Internal.BlockHitResult} */
//     const hitResult = event.rayTraceResult
//     const spellStats = event.spellStats
//     if (!(hitResult instanceof $BlockHitResult)) return
//     const shooter = event.shooter
//     const level = event.world
//     let aoeBlockPosList = $SpellUtil.calcAOEBlocks(shooter, hitResult.blockPos, hitResult, spellStats)
//     aoeBlockPosList.forEach(blockPos => {
//         if ($BoneMealItem.applyBonemeal(Item.of('minecraft:bone_meal'), level, blockPos, shooter)) {
//             level.spawnParticles('minecraft:glow', true, blockPos.x + 0.2, blockPos.y + 0.3, blockPos.z - 0.4, 0, 0.1, 0, 2, 0)
//             level.spawnParticles('minecraft:glow', true, blockPos.x + 0.1, blockPos.y + 0.2, blockPos.z + 0.3, 0, 0.1, 0, 2, 0)
//         }
//     })
// }


// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:whirlisprig_star_gem')
//         .addOnlyStrategy('organ_take_on', WhirlisprigStarGemChestCavityTakeOn)
//         .addOnlyStrategy('organ_take_off', WhirlisprigStarGemChestCavityTakeOff)
//         .addOnlyStrategy('ars_effect_resolve_post', WhirlisprigStarGemArsEffectResolvePost)
// )

// ===== wither_skull.js =====
// RegistryOrgan('kubejs:wither_skull')
    // .addScore('chestcavity:health', -0.5)
    // .addScore('kubejs:magic_capacity', 2)
    // .setCanSpawn(true)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function WithitherSkullChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:advance_wither_skull', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function WithitherSkullTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:advance_wither_skull')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:wither_skull')
        // .addOnlyStrategy('chest_cavity_update', WithitherSkullChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', WithitherSkullTakeOff)
// )

// ===== allay_wing.js =====
RegistryOrgan('kubejs:allay_wing')
    .addScore('chestcavity:speed', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function AllayWingChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (!(entity instanceof $OwnableEntity)) return
    if (entity.type == 'minecraft:allay') return
    RemoveCustomGoalByName(entity.goalSelector, 'pick_item_bdnet')
    entity.goalSelector.addGoal(0, NewPickItemBDNetGoal(entity))
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function AllayWingChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (!(entity instanceof $OwnableEntity)) return
    if (entity.type == 'minecraft:allay') return
    RemoveCustomGoalByName(entity.goalSelector, 'pick_item_bdnet')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:allay_wing')
        .addOnlyStrategy('organ_take_on', AllayWingChestCavityTakeOn)
        .addOnlyStrategy('organ_take_off', AllayWingChestCavityTakeOff)
)

// ===== armadillo_shell.js =====
RegistryOrgan('kubejs:armadillo_shell')
    .addScore('chestcavity:defense', 2)
    .setCanSpawn(true)

const ArmadilloShellDefenseUpUUID = UUID.fromString('683322F6-174C-40F2-A282-818A148F2A8B')
const ArmadilloShellDefenseUpIdentifier = 'ArmadilloShellDefenseUp'

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ArmadilloShellEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    let attr = entity.getAttribute('minecraft:generic.armor')
    if (entity.isMoving()) {
        attr.removePermanentModifier(ArmadilloShellDefenseUpUUID)
        return
    }
    let modifier = new $AttributeModifier(ArmadilloShellDefenseUpUUID, ArmadilloShellDefenseUpIdentifier, 1, $Operation.MULTIPLY_BASE)
    if (attr.hasModifier(modifier)) return
    attr.addPermanentModifier(modifier)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:armadillo_shell')
        .addOnlyStrategy('entity_tick', ArmadilloShellEntityTick)
)

// ===== axolotl_gill.js =====
RegistryOrgan('kubejs:axolotl_gill')
    .addScore('chestcavity:breath_capacity', 1)
    .addScore('chestcavity:water_breath', 2)
    .addScore('chestcavity:breath_recovery', 1)
    .setCanSpawn(true)

// ===== basic_organs.js =====
RegistryOrgan('kubejs:appendix').addScore('chestcavity:luck', 1).setCanSpawn(true)
RegistryOrgan('kubejs:intestine').addScore('chestcavity:nutrition', 1).setCanSpawn(true)
RegistryOrgan('kubejs:heart').addScore('chestcavity:health', 1).setCanSpawn(true)
RegistryOrgan('kubejs:kidney').addScore('chestcavity:filtration', 1).setCanSpawn(true)
RegistryOrgan('kubejs:liver').addScore('chestcavity:detoxification', 1).setCanSpawn(true)
RegistryOrgan('kubejs:lung').addScore('chestcavity:breath_recovery', 1).addScore('chestcavity:breath_capacity', 1).addScore('chestcavity:endurance', 1).setCanSpawn(true)
RegistryOrgan('kubejs:muscle').addScore('chestcavity:strength', 1).setCanSpawn(true)
RegistryOrgan('kubejs:rib').addScore('chestcavity:defense', 1).setCanSpawn(true)
RegistryOrgan('kubejs:spine').addScore('chestcavity:nerves', 1).setCanSpawn(true)
RegistryOrgan('kubejs:spleen').addScore('chestcavity:metabolism', 1).setCanSpawn(true)
RegistryOrgan('kubejs:stomach').addScore('chestcavity:digestion', 1).setCanSpawn(true)

RegistryOrgan('kubejs:animal_appendix').addScore('chestcavity:luck', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:animal_intestine').addScore('chestcavity:nutrition', 1).addScore('chestcavity:digestion', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:animal_heart').addScore('chestcavity:health', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_kidney').addScore('chestcavity:filtration', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_liver').addScore('chestcavity:detoxification', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_lung').addScore('chestcavity:breath_recovery', 1).addScore('chestcavity:breath_capacity', 1).addScore('chestcavity:endurance', 1.5).setCanSpawn(true)
RegistryOrgan('kubejs:animal_muscle').addScore('chestcavity:strength', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_rib').addScore('chestcavity:defense', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_spine').addScore('chestcavity:nerves', 1).addScore('chestcavity:defense', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:animal_spleen').addScore('chestcavity:metabolism', 1).setCanSpawn(true)
RegistryOrgan('kubejs:animal_stomach').addScore('chestcavity:digestion', 1.5).setCanSpawn(true)
RegistryOrgan('kubejs:rumen').addScore('chestcavity:digestion', 1).addScore('chestcavity:nutrition', 1).setCanSpawn(true)

RegistryOrgan('kubejs:insect_lung').addScore('chestcavity:breath_recovery', 1).addScore('chestcavity:breath_capacity', 1).addScore('chestcavity:endurance', 1).setCanSpawn(true)
RegistryOrgan('kubejs:insect_muscle').addScore('chestcavity:strength', 1.5).setCanSpawn(true)
RegistryOrgan('kubejs:insect_intestine').addScore('chestcavity:nutrition', 1.5).setCanSpawn(true)
RegistryOrgan('kubejs:insect_heart').addScore('chestcavity:health', 1).setCanSpawn(true)
RegistryOrgan('kubejs:insect_caeca').addScore('chestcavity:metabolism', 1).setCanSpawn(true)
RegistryOrgan('kubejs:insect_stomach').addScore('chestcavity:digestion', 1).setCanSpawn(true)

RegistryOrgan('kubejs:gills').addScore('chestcavity:breath_recovery', -1).addScore('chestcavity:breath_capacity', 1).addScore('chestcavity:water_breath', 1).setCanSpawn(true)

// ===== bat_throat.js =====
RegistryOrgan('kubejs:bat_throat')
    .addScore('chestcavity:endurance', 0.5)
    .setCanSpawn(true)


/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BatThroatKeyActive(customData, event, organItem, organIndex, slotType) {
    const level = event.level
    const player = event.player
    const entities = GetEntityWithinRadiusVec3d(level, player.position(), 16, (pLevel, pEntity) => pEntity instanceof $LivingEntity)
    entities.forEach(entity => {
        entity.potionEffects.add('minecraft:glowing', 20 * 30)
    })
    player.addItemCooldown('kubejs:bat_throat', 20 * 30)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:bat_throat')
        .addOnlyStrategy('key_active', BatThroatKeyActive)
)

// ===== bear_paw.js =====
RegistryOrgan('kubejs:bear_paw')
    .addScore('chestcavity:endurance', -1)
    .addScore('kubejs:extreme_strength', 2)
    .setCanSpawn(true)

// ===== bee_honey_stomach.js =====
RegistryOrgan('kubejs:bee_honey_stomach')
    .addScore('chestcavity:digestion', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.BlockRightClickedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BeeHoneyStomachBlockRightClicked(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const block = event.block
    if (event.hand != 'main_hand') return
    if (!block.hasTag('minecraft:flowers')) return
    if (OrganItemCoolDown(player, organItem)) return
    let damage = organItem.getDamageValue()

    if (damage <= 0) {
        SpawnItemEntityWithMovement(event.level, player.blockPosition(), Item.of('minecraft:honeycomb', Math.floor(Math.random() * 3 + 1)), new Vec3d(0, 0, 0))
        organItem.setDamageValue(organItem.getMaxDamage())
        damage = organItem.getMaxDamage()
    } else {
        damage = damage - 1
        organItem.setDamageValue(damage)
    }
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - damage).toFixed(0))
    SetOrganEffect(player.chestCavityInstance, organEffect)
    player.addItemCooldown(organItem, 20 * 3)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BeeHoneyStomachOrganTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:bee_honey_stomach')
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BeeHoneyStomachOrganTakeOn(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:bee_honey_stomach')
        .addOnlyStrategy('organ_take_on', BeeHoneyStomachOrganTakeOn)
        .addOnlyStrategy('organ_take_off', BeeHoneyStomachOrganTakeOff)
        .addOnlyStrategy('block_right_clicked', BeeHoneyStomachBlockRightClicked)
)

// ===== bison_horn.js =====
RegistryOrgan('kubejs:bison_horn')
    .addScore('kubejs:extreme_strength', 2)
    .addScore('chestcavity:defense', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BisonHornEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    event.amount = event.amount + (entity.maxHealth - entity.health) * 2
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:bison_horn')
        .addOnlyStrategy('entity_do_damage', BisonHornEntityDoDamage)
)

// ===== boar_nose.js =====
RegistryOrgan('kubejs:boar_nose')
    .addScore('chestcavity:endurance', 2)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.BlockRightClickedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BoarNoseBlockRightClicked(customData, event, organItem, organIndex, slotType) {
    const block = event.block
    const player = event.player
    const level = event.level
    if (event.hand != 'main_hand' || !player.getMainHandItem().isEmpty()) return
    if (block.id == 'minecraft:grass_block') {
        level.setBlockAndUpdate(block.getPos(), Blocks.DIRT.defaultBlockState())
        RecoverPlayerHungerAndSaturation(player, 2)
        player.swing()
        level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 1, 1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:boar_nose')
        .addOnlyStrategy('block_right_clicked', BoarNoseBlockRightClicked)
)

// ===== camel_hump.js =====
RegistryOrgan('kubejs:camel_hump')
    .addScore('chestcavity:endurance', 2)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingFallEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function CamelHumpEntityFall(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (event.distance < 3) return
    let curBlockPos = entity.blockPosition()
    let belowBlockPos = curBlockPos.below()
    const level = entity.level
    let belowBlockState = level.getBlockState(belowBlockPos)
    if (belowBlockState.isAir()) return
    level.setBlockAndUpdate(curBlockPos, Blocks.WATER.defaultBlockState())
    event.cancel()
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function CamelHumpKeyActive(customData, event, organItem, organIndex, slotType) {
    const level = event.level
    const player = event.player
    if (OrganItemCoolDown(player, organItem)) return
    let curBlockPos = player.blockPosition()
    level.setBlockAndUpdate(curBlockPos, Blocks.WATER.defaultBlockState())
    player.addItemCooldown('kubejs:camel_hump', 20 * 3)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:camel_hump')
        .addOnlyStrategy('entity_fall', CamelHumpEntityFall)
        .addOnlyStrategy('key_active', CamelHumpKeyActive)
)

// ===== cassowary_muscle.js =====
RegistryOrgan('kubejs:cassowary_muscle')
    .addScore('kubejs:extreme_strength', 1)
    .addScore('kubejs:crit_chance', 1)
    .setCanSpawn(true)

// ===== chimera_heart.js =====
RegistryOrgan('kubejs:chimera_heart')
    .addScore('chestcavity:health', 1)
    .addScore('kubejs:extreme_fitness', 2)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ChimeraHeartEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const entity = event.entity
    if (!entity.isPlayer()) return
    if (OrganItemCoolDown(entity, organItem)) return
    if (event.amount <= entity.getHealth() + entity.getAbsorptionAmount()) return
    entity.setAbsorptionAmount(entity.getAbsorptionAmount() + event.amount)
    event.amount = 0
    entity.addItemCooldown(organItem, 20 * 180)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:chimera_heart')
        .addOnlyStrategy('entity_be_hurt', ChimeraHeartEntityBeHurt)
)

// ===== cindaria_umbrella.js =====
RegistryOrgan('kubejs:cindaria_umbrella')
    .addScore('chestcavity:detoxification', 1)
    .addScore('chestcavity:defense', -1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.OrganAddStatusEffectJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function CindariaUmbrellaOrganAddStatusEffect(customData, event, organItem, organIndex, slotType) {
    const effectInstance = event.effect
    if (effectInstance.getEffect().equals($MobEffects.POISON)) {
        event.setEffect(new $MobEffectInstance('minecraft:regeneration', 20 * 10))
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:cindaria_umbrella')
        .addOnlyStrategy('organ_add_status_effect', CindariaUmbrellaOrganAddStatusEffect)
)

// ===== cornucopia.js =====
RegistryOrgan('kubejs:multifaceted_ambrosia')
    .addScore('chestcavity:digestion', 1)
    .addScore('chestcavity:nutrition', 2)
    .setCanSpawn(true)

// ===== deer_horn.js =====
RegistryOrgan('kubejs:deer_horn')
    .addScore('chestcavity:speed', -0.5)
    .addScore('chestcavity:defense', 3)
    .setCanSpawn(true)

// ===== dog_tail.js =====
RegistryOrgan('kubejs:dog_tail')
    .addScore('kubejs:extreme_fitness', 1)
    .addScore('chestcavity:strength', -1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function DogTailEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    if (entity.age % 200 != 0) return
    let tamedLivingList = GetTamedEntityWithinRadius(level, entity, 16)

    if (tamedLivingList.length <= 0) return
    tamedLivingList.forEach(pTarget => {
        pTarget.potionEffects.add('minecraft:regeneration', 20 * 30, 0, false, false)
    })
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:dog_tail')
        .addOnlyStrategy('entity_tick', DogTailEntityTick)
)

// ===== dolphin_fin.js =====
RegistryOrgan('kubejs:dolphin_fin')
    .addScore('chestcavity:swim_speed', 1)
    .addScore('chestcavity:defense', 0.5)
    .setCanSpawn(true)
    
/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.OpenedEntityTickJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function DolphinFinEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.hasEffect('minecraft:dolphins_grace') && entity.getEffect('minecraft:dolphins_grace').getDuration() > 20 * 3) return
    if (!entity.isInFluidType()) {
        entity.potionEffects.add('minecraft:dolphins_grace', 20 * 30, 0, false, false)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:dolphin_fin')
        .addOnlyStrategy('entity_tick', DolphinFinEntityTick)
)

// ===== dragonfly_wing.js =====
RegistryOrgan('kubejs:dragonfly_wing')
    .addScore('chestcavity:speed', 1)
    .addScore('kubejs:attack_dodge', 2)
    .setCanSpawn(true)

// ===== dumbo_octopus_hat.js =====
RegistryOrgan('kubejs:dumbo_octopus_hat')
    .addScore('chestcavity:defense', -0.5)
    .addScore('chestcavity:swim_speed', 1)
    .setCanSpawn(true)
/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function DumboOctopusHatKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    let nervesScore = player.chestCavityInstance.getOrganScore('chestcavity:nerves')
    let duration = 20 * (10 + nervesScore * 5)
    player.potionEffects.add('minecraft:water_breathing', duration, 0, false, false)
    player.addItemCooldown(organItem, 20 * 60)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:dumbo_octopus_hat')
        .addOnlyStrategy('key_active', DumboOctopusHatKeyActive)
)

// ===== elder_guardian_core.js =====
// RegistryOrgan('kubejs:elder_guardian_core')
    // .addScore('chestcavity:nerves', 1)
    // .addScore('chestcavity:luck', 1)

// /**
// * @param {OrganChestCavityUpdateStrategyCustomData} customData
// * @param {Internal.NetworkEventJS} event
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function ElderGuardianCoreKeyActive(customData, event, organItem, organIndex, slotType) {
    // const player = event.player
    // const level = event.level
    // const server = event.server
    // if (level.getDimension() == 'infinity:missingno') {
        // player.teleportTo(new ResourceLocation('minecraft:overworld'), player.x, player.y, player.z, player.yaw, player.pitch)
    // } else {
        // InfinityPortalCreator.addInfinityDimensionIfNotExists(server, new ResourceLocation('infinity:missingno'))
        // player.teleportTo(new ResourceLocation('infinity:missingno'), player.x, player.y, player.z, player.yaw, player.pitch)
    // }

    // player.addItemCooldown(organItem, 20 * 60)
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:elder_guardian_core')
        // .addOnlyStrategy('key_active', ElderGuardianCoreKeyActive)
// )

// ===== explosion_bag.js =====
RegistryOrgan('kubejs:explosion_bag')
    .addScore('chestcavity:digestion', 1)
    .addScore('chestcavity:detoxification', 0.5)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ExplosionBagEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.age % 200 != 0) return
    AddItemIntoBundle(organItem, Item.of('minecraft:gunpowder'), 64, (pStack) => pStack.getMaxStackSize() / 64)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.NetworkEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ExplosionBagKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    let playerPos = player.position()
    let itemList = GetBundleContents(organItem)
    if (itemList.length <= 0) return
    let gunpowderItem = itemList[0]
    if (!gunpowderItem.is('minecraft:gunpowder')) return
    let gunpowderCount = gunpowderItem.getCount()
    /**@type {Internal.LivingEntity[]} */
    let entityList = GetLivingWithinRadiusVec3d(level, playerPos, 5, (plevel, pEntity) => true)
    let damageSource = level.damageSources().explosion(player, null)
    entityList.forEach(pEntity => pEntity.attack(damageSource, gunpowderCount))
    RemoveBundleItem(organItem, 0, gunpowderCount)
    level.createExplosion(playerPos.x(), playerPos.y(), playerPos.z()).explode()
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:explosion_bag')
        .addOnlyStrategy('entity_tick', ExplosionBagEntityTick)
        .addOnlyStrategy('key_active', ExplosionBagKeyActive)
)

// ===== ferret_tail.js =====
RegistryOrgan('kubejs:ferret_tail')
    .addScore('chestcavity:nerves', 0.25)
    .addScore('chestcavity:speed', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FerretTailChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    chestCavity.setOrganScore('chestcavity:speed', 1)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:ferret_tail')
        .addOnlyStrategy('chest_cavity_update', FerretTailChestCavityUpdate, 1)
)

// ===== flamingo_beak.js =====
RegistryOrgan('kubejs:flamingo_beak')
    .addScore('chestcavity:defense', -1)
    .addScore('kubejs:crit_damage', 4)
    .setCanSpawn(true)

// ===== fox_tail.js =====
RegistryOrgan('kubejs:fox_tail')
    .addScore('chestcavity:strength', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FoxTailKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    const ccIns = player.chestCavityInstance
    player.addDeltaMovement(new Vec3d(0, Math.max(ccIns.getOrganScore('chestcavity:strength'), 1), 0))
    player.connection.send(new $ClientboundSetEntityMotionPacket(player))
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.horse.jump', player.getSoundSource(), 1, 1)
    player.addItemCooldown('kubejs:fox_tail', 20 * 10)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:fox_tail')
        .addOnlyStrategy('key_active', FoxTailKeyActive)
)

// ===== frog_tongue.js =====
RegistryOrgan('kubejs:frog_tongue')
    .addScore('chestcavity:strength', 1.5)
    .addScore('chestcavity:knockback_resistant', 0.5)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FrogTongueKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const chestCavity = player.chestCavityInstance
    SetCustomDataMap(chestCavity, 'frogTongueEffectReady', 1)
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex)
    SetOrganEffect(chestCavity, organEffect)
    player.addItemCooldown(organItem, 20 * 60)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FrogTongueChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:frog_tongue')
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.ItemEntityInteractedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function FrogTongueEntityInteract(customData, event, organItem, organIndex, slotType) {
    const target = event.target
    const player = event.player
    const level = event.level
    if (event.hand != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return

    const playerCC = player.chestCavityInstance
    let counter = GetCustomDataMap(playerCC, 'frogTongueEffectReady', 0)
    if (counter == 0) return

    if (playerCC.inventory.countEmpty() == 0) return
    if (!(target instanceof $LivingEntity)) return

    let targetCC = target.chestCavityInstance
    let ccInv = targetCC.inventory
    if (!targetCC.opened) {
        ccInv = ChestCavityUtils.openChestCavity(targetCC)
    }
    /**@type {number[]} */
    let validSlotIds = []
    for (let i = 0; i < ccInv.getContainerSize(); i++) {
        if (ccInv.getItem(i).isEmpty()) continue
        validSlotIds.push(i)
    }
    if (validSlotIds.length == 0) return
    /**@type {number} */
    let randomSlotId = RandomGet(validSlotIds)
    let extractedItem = ccInv.extractItem(randomSlotId, 1, false)
    playerCC.inventory.insertItem(extractedItem, false)
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'minecraft:entity.item.pickup', player.getSoundSource(), 1, 1)
    
    SetCustomDataMap(playerCC, 'frogTongueEffectReady', counter - 1)
    RemoveOrganEffect(playerCC, 'kubejs:frog_tongue')
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:frog_tongue')
        .addOnlyStrategy('key_active', FrogTongueKeyActive)
        .addOnlyStrategy('organ_take_off', FrogTongueChestCavityTakeOffOnly)
        .addOnlyStrategy('entity_interact', FrogTongueEntityInteract)
)

// ===== giant_claw.js =====
RegistryOrgan('kubejs:giant_claw')
    .addScore('kubejs:extreme_strength', 1)
    .addScore('chestcavity:luck', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function GiantClawEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (RandomWithPlayerLuck(entity) <= 0.8) return
    let amplifier = 0
    let duration = 200
    if (entity.hasEffect('cataclysm:wetness')) {
        let wetnessEffect = entity.getEffect('cataclysm:wetness')
        amplifier = Math.min(wetnessEffect.getAmplifier() + 1, 4)
    }
    entity.potionEffects.add('cataclysm:wetness', duration, amplifier)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:giant_claw')
        .addOnlyStrategy('entity_do_damage', GiantClawEntityDoDamage)
)

// ===== glow_gland.js =====
RegistryOrgan('kubejs:glow_gland')
    .addScore('kubejs:glowing', 1)
    .setCanSpawn(true)

// ===== guardian_core.js =====
RegistryOrgan('kubejs:guardian_core')
    .addScore('chestcavity:nerves', 1)
    .addScore('chestcavity:luck', 1)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OrganAddStatusEffectJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function GuardianCoreOrganAddStatusEffect(customData, event, organItem, organIndex, slotType) {
    const effectInstance = event.effect
    if (effectInstance.getEffect().equals($MobEffects.DIG_SLOWDOWN)) {
        event.setEffect(new $MobEffectInstance('minecraft:haste', effectInstance.getDuration(), effectInstance.getAmplifier()))
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:guardian_core')
        .addOnlyStrategy('organ_add_status_effect', GuardianCoreOrganAddStatusEffect)
)

// ===== hatching_chamber.js =====
RegistryOrgan('kubejs:hatching_chamber')
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)
/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HatchingChamberChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (!(entity instanceof $PathfinderMob)) return
    RemoveCustomGoalByName(entity.goalSelector, 'lay_loot')
    entity.goalSelector.addGoal(0, NewLayLootGoal(entity))
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HatchingChamberChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (!(entity instanceof $PathfinderMob)) return
    RemoveCustomGoalByName(entity.goalSelector, 'lay_loot')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:hatching_chamber')
        .addOnlyStrategy('organ_take_on', HatchingChamberChestCavityTakeOn)
        .addOnlyStrategy('organ_take_off', HatchingChamberChestCavityTakeOff)
)

// ===== hedgehog_thorn.js =====
RegistryOrgan('kubejs:hedgehog_thorn')
    .addScore('chestcavity:defense', 1)
    .addScore('kubejs:extreme_fitness', 1)
    .setCanSpawn(true)
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function HedgehogThornEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity instanceof $LivingEntity) {
        customData.thornsDamage = customData.thornsDamage + entity.armorValue
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:hedgehog_thorn')
        .addOnlyStrategy('entity_be_hurt', HedgehogThornEntityBeHurt)
)

// ===== horse_tendon.js =====
RegistryOrgan('kubejs:horse_tendon')
    .addScore('kubejs:steppy', 0.75)
    .addScore('chestcavity:strength', 0.5)
    .setCanSpawn(true)

// ===== jumping_spider_leg.js =====
RegistryOrgan('kubejs:jumping_spider_leg')
    .addScore('chestcavity:knockback_resistant', -0.5)
    .setCanSpawn(true)
/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.OpenedEntityTickJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function JumpingSpiderLegEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    // if (entity.type == 'crittersandcompanions:jumping_spider') return
    if (!entity.onGround()) return
    entity.setDeltaMovement(entity.getDeltaMovement().add(0, 0.42, 0))
    entity.setJumping(true)
    if (entity.isPlayer()) {
        entity.hasImpulse = true
        entity.connection.send(new $ClientboundSetEntityMotionPacket(entity))
    }
}
RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:jumping_spider_leg')
        .addOnlyStrategy('entity_tick', JumpingSpiderLegEntityTick)
)

// ===== koi_fish_scale.js =====
RegistryOrgan('kubejs:koi_fish_scale')
    .addScore('chestcavity:knockback_resistant', -0.5)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function KoiFishScaleKeyActive(customData, event, organItem, organIndex, slotType) {
    const level = event.level
    const player = event.player

    if (level.getDimension() != 'minecraft:overworld') return
    let lootTableId = 'kubejs:koi_fish_loot/weather_clear'
    if (level.isThundering()) lootTableId = 'kubejs:koi_fish_loot/weather_thunder'
    else if (level.isRaining()) lootTableId = 'kubejs:koi_fish_loot/weather_rain'
    let chestPos = getKoiChestSpawnLocation(level, player)

    let mapItem = $MapItem.create(level, chestPos.x, chestPos.z, 1, true, true)
    $MapItem.renderBiomePreviewMap(level, mapItem)
    $MapItemSavedData.addTargetDecoration(mapItem, chestPos, "+", $MapDecorationType.RED_X)
    player.give(mapItem.withName(Text.translatable('map.kubejs.airdrop')))
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'item.book.page_turn', player.getSoundSource(), 0.5, 1)

    GenLootrChestWithLootTable(level, chestPos, lootTableId)
    // 增强功能
    // CreateWaypoint(player, chestPos, new Date().toLocaleString(), 0xFC4C00)
    player.addItemCooldown('kubejs:koi_fish_scale', 20 * 180)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:koi_fish_scale')
        .addOnlyStrategy('key_active', KoiFishScaleKeyActive, 1)
)

/**
 * @param {Internal.Level} level 
 * @param {Internal.ServerPlayer} player 
 * @return {BlockPos}
 */
function getKoiChestSpawnLocation(level, player) {
    const luck = Math.max(player.getLuck(), 0)
    const distance = Math.max(1500 - luck * 50, 100) + Math.random() * 100

    let deltaDim = Math.floor(Math.random() * 4) + 1
    let deltaX = Math.pow(-1, Math.floor(deltaDim / 2)) * Math.random() * distance
    let deltaZ = Math.pow(-1, Math.floor((deltaDim + 1) / 2)) * Math.sqrt(Math.pow(distance, 2) - Math.pow(deltaX, 2))
    let ranPosBlock = player.block.offset(deltaX, 0, deltaZ)

    let chunkX = Math.floor(ranPosBlock.x / 16)
    let chunkZ = Math.floor(ranPosBlock.z / 16)
    let blockX = ranPosBlock.x % 16
    let blockZ = ranPosBlock.z % 16

    let targetChunk = level.getChunk(chunkX, chunkZ, 'surface', true)
    let y = Math.min(targetChunk.getHeight('motion_blocking', blockX, blockZ), 255)
    return new BlockPos(ranPosBlock.x, y - 4, ranPosBlock.z)
}

// ===== leaf_insect_exoskeleton.js =====
RegistryOrgan('kubejs:leaf_insect_exoskeleton')
    .addScore('chestcavity:defense', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function LeafInsectExoskeletonChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const { chestCavity } = event
    let defenseScore = chestCavity.getOrganScore('chestcavity:defense')
    chestCavity.setOrganScore('chestcavity:strength', defenseScore)
}



RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:leaf_insect_exoskeleton')
        .addOnlyStrategy('chest_cavity_update', LeafInsectExoskeletonChestCavityUpdate)
)

// ===== leech_mouthpart.js =====
RegistryOrgan('kubejs:leech_mouthpart')
    .addScore('kubejs:extreme_strength', 1)
    .addScore('chestcavity:digestion', 1)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function LeechMouthpartEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (entity.isPlayer() && RandomWithPlayerLuck(entity) < 0.5) {
        let foodData = entity.getFoodData()
        foodData.setSaturation(Math.min(foodData.getSaturationLevel() + 1, 20))
    } else if (entity.getHealth() < entity.getMaxHealth()) {
        entity.heal(1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:leech_mouthpart')
        .addOnlyStrategy('entity_do_damage', LeechMouthpartEntityDoDamage)
)

// ===== llama_gland.js =====
RegistryOrgan('kubejs:llama_gland')
    .addScore('chestcavity:speed', 1)
    .addScore('chestcavity:breath_recovery', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function LlamaGlandKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    SpitTowardFacing(player, level)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:llama_gland')
        .addOnlyStrategy('key_active', LlamaGlandKeyActive)
)

// ===== minisheep_gland.js =====
RegistryOrgan('kubejs:minisheep_gland')
    .addScore('chestcavity:defense', 1)
    .addScore('chestcavity:detoxification', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function MinisheepGlandEntityBeInteracted(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const entity = event.entity
    const target = event.target
    if (event.getHand() != 'main_hand') return
    if (target instanceof $LivingEntity && !target.hasEffect('minecraft:weakness')) {
        entity.give(Item.of('minecraft:white_wool').withName(Text.translate('item_name.kubejs.minisheep_gland_bucket.name', target.getName())))
        target.potionEffects.add('minecraft:weakness', 20 * 60)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:minisheep_gland')
        .addOnlyStrategy('entity_be_interacted', MinisheepGlandEntityBeInteracted)
)

// ===== moew_nose.js =====
RegistryOrgan('kubejs:moew_nose')
    .addScore('chestcavity:luck', 1.5)
    .setCanSpawn(true)

const MoewLanguageCharCodeMap = [
    Text.translatable('msg.kubejs.moew_nose.char_code.1').getString(),
    Text.translatable('msg.kubejs.moew_nose.char_code.2').getString(),
    Text.translatable('msg.kubejs.moew_nose.char_code.3').getString(),
    Text.translatable('msg.kubejs.moew_nose.char_code.4').getString(),
    Text.translatable('msg.kubejs.moew_nose.char_code.5').getString(),
]
const MoewLanguageBegin = Text.translatable('msg.kubejs.moew_nose.begin.1').getString()

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.PlayerChatDecorateEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MoewNoseDecorateChat(customData, event, organItem, organIndex, slotType) {
    let msg = String(event.getMessage())
    let resMsg = MoewNoseEncode(msg)
    resMsg = MoewLanguageBegin + resMsg
    event.setMessage(Text.of(resMsg))
}


/**
 * 编码函数
 * @param {string} input 原始字符串
 * @returns {string} 编码后的字符串
 */
function MoewNoseEncode(input) {
    let encoded = ''
    const radix = MoewLanguageCharCodeMap.length
    for (let i = 0; i < input.length; i++) {
        let charCode = input.charCodeAt(i).charValue()
        let baseStr = charCode.toString(radix)
        for (let j = 0; j < baseStr.length; j++) {
            let index = parseInt(baseStr[j], radix)
            encoded += MoewLanguageCharCodeMap[index]
        }
        encoded += ' '
    }
    return encoded.trim()
}

/**
 * 解码函数
 * @param {string} input 编码后的字符串
 * @returns {string} 原始字符串
 */
function MoewNoseDecode(input) {
    const radix = MoewLanguageCharCodeMap.length
    const words = input.split(' ')
    let decoded = ''

    for (let i = 0; i < words.length; i++) {
        let word = words[i]
        let baseStr = ''
        for (let j = 0; j < word.length; j++) {
            let char = word[j]
            let index = MoewLanguageCharCodeMap.indexOf(char)
            baseStr += index.toString()
        }
        if (baseStr) {
            let charCode = parseInt(baseStr, radix)
            decoded += String.fromCharCode(charCode)
        }
    }
    return decoded.trim()
}
/**
 * 解码使用范例
 *if (resMsg.startsWith(MoewLanguageBegin)) {
 *   resMsg = resMsg.substring(MoewLanguageBegin.length)
 *   console.log(MoewNoseDecode(resMsg))
 *}
 */

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:moew_nose')
        .addOnlyStrategy('decorate_chat', MoewNoseDecorateChat)
)

// ===== otter_tooth.js =====
// ServerEvents.recipes(event => {
//     $AxeItem.STRIPPABLES.put(Block.getBlock('ars_nouveau:blue_archwood_log'), Block.getBlock('ars_nouveau:stripped_blue_archwood_log'))
//     $AxeItem.STRIPPABLES.put(Block.getBlock('ars_nouveau:red_archwood_log'), Block.getBlock('ars_nouveau:stripped_red_archwood_log'))
//     $AxeItem.STRIPPABLES.put(Block.getBlock('ars_nouveau:purple_archwood_log'), Block.getBlock('ars_nouveau:stripped_purple_archwood_log'))
//     $AxeItem.STRIPPABLES.put(Block.getBlock('ars_nouveau:green_archwood_log'), Block.getBlock('ars_nouveau:stripped_green_archwood_log'))
// })
RegistryOrgan('kubejs:otter_tooth')
    .addScore('chestcavity:strength', 1)
    .addScore('chestcavity:swim_speed', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.BlockRightClickedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function OtterToothBlockRightClicked(customData, event, organItem, organIndex, slotType) {
    const level = event.level
    const block = event.block
    if (event.hand != 'main_hand') return
    if (!event.item.isEmpty()) return
    let resultBlockState = $AxeItem.getAxeStrippingState(block.blockState)
    if (!resultBlockState) return
    block.popItemFromFace(Item.of('kubejs:sawdust', Math.ceil(Math.random() * 4)), event.facing)
    level.playSound(null, block.pos.getX(), block.pos.getY(), block.pos.getZ(), 'item.axe.strip', $SoundSource.BLOCKS, 1, 1)
    level.setBlockAndUpdate(block.pos, resultBlockState)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:otter_tooth')
        .addOnlyStrategy('block_right_clicked', OtterToothBlockRightClicked)
)

// ===== owl_vertebrae.js =====
RegistryOrgan('kubejs:owl_vertebrae')
    .addScore('chestcavity:nerves', 1)
    .addScore('chestcavity:defense', 0.5)
    .addScore('chestcavity:endurance', -1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.OpenedEntityTickJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function OwlVertebraeEntityTick(customData, event, organItem, organIndex, slotType) {
    const level = event.level
    const entity = event.entity
    if (entity.hasEffect('minecraft:night_vision') && entity.getEffect('minecraft:night_vision').getDuration() > 20 * 15) return
    if (!level.isDay()) {
        entity.potionEffects.add('minecraft:night_vision', 20 * 30, 0, false, false)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:owl_vertebrae')
        .addOnlyStrategy('entity_tick', OwlVertebraeEntityTick)
)

// ===== parrot_beak.js =====
RegistryOrgan('kubejs:parrot_beak')
    .addScore('chestcavity:luck', 1.5)
    .setCanSpawn(true)

const ParrotBeakInstrumentMap = {
    'minecraft:creeper_head': 'entity.creeper.primed',
    'minecraft:dragon_head': 'entity.ender_dragon.ambient',
    'minecraft:piglin_head': 'entity.piglin.ambient',
    'minecraft:skeleton_skull': 'entity.skeleton.ambient',
    'minecraft:wither_skeleton_skull': 'entity.wither_skeleton.ambient',
    'minecraft:zombie_head': 'entity.zombie.ambient',
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function ParrotBeakKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    Item.of('minecraft:creeper_head')
    let contentsItem = GetBundleContents(organItem)
    if (contentsItem.length <= 0) return
    let instrumentItem = contentsItem[0]
    if (instrumentItem.isEmpty() || !instrumentItem.isBlock()) return
    let instrumentName = ParrotBeakInstrumentMap[instrumentItem.id]
    if (!instrumentName) return
    level.playSound(null, player.getX(), player.getY(), player.getZ(), instrumentName, player.getSoundSource(), 1, 1)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:parrot_beak')
        .addOnlyStrategy('key_active', ParrotBeakKeyActive)
)

// ===== pelican_larynx.js =====
RegistryOrgan('kubejs:pelican_larynx')
    .addScore('chestcavity:endurance', -1.5)
    .addScore('chestcavity:digestion', 1)
    .setCanSpawn(true)


/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function PelicanLarynxEntityInteract(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    /**@type {Internal.PathfinderMob} */
    const target = event.target
    if (!(target.isLiving() && target instanceof $PathfinderMob)) return
    const level = event.level
    if (event.getHand() != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    if (OrganItemCoolDown(player, organItem)) return
    if (player.getMaxHealth() <= target.getMaxHealth()) return
    if (target.isPlayer()) return
    let itemNbt = new $CompoundTag()
    if (organItem.hasNBT()) {
        itemNbt = organItem.getNbt()
        if (itemNbt.contains('type')) {
            let targetNbt = itemNbt.get('entityNbt')
            let targetId = itemNbt.getString('type')
            let entity = level.createEntity(targetId)
            entity.setNbt(targetNbt)
            entity.setPos(target.getX(), target.getY(), target.getZ())
            entity.spawn()
        }
    }
    let targetNbt = target.getNbt()
    let targetId = target.type
    itemNbt.putString('type', targetId)
    itemNbt.put('entityNbt', targetNbt)
    organItem.setNbt(itemNbt)
    target.discard()
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.player.burp', player.getSoundSource(), 0.5, 1)
    player.addItemCooldown(organItem, 20 * 15)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pelican_larynx')
        .addOnlyStrategy('entity_interact', PelicanLarynxEntityInteract)
)

// ===== penguin_flipper.js =====
RegistryOrgan('kubejs:penguin_flipper')
    .addScore('chestcavity:swim_speed', 1)
    .setCanSpawn(true)
    
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PenguinFlipperEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    let feetBlockPos = entity.blockPosition().below()
    let feetBlockState = level.getBlockState(feetBlockPos)
    if (!feetBlockState.isAir()) return
    level.setBlockAndUpdate(feetBlockPos, Blocks.FROSTED_ICE.defaultBlockState())
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:penguin_flipper')
        .addOnlyStrategy('entity_tick', PenguinFlipperEntityTick)
)

// ===== pig_stomach.js =====
RegistryOrgan('kubejs:pig_stomach')
    .addScore('chestcavity:digestion', 1.5)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)
    
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PigStomachFoodEaten(customData, event, organItem, organIndex, slotType) {
    /**@type {Player} */
    const entity = event.entity
    if (!entity.isPlayer()) return
    const chestCavity = event.chestCavity
    let digestion = chestCavity.getOrganScore('chestcavity:digestion')
    entity.potionEffects.add('farm_and_charm:sustenance', Math.max(200, digestion * 20 * 5 + 200), 0, false, false)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pig_stomach')
        .addOnlyStrategy('food_eaten', PigStomachFoodEaten)
)

// ===== pillager_gland.js =====
RegistryOrgan('kubejs:pillager_gland')
    .addScore('chestcavity:defense', -1)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.VillagerUpdateSpecialPrices} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function PillagerGlandVillagerUpdateSpecialPrices(customData, event, organItem, organIndex, slotType) {
    event.villager.offers.forEach(pOffer => {
        let price = pOffer.getBaseCostA().getCount()
        let currentDiscount = pOffer.getSpecialPriceDiff() / price
        pOffer.addToSpecialPriceDiff(Math.round((1.0 + currentDiscount) * -0.5 * price))
    })
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pillager_gland')
        .addOnlyStrategy('villager_update_special_prices', PillagerGlandVillagerUpdateSpecialPrices)
)

// ===== pufferfish_liver.js =====
RegistryOrgan('kubejs:pufferfish_liver')
    .addScore('chestcavity:detoxification', 1)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function PufferfishLiverEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const source = event.source.actual
    if (!source || !source.isLiving()) return
    if (source.hasEffect('minecraft:poison')) return
    source.potionEffects.add('minecraft:poison', 10 * 20, 0, false, true)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pufferfish_liver')
        .addOnlyStrategy('entity_be_hurt', PufferfishLiverEntityBeHurt)
)

// ===== pumpkin_shooter.js =====
RegistryOrgan('kubejs:pumpkin_shooter')
    .addScore('chestcavity:nerves', 1)
    .setCanSpawn(true)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PumpkinShooterChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const ccInv = chestCavity.inventory
    let llamaGlandCount = 0
    let ghastSacCount = 0
    ccInv.allItems.forEach(pItem => {
        if (pItem.isEmpty()) return
        if (pItem.is('kubejs:llama_gland')) {
            llamaGlandCount++
        } else if (pItem.is('kubejs:ghast_sac')) {
            ghastSacCount++
        }
    })
    SetCustomDataMap(chestCavity, 'llamaGlandCount', llamaGlandCount)
    SetCustomDataMap(chestCavity, 'ghastSacCount', ghastSacCount)
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PumpkinShooterEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    const chestCavity = entity.chestCavityInstance
    if (entity.age % 20 != 0) return
    if (GetCustomDataMap(chestCavity, 'llamaGlandCount', 0) > 0) {
        SpitTowardFacing(entity, level)
    } else if (GetCustomDataMap(chestCavity, 'ghastSacCount', 0) > 0) {
        SummonFireballTowardFacing(entity, level)
    } else {
        SummonSnowballTowardFacing(entity, level)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pumpkin_shooter')
        .addOnlyStrategy('entity_tick', PumpkinShooterEntityTick)
        .addOnlyStrategy('chest_cavity_update', PumpkinShooterChestCavityUpdate)
)

// ===== rabbit_foot.js =====
RegistryOrgan('kubejs:rabbit_foot')
    .addScore('kubejs:jump_height', 1)
    .setCanSpawn(true)

// ===== raccoon_paw.js =====
RegistryOrgan('kubejs:raccoon_paw')
    .addScore('chestcavity:strength', 1)
    .addScore('kubejs:knockback', 3)
    .setCanSpawn(true)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.ItemEntityInteractedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RacconPawEntityInteract(customData, event, organItem, organIndex, slotType) {
    const target = event.target
    const player = event.player
    if (event.hand != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    if (OrganItemCoolDown(player, organItem)) return
    switch (target.type) {
        case 'minecraft:villager':
            break
        default:
            let allSlotsList = []
            let i = 0
            target.getAllSlots().forEach(item => {
                if (!item.isEmpty() && !item.hasEnchantment('minecraft:binding_curse', 1)) {
                    allSlotsList.push({ item: item, slot: i })
                }
                i++
            })
            if (allSlotsList.length == 0) return
            let randomObj = RandomGet(allSlotsList)
            SpawnLootAtLocation(event.level, player.blockPosition(), [randomObj.item])
            target.setItemSlot(EquimentSlotList[randomObj.slot], Item.empty)
    }

    player.addItemCooldown(organItem, 20 * 10)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:raccoon_paw')
        .addOnlyStrategy('entity_interact', RacconPawEntityInteract)
)

// ===== rat_ear.js =====
RegistryOrgan('kubejs:rat_ear')
    .addScore('kubejs:attack_dodge', 1)
    .addScore('chestcavity:speed', 1)
    .setCanSpawn(true)
    
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RatEarEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    const blockPos = entity.blockPosition()
    /**@type {Internal.LivingEntity[]} */
    let nearEntities = GetLivingWithinRadius(level, blockPos, 16, (pLevel, pEntity) => {
        return !pEntity.isPlayer() && pEntity.isAlive()
    })
    nearEntities.forEach(pEntity => {
        pEntity.potionEffects.add('minecraft:glowing', 20 * 12, 0, false, false)
    })
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rat_ear')
        .addOnlyStrategy('entity_tick', RatEarEntityTick)
)

// ===== ravager_hoof.js =====
// RegistryOrgan('kubejs:ravager_hoof')
    // .addScore('chestcavity:defense', 1)
    // .setCanSpawn(true)
// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function RavagerHoofChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:advance_stomp', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function RavagerHoofTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:advance_stomp')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:ravager_hoof')
        // .addOnlyStrategy('chest_cavity_update', RavagerHoofChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', RavagerHoofTakeOff)
// )

// ===== red_panda_tail.js =====
RegistryOrgan('kubejs:red_panda_tail')
    .addScore('chestcavity:speed', 1)
    .addScore('chestcavity:endurance', 0.5)
    .setCanSpawn(true)
    
/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RedPandaTailEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    const blockPos = entity.blockPosition()
    if (!level.isEmptyBlock(blockPos)) return
    let feetBlock = level.getBlock(blockPos.getX(), blockPos.getY() - 1, blockPos.getZ())
    let feetBlockState = feetBlock.getBlockState()
    if (!feetBlockState) return
    let berryBush = 'minecraft:sweet_berry_bush'
    // let berryBush = 'ars_nouveau:sourceberry_bush'
    let canPlant = feetBlockState.block.canSustainPlant(feetBlockState, level, blockPos, Direction.DOWN, Block.getBlock(berryBush))
    if (!canPlant) return
    level.setBlockAndUpdate(blockPos, Block.getBlock(berryBush).defaultBlockState())
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:red_panda_tail')
        .addOnlyStrategy('entity_tick', RedPandaTailEntityTick)
)

// ===== red_wolf_beard.js =====
RegistryOrgan('kubejs:red_wolf_beard')
    .addScore('chestcavity:strength', 1)
    .setCanSpawn(true)

const RedWolfBeardTempSpeedUpUUID = UUID.fromString('C8A46567-E000-48B8-8846-BD2946D4D51F')

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RedWolfBeardEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    let oldDamage = organItem.getDamageValue()
    let attributeInstance = event.entity.getAttribute('minecraft:generic.movement_speed')
    if (oldDamage == organItem.getMaxDamage()) {
        if (attributeInstance.getModifier(RedWolfBeardTempSpeedUpUUID)) {
            attributeInstance.removeModifier(RedWolfBeardTempSpeedUpUUID)
        }
        return
    }

    organItem.setDamageValue(oldDamage + 1)
    if (!attributeInstance.getModifier(RedWolfBeardTempSpeedUpUUID)) {
        let attributeModifier = new $AttributeModifier(RedWolfBeardTempSpeedUpUUID, 'RedWolfBeardTempSpeedUp', 0.5, $Operation.MULTIPLY_BASE)
        attributeInstance.addPermanentModifier(attributeModifier)
    }
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RedWolfBeardTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    let attributeInstance = event.entity.getAttribute('minecraft:generic.movement_speed')
    if (attributeInstance.getModifier(RedWolfBeardTempSpeedUpUUID)) {
        attributeInstance.removeModifier(RedWolfBeardTempSpeedUpUUID)
    }

    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:red_wolf_beard')
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RedWolfBeardTakeOn(customData, event, organItem, organIndex, slotType) {
    const { chestCavity, entity } = event
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDeathEvent} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RedWolfBeardEntityKill(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    let damageValue = organItem.getDamageValue()
    if (damageValue == 0 || damageValue == organItem.getMaxDamage()) return
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    if (!sourceEntity.isLiving()) return
    const chestCavity = sourceEntity.chestCavityInstance
    organItem.setDamageValue(damageValue - 3)
    sourceEntity.heal(4)
    sourceEntity.potionEffects.add('minecraft:absorption', 20 * 10, 0)
    if (sourceEntity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RedWolfBeardKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    organItem.setDamageValue(0)
    player.addItemCooldown('kubejs:red_wolf_beard', 20 * 120)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:red_wolf_beard')
        .addOnlyStrategy('entity_tick', RedWolfBeardEntityTick)
        .addOnlyStrategy('organ_take_off', RedWolfBeardTakeOff)
        .addOnlyStrategy('organ_take_on', RedWolfBeardTakeOn)
        .addOnlyStrategy('entity_kill', RedWolfBeardEntityKill)
        .addOnlyStrategy('key_active', RedWolfBeardKeyActive)
)

// ===== refill_agreement.js =====
RegistryOrgan('kubejs:refill_agreement')
    .addScore('chestcavity:luck', 1)
    .setCanSpawn(true)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.ItemEntityInteractedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RefillAgreementEntityInteract(customData, event, organItem, organIndex, slotType) {
    const target = event.target
    const level = event.level
    const player = event.player
    if (event.getHand() != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    if (OrganItemCoolDown(player, organItem)) return
    if (target instanceof $Villager && target.needsToRestock()) {
        level.spawnParticles($ParticleTypes.HEART, true, target.getX(), target.getY(), target.getZ(), 0.5, 0.5, 0.5, 20, 0.1)
        target.restock()
        player.addItemCooldown(organItem, 20 * 30)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:refill_agreement')
        .addOnlyStrategy('entity_interact', RefillAgreementEntityInteract)
)

// ===== rib_blade.js =====
RegistryOrgan('kubejs:rib_blade')
    .addScore('chestcavity:defense', 0.5)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LootContextJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RibBladeEntityLoot(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const killer = event.killerEntity
    const level = event.level
    if (!killer.isLiving() || !killer.isPlayer()) return
    if (RandomWithPlayerLuck(killer) <= 0.5) return
    let lootTable = $DespoilLootModifier.getLootTable(level, entity)
    let loot = event.loot
    let stackSplitter = $LootTable.createStackSplitter(level, (i) => {
        return loot.add(i)
    })
    let lootParams = new $LootParamsBuilder(level)
        .withParameter($LootContextParams.THIS_ENTITY, entity)
        .withParameter($LootContextParams.DAMAGE_SOURCE, event.damageSource)
        .withParameter($LootContextParams.ORIGIN, event.blockPos)
        .withOptionalParameter($LootContextParams.KILLER_ENTITY, killer)
        .withOptionalParameter($LootContextParams.DIRECT_KILLER_ENTITY, killer)
        .create($LootContextParamSets.ENTITY)

    $DespoilLootModifier.getRandomItems(lootTable, lootParams, stackSplitter)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rib_blade')
        .addOnlyStrategy('entity_loot', RibBladeEntityLoot)
)

// ===== scry_stomach_pouch.js =====
// RegistryOrgan('kubejs:scry_stomach_pouch')
    // .addScore('chestcavity:nutrition', 1)
    // .addScore('kubejs:magic_capacity', 2)
    // .setCanSpawn(true)

// /**
// * @param {OrganChestCavityUpdateStrategyCustomData} customData
// * @param {Internal.NetworkEventJS} event 
// * @param {Internal.ItemStack} organItem
// * @param {number} organIndex
// * @param {string} slotType
// */
// function ScryStomachPouchKeyActiveOnly(customData, event, organItem, organIndex, slotType) {
    // const player = event.player
    // const level = event.level
    // let renderList = []
    // /** @type {string[]} */
    // let revealBlockList = []
    // let contentsItem = GetBundleContents(organItem)
    // for (let pItemStack of contentsItem) {
        // if (pItemStack.isEmpty()) continue
        // let pItem = pItemStack.getItem()
        // if (!(pItem instanceof $BlockItem)) continue
        // let pBlock = pItem.getBlock()
        // if (pBlock.id == 'minecraft:chest') revealBlockList.push('lootr:lootr_chest')
        // if (pBlock.id == 'minecraft:barrel') revealBlockList.push('lootr:lootr_barrel')
        // revealBlockList.push(pBlock.id)
    // }
    // if (revealBlockList.length <= 0) return
    // for (let pBlockPos of BlockPos.withinManhattan(player.blockPosition(), 16, 32, 16)) {
        // if (level.isOutsideBuildHeight(pBlockPos)) continue
        // let blockState = level.getBlockState(pBlockPos)
        // if (blockState.isAir()) continue
        // if (renderList.length >= 50) break
        // let block = blockState.getBlock()
        // for (let pBlock of revealBlockList) {
            // if (pBlock == block.id) {
                // renderList.push(new OutlineRenderModel(pBlockPos, 0xf50000).setTime(level.time + 20 * 60))
                // break
            // }
        // }
    // }
    // HighlightBlockPos(player, renderList)
    // player.addItemCooldown(organItem, 20 * 60)
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:scry_stomach_pouch')
        // .addOnlyStrategy('key_active', ScryStomachPouchKeyActiveOnly)
// )

// ===== sea_bunny_skin.js =====
// RegistryOrgan('kubejs:sea_bunny_skin')
    // .addScore('kubejs:extreme_fitness', 2)
    // .addScore('chestcavity:swim_speed', 1.5)
    // .setCanSpawn(true)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.LivingDamageEvent} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function SeaBunnyGlandEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (entity instanceof $ServerPlayer) {
        // let magicData = entity.getMagicData()
        // let curMana = magicData.getMana()
        // if (curMana <= 30) return
        // magicData.setMana(curMana - 30)
    // }
    // entity.potionEffects.add('irons_spellbooks:true_invisibility', 20 * 3, 0, false, false)
// }


// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:sea_bunny_skin')
        // .addOnlyStrategy('entity_be_hurt', SeaBunnyGlandEntityBeHurt)
// )

// ===== shima_enaga_feather.js =====
RegistryOrgan('kubejs:shima_enaga_feather')
    .addScore('chestcavity:luck', 2)
    .addScore('kubejs:attack_dodge', 1)
    .setCanSpawn(true)

// ===== silverfish_gland.js =====
RegistryOrgan('kubejs:silverfish_gland')
    .addScore('chestcavity:climbing', 0.5)
    .addScore('chestcavity:metabolism', 0.5)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.BlockRightClickedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SilverfishGlandBlockRightClicked(customData, event, organItem, organIndex, slotType) {
    const block = event.block
    const level = event.level
    const player = event.player
    if (event.hand != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    const state = block.getBlockState()
    if (!$InfestedBlock.isCompatibleHostBlock(state)) return
    level.setBlockAndUpdate(block.getPos(), $InfestedBlock.infestedStateByHost(state))
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:silverfish_gland')
        .addOnlyStrategy('block_right_clicked', SilverfishGlandBlockRightClicked)
)

// ===== slime_colloid.js =====
RegistryOrgan('kubejs:slime_colloid')
    .addScore('chestcavity:detoxification', 1)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

const SlimColloidEntityGrowUpMap = {
    'minecraft:guardian': 'minecraft:elder_guardian',
    'cataclysm:netherite_ministrosity': 'cataclysm:netherite_monstrosity',
    'cataclysm:modern_remnant': 'cataclysm:ancient_remnant',
    'cataclysm:the_baby_leviathan': 'cataclysm:the_leviathan',
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.ItemEntityInteractedEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SlimeColloidEntityInteract(customData, event, organItem, organIndex, slotType) {
    const target = event.target
    const level = event.level
    const player = event.player
    if (event.getHand() != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    if (!(target instanceof $Mob)) return
    if (target.type == 'minecraft:tadpole') {
        // target.biomancy$AgeUp()
        level.spawnParticles($ParticleTypes.HEART, true, target.getX(), target.getY(), target.getZ(), 0.5, 0.5, 0.5, 20, 0.1)
    } else if (SlimColloidEntityGrowUpMap[target.type]) {
        $MobUtil.convertMobTo(level, target, SlimColloidEntityGrowUpMap[target.type], true, (old, elder) => { })
        level.spawnParticles($ParticleTypes.HEART, true, target.getX(), target.getY(), target.getZ(), 0.5, 0.5, 0.5, 20, 0.1)
    } else if (target.isBaby()) {
        $MobUtil.convertToAdult(target)
        level.spawnParticles($ParticleTypes.HEART, true, target.getX(), target.getY(), target.getZ(), 0.5, 0.5, 0.5, 20, 0.1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:slime_colloid')
        .addOnlyStrategy('entity_interact', SlimeColloidEntityInteract)
)

// ===== spider_leg.js =====
RegistryOrgan('kubejs:spider_leg')
    .addScore('chestcavity:climbing', 1)
    .addScore('chestcavity:strength', 1)
    .setCanSpawn(true)

// ===== squirrel_tail.js =====
RegistryOrgan('kubejs:squirrel_tail')
    .addScore('kubejs:extreme_strength', -1)
    .addScore('chestcavity:digestion', 2)
    .setCanSpawn(true)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SquirrelTailChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    const inventory = chestCavity.inventory
    let allItemCount = 0

    inventory.allItems.forEach(item => {
        allItemCount += item.count
    })

    customData.attackDamage.addAttributeModifier(allItemCount * 0.1, 'addition', 'base')
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:squirrel_tail')
        .addOnlyStrategy('chest_cavity_update', SquirrelTailChestCavityUpdate)
)

// ===== sulfur_cube_colloid.js =====
// RegistryOrgan('kubejs:sulfur_cube_colloid')
//     .addScore('chestcavity:endurance', 1)
//     .setCanSpawn(true)

// /**
//  * @param {OrganEventCustomData} customData
//  * @param {Internal.BlockRightClickedEventJS} event 
//  * @param {Internal.ItemStack} organItem
//  * @param {number} organIndex
//  * @param {string} slotType
//  */
// function SulfurCubeColloidBlockRightClicked(customData, event, organItem, organIndex, slotType) {
//     const block = event.block
//     const level = event.level
//     const player = event.player
//     if (event.hand != 'main_hand') return
//     if (!player.mainHandItem.isEmpty()) return
//     let state = block.getBlockState()
//     let blockSummon = block instanceof $AbstractSkullBlock ? new $AnimHeadSummon(level, state, new $CompoundTag()) : new $AnimBlockSummon(level, state)
//     blockSummon.setColor(player.getTeamColor())
//     blockSummon.setPos(block.getPos())
//     blockSummon.setTicksLeft(20 * 300)
//     blockSummon.setAggressive(true)
//     blockSummon.setTame(true)
//     blockSummon.tame(player)
//     blockSummon.setOwnerID(player.getUuid())
//     level.addFreshEntity(blockSummon)
//     level.removeBlock(block.getPos(), false)
// }



// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:sulfur_cube_colloid')
//         .addOnlyStrategy('block_right_clicked', SulfurCubeColloidBlockRightClicked)
// )

// ===== swim_bladder.js =====
RegistryOrgan('kubejs:swim_bladder')
    .addScore('chestcavity:breath_capacity', 3)
    .setCanSpawn(true)

// ===== symbiocto_arm.js =====
RegistryOrgan('kubejs:symbiocto_arm')
    .addScore('chestcavity:strength', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SymbioctoArmEntityInteract(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const player = event.player
    if (!player) return
    if (event.getHand() != 'main_hand') return
    if (!player.mainHandItem.isEmpty()) return
    const target = event.target
    if (player.isVehicle()) return
    target.startRiding(player, true)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.ItemEntityInteractedEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function SymbioctoArmEntityBeInteracted(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const player = event.player
    if (!player) return
    const target = event.target
    if (target.isVehicle()) return
    player.startRiding(target, true)
}


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingFallEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function SymbioctoArmEntityFall(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (event.distance < 3) return
    if (!entity.isPlayer()) return
    if (entity.getPassengers().size() <= 0) return
    entity.getFirstPassenger().stopRiding()
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:symbiocto_arm')
        .addOnlyStrategy('entity_interact', SymbioctoArmEntityInteract)
        .addOnlyStrategy('entity_be_interacted', SymbioctoArmEntityBeInteracted)
        .addOnlyStrategy('entity_fall', SymbioctoArmEntityFall)
)

// ===== tentacles_harvester.js =====
RegistryOrgan('kubejs:tentacles_harvester')
    .addScore('chestcavity:digestion', 1)
    .addScore('chestcavity:defense', -2)


/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TentaclesHarvesterChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    SetCustomDataMap(entity.chestCavityInstance, 'hasTentaclesHarvester', 1)
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TentaclesHarvesterChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    RemoveCustomDataMap(entity.chestCavityInstance, 'hasTentaclesHarvester')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:tentacles_harvester')
        .addOnlyStrategy('organ_take_on', TentaclesHarvesterChestCavityTakeOn)
        .addOnlyStrategy('organ_take_off', TentaclesHarvesterChestCavityTakeOff)
)

// ===== turkey_gizzard.js =====
// RegistryOrgan('kubejs:turkey_gizzard')
//     .addScore('chestcavity:digestion', 1)
//     .addScore('chestcavity:nutrition', 1)
//     .setCanSpawn(true)

// /**
//  * @param {OrganChestCavityUpdateStrategyCustomData} customData
//  * @param {Internal.FoodEatenEventJS} event
//  * @param {Internal.ItemStack} organItem
//  * @param {number} organIndex
//  * @param {string} slotType
//  */
// function TurkeyGizzardFoodEaten(customData, event, organItem, organIndex, slotType) {
//     const player = event.player
//     if (!player) return
//     const level = event.level
//     const item = event.item
//     let foodProperties = item.getFoodProperties(player)
//     if (!foodProperties) return
//     if (foodProperties.canAlwaysEat()) return
//     if (RandomWithPlayerLuck(player) <= 0.6) return
//     const chestCavity = player.chestCavityInstance
//     const ccInv = chestCavity.inventory
//     const invTypeData = chestCavity.getInventoryTypeData()
//     let eightDirectionRelativeSlot = GetDirectionRelativeSlotByParam(invTypeData, organIndex, EightDirectionOffset)
//     for (let slotDefinition of eightDirectionRelativeSlot) {
//         let curItem = ccInv.getStackInSlot(slotDefinition.getId())
//         if (curItem.isEmpty()) continue

//         let processingInv = new $ProcessingInventory(ctx => { })
//         processingInv.insertItem(curItem, false)
//         let warpper = new $RecipeWrapper(processingInv)
//         let curshingRecipe = $CreateRecipesType.CRUSHING.find(warpper, level)
//         if (!curshingRecipe.isPresent()) {
//             curshingRecipe = $CreateRecipesType.MILLING.find(warpper, level)
//         }
//         if (!curshingRecipe.isPresent()) {
//             continue
//         }

//         /**@type {Internal.ItemStack[]} */
//         let outputItemList = curshingRecipe.get().rollResults()
//         if (outputItemList.length <= 0) {
//             continue
//         }
//         curItem.shrink(1)
//         SpawnLootAtLocation(level, player.blockPosition(), outputItemList.toArray())
//     }
// }


// RegistryOrganStrategy(
//     new OrganStrategyModel('kubejs:turkey_gizzard')
//         .addOnlyStrategy('food_eaten', TurkeyGizzardFoodEaten)
// )

// ===== turtle_shell.js =====
RegistryOrgan('kubejs:turtle_shell')
    .addScore('chestcavity:defense', 1)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

const TurtleShellTempDefenseUpUUID = UUID.fromString('77C62F52-9FC7-4791-9417-65C0107F7719')
const TurtleShellTempDefenseIdentifier = 'TurtleShellTempDefenseUp'

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TurtleShellEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const entity = event.entity
    if (!event.source.actual) return
    let attributeInstance = entity.getAttribute('minecraft:generic.armor')
    if (!attributeInstance) return
    let value = 1
    let oldModifier = attributeInstance.getModifier(TurtleShellTempDefenseUpUUID)
    if (oldModifier) value = oldModifier.amount + value
    let attributeModifier = new $AttributeModifier(TurtleShellTempDefenseUpUUID, TurtleShellTempDefenseIdentifier, value, $Operation.ADDITION)
    attributeInstance.removeModifier(TurtleShellTempDefenseUpUUID)
    attributeInstance.addPermanentModifier(attributeModifier)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function TurtleShellOrganTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    let attributeInstance = entity.getAttribute('minecraft:generic.attack_damage')
    if (!attributeInstance) return
    attributeInstance.removeModifier(TurtleShellTempDefenseUpUUID)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function TurtleShellEntityTick(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    if (entity.age % 100 != 0) return
    let attributeInstance = entity.getAttribute('minecraft:generic.armor')
    if (!attributeInstance) return
    let value = -1
    let oldModifier = attributeInstance.getModifier(TurtleShellTempDefenseUpUUID)
    if (oldModifier) value = oldModifier.amount + value

    attributeInstance.removeModifier(TurtleShellTempDefenseUpUUID)
    if (value <= 0) return
    let attributeModifier = new $AttributeModifier(TurtleShellTempDefenseUpUUID, 'TurtleShellTempDefenseUp', value, $Operation.ADDITION)
    attributeInstance.addPermanentModifier(attributeModifier)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:turtle_shell')
        .addOnlyStrategy('entity_be_hurt', TurtleShellEntityBeHurt)
        .addOnlyStrategy('organ_take_off', TurtleShellOrganTakeOff)
        .addOnlyStrategy('entity_tick', TurtleShellEntityTick)
)

// ===== urchinkin_stinger.js =====
RegistryOrgan('kubejs:urchinkin_stinger')
    .addScore('chestcavity:defense', 1.5)
    .addScore('chestcavity:endurance', 1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function UrchinkinStingerEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const source = event.source.actual
    if (!source || !source.isLiving()) return
    const entity = event.entity
    const harmEffectList = []
    entity.potionEffects.active.forEach(pEffect => {
        if (pEffect.effect.isHarmful()) {
            harmEffectList.push(pEffect)
        }
    })
    if (harmEffectList.length <= 0) return
    /**@type {Internal.MobEffectInstance} */
    let targetEffect = RandomGet(harmEffectList)
    if (targetEffect.effect.isInstantenous()) return
    source.potionEffects.add(targetEffect.getEffect(), 10 * 20, targetEffect.getAmplifier())
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:urchinkin_stinger')
        .addOnlyStrategy('entity_be_hurt', UrchinkinStingerEntityBeHurt)
)

// ===== wilden_heart.js =====
RegistryOrgan('kubejs:wilden_heart')
    .addScore('chestcavity:health', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function WildenHeartEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = entity.chestCavityInstance
    if (event.amount <= entity.getHealth() + entity.getAbsorptionAmount()) return
    entity.setAbsorptionAmount(entity.getAbsorptionAmount() + event.amount)
    event.amount = 0
    let replaceItem = Item.of('kubejs:rotten_heart')
    SetChestCavityOrgan(customData, chestCavity, replaceItem, organIndex, slotType, true)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:wilden_heart')
        .addOnlyStrategy('entity_be_hurt', WildenHeartEntityBeHurt)
)

// ===== wolf_fang.js =====
RegistryOrgan('kubejs:wolf_fang')
    .addScore('chestcavity:strength', 2)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function WolfFangKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    let target = player.lastHurtMob
    let tamedLivingList = GetTamedEntityWithinRadius(level, player, 16)
    tamedLivingList.forEach(pEntity => {
        if (pEntity instanceof $PathfinderMob) {
            pEntity.setTarget(target)
            pEntity.potionEffects.add('farm_and_charm:dog_food', 20 * 30, 2, false, false)
        }
    })
    player.addItemCooldown(organItem, 20 * 60)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:wolf_fang')
        .addOnlyStrategy('key_active', WolfFangKeyActive)
)

// ===== basic_organs.js =====
RegistryOrgan('kubejs:fireproof_appendix').addScore('chestcavity:luck', 0.5).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_intestine').addScore('chestcavity:nutrition', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_heart').addScore('chestcavity:health', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_kidney').addScore('chestcavity:filtration', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_liver').addScore('chestcavity:detoxification', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_lung').addScore('chestcavity:breath_recovery', 0.75).addScore('chestcavity:breath_capacity', 0.75).addScore('chestcavity:endurance', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_muscle').addScore('chestcavity:strength', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_rib').addScore('chestcavity:defense', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_spine').addScore('chestcavity:nerves', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_spleen').addScore('chestcavity:metabolism', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)
RegistryOrgan('kubejs:fireproof_stomach').addScore('chestcavity:digestion', 0.75).addScore('chestcavity:fire_resistant', 0.5).setCanSpawn(true)

// ===== blaze_spine.js =====
RegistryOrgan('kubejs:blaze_spine')
    .addScore('chestcavity:nerves', 1)
    .addScore('kubejs:crit_chance', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function BlazeSpineEntityDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const source = event.source.actual
    const chestCavity = source.chestCavityInstance
    /**@type {number} */
    let blazeSpineCounter = GetCustomDataMap(chestCavity, 'blazeSpineCounter', 0)
    if (blazeSpineCounter <= 0) return
    blazeSpineCounter = blazeSpineCounter - 1
    const target = event.entity
    let fireTicks = Math.max(source.getRemainingFireTicks() - 60, 0)
    source.setRemainingFireTicks(fireTicks)
    target.setRemainingFireTicks(fireTicks)
    if (slotType == MachinaryLubricant) return
    SetCustomDataMap(chestCavity, 'blazeSpineCounter', blazeSpineCounter)
    if (blazeSpineCounter > 0) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText(blazeSpineCounter.toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    } else {
        RemoveOrganEffect(chestCavity, 'kubejs:blaze_spine')
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BlazeSpineKeyActiveOnly(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const chestCavity = player.chestCavityInstance
    SetCustomDataMap(chestCavity, 'blazeSpineCounter', 1)
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText('1')
    SetOrganEffect(chestCavity, organEffect)
    player.addItemCooldown(organItem, 20 * 10)
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function BlazeSpineOrganTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity.isPlayer()) {
        RemoveOrganEffect(chestCavity, 'kubejs:blaze_spine')
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:blaze_spine')
        .addOnlyStrategy('entity_do_damage', BlazeSpineEntityDoDamage)
        .addOnlyStrategy('key_active', BlazeSpineKeyActiveOnly)
        .addOnlyStrategy('organ_take_off', BlazeSpineOrganTakeOff)
)

// ===== explosion_cavity.js =====
RegistryOrgan('kubejs:explosion_cavity')
    .addScore('chestcavity:breath_capacity', 3)
    .addScore('chestcavity:breath_recovery', 0.5)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.NetworkEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function ExplosionCavityKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    let fireTick = player.getRemainingFireTicks()
    let playerPos = player.position()
    if (fireTick <= 200) return
    let damageAmount = Math.floor(fireTick / 20)
    /**@type {Internal.LivingEntity[]} */
    let entityList = GetLivingWithinRadiusVec3d(level, playerPos, 5, (plevel, pEntity) => !pEntity.equals(player))
    let damageSource = level.damageSources().explosion(player, null)
    entityList.forEach(pEntity => pEntity.attack(damageSource, damageAmount))
    level.createExplosion(playerPos.x(), playerPos.y(), playerPos.z()).explode()
    player.setRemainingFireTicks(fireTick - 200)
    player.addItemCooldown(organItem, 20 * 3)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:explosion_cavity')
        .addOnlyStrategy('key_active', ExplosionCavityKeyActive)
)

// ===== ghast_sac.js =====
RegistryOrgan('kubejs:ghast_sac')
    .addScore('chestcavity:speed', 1)
    .addScore('chestcavity:breath_recovery', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function GhastSacKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    SummonFireballTowardFacing(player, level)
    player.addItemCooldown(organItem, 20 * 3)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:ghast_sac')
        .addOnlyStrategy('key_active', GhastSacKeyActive)
)

// ===== golden_stone.js =====
RegistryOrgan('kubejs:golden_stone')
    .addScore('chestcavity:defense', 2)
    .addScore('chestcavity:health', -0.5)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function GoldenStoneTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    entity.setWearingGold(false)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function GoldenStoneTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    entity.setWearingGold(true)
}
RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:golden_stone')
    .addOnlyStrategy('organ_take_off', GoldenStoneTakeOff)
    .addOnlyStrategy('organ_take_on', GoldenStoneTakeOn)
)

// ===== hoglin_stomach.js =====
RegistryOrgan('kubejs:hoglin_rumen')
    .addScore('chestcavity:digestion', 1)
    .addScore('chestcavity:fire_resistant', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HoglinStomachKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    const server = event.server
    let rad = JavaMath.toRadians(player.getYHeadRot() + 90)
    let dx = JavaMath.cos(rad)
    let dy = JavaMath.sin(rad)

    let nowMove = player.getDeltaMovement().add(dx * 16, 0.35, dy * 16)
    player.setDeltaMovement(nowMove)
    player.connection.send(new $ClientboundSetEntityMotionPacket(player))
    let timer = 0
    let armorValue = player.getArmorValue()
    server.scheduleRepeatingInTicks(2, (ctx) => {
        let entityInRadius = GetLivingWithinRadius(level, player.blockPosition(), 3, (curlevel, curEntity) => {
            if (!curEntity.isPlayer()) {
                return true
            }
        })
        entityInRadius.forEach(entity => {
            entity.attack(level.damageSources().playerAttack(player), armorValue)
            entity.invulnerableTime = 0
        })
        if (timer > 5) ctx.clear()
        timer++
    })

    player.addItemCooldown(organItem, 20 * 10)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:hoglin_rumen')
        .addOnlyStrategy('key_active', HoglinStomachKeyActive)
)

// ===== ignited_armour.js =====
RegistryOrgan('kubejs:ignited_armour')
    .addScore('chestcavity:defense', 1)
    .addScore('chestcavity:fire_resistant', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function IgnitedArmourEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const source = event.source.actual
    if (!source) return
    const chestCavity = entity.chestCavityInstance
    let fireResistantScore = chestCavity.getOrganScore('chestcavity:fire_resistant')
    let fireTicks = source.getRemainingFireTicks()
    source.setRemainingFireTicks(fireTicks, fireResistantScore * 10)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:ignited_armour')
        .addOnlyStrategy('entity_be_hurt', IgnitedArmourEntityBeHurt)
)

// ===== magam_colloid.js =====
RegistryOrgan('kubejs:magam_colloid')
    .addScore('chestcavity:health', 1)
    .setCanSpawn(true)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingDamageEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function MagamColloidEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    if (slotType == RevolutionFlameType) {
        let fireTick = 60 * event.amount
        let remainTick = entity.getRemainingFireTicks()
        if (fireTick <= remainTick) {
            entity.setRemainingFireTicks(remainTick - fireTick)
        } else {
            event.setAmount(event.amount - Math.floor(remainTick / 60))
            entity.setRemainingFireTicks(0)
        }
    } else {
        let fireTick = 20 * event.amount
        entity.setRemainingFireTicks(entity.getRemainingFireTicks() + fireTick)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:magam_colloid')
        .addStrategy('entity_be_hurt', MagamColloidEntityBeHurt)
)

// ===== cherry_bone.js =====
RegistryOrgan('kubejs:cherry_bone')
    .addScore('chestcavity:defense', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function CherryBoneEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const sourceEntity = event.source.actual
    if (!sourceEntity) return
    if (sourceEntity.hasEffect('kubejs:vita_toxins')) {
        let effect = sourceEntity.getEffect('kubejs:vita_toxins')
        if (effect.duration > event.amount * 20) {
            effect.setDuration(effect.duration - event.amount * 20)
            event.amount = 0
        } else {
            event.amount -= effect.duration / 20
            sourceEntity.removeEffect('kubejs:vita_toxins')
        }
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:cherry_bone')
        .addOnlyStrategy('entity_be_hurt', CherryBoneEntityBeHurt)
)

// ===== crimson_brain.js =====
RegistryOrgan('kubejs:crimson_brain')
    .addScore('chestcavity:nerves', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.LivingEntityDeathEventJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function CrimsonBrainEntityKill(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    const killer = event.source.actual
    if (!entity.hasEffect('kubejs:putrid_toxins')) return
    let effect = entity.getEffect('kubejs:putrid_toxins')
    let duration = effect.duration
    let amplifier = effect.amplifier
    let damage = GetPutridToxinsDamage(entity)
    let entityList = GetLivingWithinRadius(level, entity.blockPosition(), 4, (pLevel, pEntity) => {
        if (pEntity.isPlayer()) return false
        if (pEntity instanceof $TamableAnimal) {
            if (pEntity.getOwner() && pEntity.getOwner().isPlayer()) return false
        }
        if (pEntity.is(killer)) return false
        if (pEntity.is(entity)) return false
        return true
    })
    entityList.forEach(/**@param {Internal.LivingEntity} pEntity*/pEntity => {
        // 强化效果，如果存在效果，就会延续时间，而不是跳过，等级取最大值
        if (pEntity.hasEffect('kubejs:putrid_toxins')) return
        SetPutridToxinsDamage(pEntity, damage)
        pEntity.potionEffects.add('kubejs:putrid_toxins', duration, amplifier, false, false)
    })
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:crimson_brain')
        .addOnlyStrategy('entity_kill', CrimsonBrainEntityKill)
)

// ===== foliaath_stem.js =====
RegistryOrgan('kubejs:foliaath_stem')
    .addScore('chestcavity:digestion', 1)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.OrganAddStatusEffectJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function FoliaathStemOrganAddStatusEffect(customData, event, organItem, organIndex, slotType) {
    const effectInstance = event.effect
    if (effectInstance.effect.isInstantenous()) return
    effectInstance.setDuration(effectInstance.getDuration() + 20 * 3)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:foliaath_stem')
        .addOnlyStrategy('organ_add_status_effect', FoliaathStemOrganAddStatusEffect)
)

// ===== hop_kidney.js =====
RegistryOrgan('kubejs:hop_kidney')
    .addScore('chestcavity:detoxification', 0.5)
    .addScore('chestcavity:filtration', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HopKidneyKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    $AlcoholManager.drinkAlcohol(player)
    player.addItemCooldown(organItem, 20 * 5)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:hop_kidney')
        .addOnlyStrategy('key_active', HopKidneyKeyActive)
)

// ===== lily_pad_lung.js =====
RegistryOrgan('kubejs:lily_pad_lung')
    .addScore('chestcavity:defense', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function LilyPadLungDoDamageDefer(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const source = event.source.actual
    if (source.isPlayer() && OrganItemCoolDown(source, organItem)) {
        return
    }
    /**@type {Internal.LivingEntity} */
    const target = event.entity
    if (!target.isAlive()) return
    if (!target.hasEffect('kubejs:putrid_toxins')) return
    let effect = target.getEffect('kubejs:putrid_toxins')
    let damage = GetPutridToxinsDamage(target)
    // 强化效果，如果伤害小于快照伤害，则延长效果时长；且不在具有内置冷却
    if (event.amount < damage) return
    SetPutridToxinsDamage(target, event.amount)
    if (source.isPlayer()) {
        source.addItemCooldown('kubejs:lily_pad_lung', 20 * 3)
    }
    target.potionEffects.add('kubejs:putrid_toxins', effect.duration, Math.min(effect.amplifier + 1, 4), false, false)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:lily_pad_lung')
        .addOnlyStrategy('entity_do_damage', LilyPadLungDoDamageDefer, -1)
)

// ===== pitcher_stomach.js =====
RegistryOrgan('kubejs:pitcher_stomach')
    .addScore('chestcavity:nutrition', 0.5)
    .addScore('chestcavity:digestion', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function PitcherStomachDoDamageDefer(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    const chestCavity = sourceEntity.chestCavityInstance
    let isTwistingIntestineAttack = GetCustomDataMap(chestCavity, 'twistingIntestineAttack', false)
    if (isTwistingIntestineAttack) {
        // 毒素爆发并不会触发毒效果
        SetCustomDataMap(chestCavity, 'twistingIntestineAttack', false)
        return
    }
    const curDamage = organItem.getDamageValue()
    if (curDamage == 1) CommonDingNotice(sourceEntity.level, sourceEntity)
    if (curDamage > 0) {
        organItem.setDamageValue(curDamage - 1)
    } else {
        /** @type {Internal.LivingEntity} */
        const target = event.entity
        organItem.setDamageValue(organItem.getMaxDamage())
        target.potionEffects.add('kubejs:putrid_toxins', 20 * 20, 0, false, false)
        SetPutridToxinsDamage(target, event.amount)
    }
    if (sourceEntity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function PitcherStomachChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:pitcher_stomach')
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:pitcher_stomach')
        .addOnlyStrategy('entity_do_damage', PitcherStomachDoDamageDefer, -2)
        .addOnlyStrategy('organ_take_off', PitcherStomachChestCavityTakeOffOnly)
)

// ===== rootling_ectoplasm.js =====
RegistryOrgan('kubejs:rootling_ectoplasm')
    .addScore('kubejs:extreme_fitness', 2)
    .addScore('kubejs:photosynthesis', 1)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.OpenedEntityTickJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RootlingEctoplasmEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const level = event.level
    const chestCavity = event.chestCavity
    if (Math.random() > 1) return
    let digestion = chestCavity.getOrganScore('chestcavity:digestion')
    let growthCnt = Math.min(Math.floor(digestion / 5) + 1, 1)
    const hasTentaclesHarvester = GetCustomDataMap(chestCavity, 'hasTentaclesHarvester', 0)
    const hasWhirlisprigStarGem = GetCustomDataMap(chestCavity, 'hasWhirlisprigStarGem', 0)
    const radius = 4
    const diameter = radius * 2 + 1
    for (let i = 0; i < growthCnt; i++) {
        let rx = Math.floor(Math.random() * diameter) - radius
        let rz = Math.floor(Math.random() * diameter) - radius
        let entityBlockPos = entity.getOnPos()
        let targetBlockPos = new BlockPos(entityBlockPos.x + rx, Math.ceil(entityBlockPos.y) + 1, entityBlockPos.z + rz)
        let targetBlockState = level.getBlockState(targetBlockPos)
        if ($BoneMealItem.applyBonemeal(Item.of('minecraft:bone_meal'), level, targetBlockPos, null)) {
            level.spawnParticles('minecraft:glow', true, targetBlockPos.x + 0.2, targetBlockPos.y + 0.3, targetBlockPos.z - 0.4, 0, 0.1, 0, 2, 0)
            level.spawnParticles('minecraft:glow', true, targetBlockPos.x + 0.1, targetBlockPos.y + 0.2, targetBlockPos.z + 0.3, 0, 0.1, 0, 2, 0)
        }

        if (hasTentaclesHarvester == 0) return
        if (targetBlockState.hasProperty(BlockProperties.AGE_7) && targetBlockState.getValue(BlockProperties.AGE_7).intValue() == 7) {
            let lootContext = new $LootParamsBuilder(level)
                .withParameter(LootContextParams.ORIGIN, new Vec3d(targetBlockPos.getX(), targetBlockPos.getY(), targetBlockPos.getZ()))
                .withParameter(LootContextParams.BLOCK_STATE, targetBlockState)
                .withParameter(LootContextParams.THIS_ENTITY, entity)
                .withParameter(LootContextParams.TOOL, entity.getMainHandItem())
            targetBlockState.getDrops(lootContext).forEach(itemStack => {
                $Containers.dropItemStack(level, targetBlockPos.getX(), targetBlockPos.getY(), targetBlockPos.getZ(), itemStack.copy())
            })
            level.spawnParticles('minecraft:glow', true, targetBlockPos.x + 0.2, targetBlockPos.y + 0.3, targetBlockPos.z - 0.4, 0, 0.1, 0, 2, 0)
            level.spawnParticles('minecraft:glow', true, targetBlockPos.x + 0.1, targetBlockPos.y + 0.2, targetBlockPos.z + 0.3, 0, 0.1, 0, 2, 0)
            if (hasWhirlisprigStarGem == 0) {
                targetBlockState = targetBlockState.setValue(BlockProperties.AGE_7, Int2Integer(0))
                level.setBlockAndUpdate(targetBlockPos, targetBlockState)
            }
        }
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rootling_ectoplasm')
        .addOnlyStrategy('entity_tick', RootlingEctoplasmEntityTick)
)

// ===== twisting_weeping_intestine.js =====
RegistryOrgan('kubejs:twisting_weeping_intestine')
    .addScore('chestcavity:nutrition', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)



/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TwistingWeepingIntestineKeyActiveOnly(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const chestCavity = player.chestCavityInstance
    SetCustomDataMap(chestCavity, 'twistingIntestineAttackReady', true)
    let organEffect = new OragnEffectModel(organItem).setPriority(organIndex)
    SetOrganEffect(chestCavity, organEffect)
    player.addItemCooldown(organItem, 20 * 30)
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function TwistingWeepingIntestineChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:twisting_weeping_intestine')
    }
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function TwistingWeepingIntestineDoDamage(customData, event, organItem, organIndex, slotType) {
    /** @type {Internal.LivingEntity} */
    const entity = event.source.actual
    const chestCavity = entity.chestCavityInstance
    /** @type {number} */
    let twistingIntestineAttackReady = GetCustomDataMap(chestCavity, 'twistingIntestineAttackReady', false)
    if (!twistingIntestineAttackReady) return
    /** @type {Internal.LivingEntity} */
    const target = event.entity
    if (!target.hasEffect('kubejs:putrid_toxins')) return
    let effect = target.getEffect('kubejs:putrid_toxins')

    let damage = (effect.getAmplifier() * 0.25 + 0.25) * GetPutridToxinsDamage(target) * effect.getDuration() / 40
    event.amount = damage + event.amount
    SetCustomDataMap(chestCavity, 'twistingIntestineAttack', true)
    SetCustomDataMap(chestCavity, 'twistingIntestineAttackReady', false)

    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:twisting_weeping_intestine')
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:twisting_weeping_intestine')
        .addOnlyStrategy('key_active', TwistingWeepingIntestineKeyActiveOnly)
        .addOnlyStrategy('organ_take_off', TwistingWeepingIntestineChestCavityTakeOffOnly)
        .addOnlyStrategy('entity_do_damage', TwistingWeepingIntestineDoDamage)
)

// ===== vita_berry.js =====
RegistryOrgan('kubejs:vita_berry')
    .addScore('chestcavity:endurance', 1.5)
    .addScore('kubejs:photosynthesis', 0.5)


/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function VitaBerryChestCavityTakeOn(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    SetCustomDataMap(entity.chestCavityInstance, 'hasVitaBerry', 1)
}

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingHurtEvent} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function VitaBerryChestCavityTakeOff(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    RemoveCustomDataMap(entity.chestCavityInstance, 'hasVitaBerry')
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:vita_berry')
        .addOnlyStrategy('organ_take_on', VitaBerryChestCavityTakeOn)
        .addOnlyStrategy('organ_take_off', VitaBerryChestCavityTakeOff)
)

// ===== vita_rose_liver.js =====
RegistryOrgan('kubejs:vita_rose_liver')
    .addScore('chestcavity:detoxification', 1.5)
    .addScore('chestcavity:filtration', 0.5)
    .addScore('kubejs:photosynthesis', 0.5)

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function VitaRoseLiverDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const sourceEntity = event.source.actual
    /**@type {Internal.LivingEntity} */
    const target = event.entity
    if (target.getHealth() < target.getMaxHealth() * 0.99) return
    target.potionEffects.add('kubejs:vita_toxins', 20 * 60, 0, false, false)
    if (!sourceEntity.getUuid()) return
    SetVitaToxinsSource(target, sourceEntity.getUuid())
    if (GetCustomDataMap(target.chestCavityInstance, 'hasVitaBerry', 0) == 1) {
        SetVitaToxinsType(target, 'max_health')
        SetVitaToxinsCoe(target, 2)
    } else {
        SetVitaToxinsType(target, 'attack_damage')
        SetVitaToxinsCoe(target, 1)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:vita_rose_liver')
        .addOnlyStrategy('entity_do_damage', VitaRoseLiverDoDamage)
)

// ===== vita_sunflower.js =====
RegistryOrgan('kubejs:vita_sunflower')
    .addScore('chestcavity:health', 1.5)
    .addScore('kubejs:photosynthesis', 1)


/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.LivingHurtEvent} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function VitaSunflowerDoDamage(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const target = event.entity
    if (!target.hasEffect('kubejs:vita_toxins')) return
    let effect = target.getEffect('kubejs:vita_toxins')
    let leftDamage = organItem.getMaxDamage() - organItem.getDamageValue()
    if (leftDamage < 10) return
    organItem.setDamageValue(organItem.getMaxDamage())
    effect.setDuration(effect.duration + 20 * leftDamage)
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.OpenedEntityTickJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function VitaSunflowerEntityTick(customData, event, organItem, organIndex, slotType) {
    const { entity, chestCavity } = event
    if (organItem.getDamageValue() - 1 >= 0) {
        organItem.setDamageValue(organItem.getDamageValue() - 1)
    }
    if (entity instanceof $ServerPlayer) {
        let organEffect = new OragnEffectModel(organItem).setPriority(organIndex).setCustomText((organItem.getMaxDamage() - organItem.getDamageValue()).toFixed(0))
        SetOrganEffect(chestCavity, organEffect)
    }
}

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function VitaSunflowerChestCavityTakeOffOnly(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    const chestCavity = event.chestCavity
    if (entity instanceof $ServerPlayer) {
        RemoveOrganEffect(chestCavity, 'kubejs:vita_sunflower')
    }
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:vita_sunflower')
        .addOnlyStrategy('entity_do_damage', VitaSunflowerDoDamage)
        .addOnlyStrategy('entity_tick', VitaSunflowerEntityTick)
        .addOnlyStrategy('organ_take_off', VitaSunflowerChestCavityTakeOffOnly)
)

// ===== dying_heart.js =====
// RegistryOrgan('kubejs:dying_heart')
    // .addScore('chestcavity:health', 0.5)
    // .setCanSpawn(true)
// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DyingHeartChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // if (!entity.isPlayer()) return
    // const chestCavity = event.chestCavity
    // AddSpellSelection(customData, chestCavity, 'kubejs:raise_overlord_undead', 1)
// }

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.EvaluateChestCavityJS} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function DyingHeartTakeOff(customData, event, organItem, organIndex, slotType) {
    // const { entity, chestCavity } = event
    // if (!entity.isPlayer()) return
    // RemoveSpellSelectionBySpellId(customData, chestCavity, 'kubejs:raise_overlord_undead')
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:dying_heart')
        // .addOnlyStrategy('chest_cavity_update', DyingHeartChestCavityUpdate)
        // .addOnlyStrategy('organ_take_off', DyingHeartTakeOff)
// )

// ===== ender_guardian_spine.js =====
RegistryOrgan('kubejs:ender_guardian_spine')
    .addScore('chestcavity:defense', 3)
    .addScore('chestcavity:nerves', 1)
    .addScore('chestcavity:health', -1)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.LivingDamageEvent} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function EnderGuardianSpineBeHurt(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.LivingEntity} */
    const source = event.source.actual
    if (!source || !source.isLiving()) return
    let amplifier = 0
    if (source.hasEffect('kubejs:fragile')) {
        amplifier = Math.min(source.getEffect('kubejs:fragile').getAmplifier() + 1, 9)
    }
    source.potionEffects.add('kubejs:fragile', 20 * 5, amplifier)
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:ender_guardian_spine')
        .addOnlyStrategy('entity_be_hurt', EnderGuardianSpineBeHurt)
)

// ===== harbinger_lung.js =====
RegistryOrgan('kubejs:harbinger_lung')
    .addScore('chestcavity:breath_capacity', 1)
    .addScore('chestcavity:breath_recovery', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.LivingEntityDeathEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function HarbingerLungEntityKill(customData, event, organItem, organIndex, slotType) {
    const target = event.entity
    if (!target.isOnFire()) return
    /**@type {Internal.LivingEntity} */
    const killer = event.source.actual
    let fireTicksRemain = target.getRemainingFireTicks()
    if (fireTicksRemain <= 0) return
    killer.heal(Math.floor(fireTicksRemain / 20))
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:harbinger_lung')
        .addOnlyStrategy('entity_kill', HarbingerLungEntityKill)
)

// ===== immortal_volcanic_rock.js =====
RegistryOrgan('kubejs:immortal_volcanic_rock')
    .addScore('chestcavity:strength', -1)
    .addScore('chestcavity:fire_resistant', -1)


/**
* @param {OrganEventCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function ImmortalVolcanicRockEntityTick(customData, event, organItem, organIndex, slotType) {
    const entity = event.entity
    let fireTick = entity.getRemainingFireTicks()
    if (fireTick < 2000) {
        entity.setRemainingFireTicks(fireTick + 20)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:immortal_volcanic_rock')
        .addOnlyStrategy('entity_tick', ImmortalVolcanicRockEntityTick)
)

// ===== leviathan_rib.js =====
// RegistryOrgan('kubejs:leviathan_rib')
    // .addScore('chestcavity:defense', 2.5)
    // .addScore('chestcavity:swim_speed', 1)
    // .addScore('chestcavity:endurance', -0.5)
    // .setCanSpawn(true)

// /**
 // * @param {OrganEventCustomData} customData
 // * @param {Internal.LivingHurtEvent} event 
 // * @param {Internal.ItemStack} organItem
 // * @param {number} organIndex
 // * @param {string} slotType
 // */
// function LeviathanRibEntityBeHurt(customData, event, organItem, organIndex, slotType) {
    // const entity = event.entity
    // const source = event.source.actual
    // if (!source) return
    // const level = entity.level
    // let targetPosition = source.position()
    // const chestCavity = entity.getChestCavityInstance()
    // targetPosition = targetPosition.add(new Vec3d(ISSUtils.getRandomScaled(1), ISSUtils.getRandomScaled(1), ISSUtils.getRandomScaled(1)))
    // targetPosition = ISSUtils.moveToRelativeGroundLevel(level, targetPosition, 8)
    // let tentacle = new $VoidTentacle(level, entity, Math.max(chestCavity.getOrganScore('chestcavity:defense'), 2))
    // tentacle.moveTo(targetPosition)
    // tentacle.setYBodyRot(Math.floor(Math.random() * 360))
    // level.addFreshEntity(tentacle)
// }

// RegistryOrganStrategy(
    // new OrganStrategyModel('kubejs:leviathan_rib')
        // .addOnlyStrategy('entity_be_hurt', LeviathanRibEntityBeHurt)
// )

// ===== maledictus_wing.js =====
RegistryOrgan('kubejs:maledictus_wing')
    .addScore('chestcavity:speed', 2)
    .addScore('chestcavity:strength', 1)
    .addScore('kubejs:crit_chance', 1)
    .setCanSpawn(true)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.NetworkEventJS} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function MaledictusWingKeyActive(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const level = event.level
    const server = event.server
    const chestCavity = player.chestCavityInstance
    let rad = JavaMath.toRadians(player.getYHeadRot() + 90)
    let dx = JavaMath.cos(rad)
    let dy = JavaMath.sin(rad)

    let nowMove = player.getDeltaMovement().add(dx * 16, 0.35, dy * 16)
    player.setDeltaMovement(nowMove)
    player.connection.send(new $ClientboundSetEntityMotionPacket(player))
    let timer = 0
    let playerStrength = chestCavity.getOrganScore('chestcavity:strength')
    server.scheduleRepeatingInTicks(2, (ctx) => {
        level.spawnParticles($ParticleTypes.SONIC_BOOM, false, player.x, player.y, player.z, 0, 0, 0, 1, 0)
        let entityInRadius = GetLivingWithinRadius(level, player.blockPosition(), 3, (curlevel, curEntity) => {
            if (!curEntity.isPlayer()) {
                return true
            }
        })
        entityInRadius.forEach(entity => {
            entity.attack(level.damageSources().playerAttack(player), 5 * playerStrength)
            entity.invulnerableTime = 0
        })
        if (timer > 5) ctx.clear()
        timer++
    })

    player.addItemCooldown(organItem, 20 * 30)
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:maledictus_wing')
        .addOnlyStrategy('key_active', MaledictusWingKeyActive)
)

// ===== netherite_muscle.js =====
RegistryOrgan('kubejs:netherite_muscle')
    .addScore('kubejs:extreme_strength', 2.5)
    .addScore('chestcavity:digestion', -0.5)
    .addScore('chestcavity:nutrition', -0.5)
    .setCanSpawn(true)

// ===== remnant_heart.js =====
RegistryOrgan('kubejs:remnant_heart')
    .addScore('chestcavity:detoxification', 1)
    .addScore('chestcavity:fire_resistant', 1)
    .setCanSpawn(true)


/**
 * @param {OrganBlockRightClickedStrategyCustomData} customData
 * @param {Internal.BlockRightClickedEventJS} event
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RemnantHeartBlockRightClicked(customData, event, organItem, organIndex, slotType) {
    const block = event.block
    const level = event.level
    if (event.hand != 'main_hand') return
    let blockPos = block.getPos()
    if (block.id != 'minecraft:sand' && block.id != 'minecraft:gravel') return
    let itemAbove = GetItemEntityWithinRadius(level, blockPos.above(), 1, (level, entity) => true)
    if (itemAbove.length <= 0) return
    /**@type {Internal.ItemEntity} */
    let itemHiden = RandomGet(itemAbove)
    let item = itemHiden.getItem()
    itemHiden.discard()
    let nbt = new $CompoundTag()
    nbt.put('item', item.serializeNBT())
    switch (block.id) {
        case 'minecraft:sand':
            level.setBlockAndUpdate(blockPos, Blocks.SUSPICIOUS_SAND.defaultBlockState())
            let sandEntity = level.getBlockEntity(blockPos)
            sandEntity.deserializeNBT(nbt)
            level.setBlockEntity(sandEntity)
            break
        case 'minecraft:gravel':
            level.setBlockAndUpdate(blockPos, Blocks.SUSPICIOUS_GRAVEL.defaultBlockState())
            let gravelEntity = level.getBlockEntity(blockPos)
            gravelEntity.deserializeNBT(nbt)
            level.setBlockEntity(gravelEntity)
            break
    }

}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:remnant_heart')
        .addOnlyStrategy('block_right_clicked', RemnantHeartBlockRightClicked)
)

// ===== scylla_star_gem.js =====
RegistryOrgan('kubejs:scylla_star_gem')
    .addScore('kubejs:magic_capacity', 3)
    .setCanSpawn(true)

/**
* @param {OrganEventCustomData} customData
* @param {Internal.EffectResolveEvent$Post} event
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function ScyllaStarGemEffectResolvePost(customData, event, organItem, organIndex, slotType) {
    const hitResult = event.rayTraceResult
    if (!(hitResult instanceof $EntityHitResult)) return
    /**@type {Internal.LivingEntity } */
    let entity = hitResult.getEntity()
    if (entity.isOnFire()) {
        entity.extinguishFire()
        entity.setTicksFrozen(entity.getRemainingFireTicks() * 2)
        entity.potionEffects.add('minecraft:fire_resistance', 10 * 20, 0, false, false)
    }
}

RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:scylla_star_gem')
        .addOnlyStrategy('ars_effect_resolve_post', ScyllaStarGemEffectResolvePost)
)

// ===== warden_core.js =====
RegistryOrgan('kubejs:warden_core')
    .addScore('chestcavity:health', 2)
    .addScore('chestcavity:endurance', -0.5)
    .setCanSpawn(true)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.NetworkEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function WardenCoreKeyActive(customData, event, organItem, organIndex, slotType) {
    /**@type {Internal.ServerPlayer} */
    const player = event.player
    const level = event.level
    let ray = player.rayTrace(24, false)
    let distance = ray.distance
    let damageSource = player.damageSources().sonicBoom(player)
    /**@type {Internal.Vec3d} */
    let vec3Nor = player.getLookAngle().normalize()
    let counter = 0
    let xpLevel = player.getXpLevel()
    if (ray.entity && ray.entity.isLiving()) {
        ray.entity.attack(damageSource, 30 + xpLevel)
        ray.entity.invulnerableTime = 0
        counter++
    }
    if (ray.block) {
        distance = player.getPosition(1.0).distanceTo(ray.block.pos)
    }
    for (let i = 0; i < distance; i++) {
        let vec3 = vec3Nor.scale(i).add(player.getEyePosition())
        level.spawnParticles($ParticleTypes.SONIC_BOOM, false, vec3.x(), vec3.y(), vec3.z(), 0, 0, 0, 1, 0)
        if (i % 2 == 0) {
            let entityInRadius = GetLivingWithinRadius(level, ConvertVec3d2BlockPos(vec3), 2, (curlevel, curEntity) => {
                if (!curEntity.isPlayer()) {
                    return true
                }
            })
            entityInRadius.forEach(entity => {
                counter++
                entity.attack(damageSource, 10 + Math.min(xpLevel * 0.5, 50))
                entity.invulnerableTime = 0 
            })
        }
    }
    level.playSound(null, player.getX(), player.getY(), player.getZ(), 'entity.warden.sonic_boom', player.getSoundSource(), 0.5, 1)
    player.addItemCooldown(organItem, Math.max(20 * 60 - counter * 40, 0))
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:warden_core')
        .addOnlyStrategy('key_active', WardenCoreKeyActive)
)

// ===== rose_quartz_dialyzer.js =====
RegistryOrgan('kubejs:rose_quartz_dialyzer')
    .addScore('chestcavity:filtration', 2.0)
    .addScore('chestcavity:defense', -1)
    .addScore('kubejs:rosy', 0.5)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.NetworkEventJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RoseQuartzDialyzerKeyActiveOnly(customData, event, organItem, organIndex, slotType) {
    const player = event.player
    const chestCavity = player.chestCavityInstance
    let repairValue = chestCavity.getOrganScore('kubejs:rosy') * 3
    switch (slotType) {
        case 'rosy_explosion': {
            repairValue = repairValue + chestCavity.getOrganScore('chestcavity:filtration') * chestCavity.inventory.countNonEmpty()
            break
        }
    }
    for (let i = 0; i < chestCavity.inventory.getContainerSize(); i++) {
        let item = chestCavity.inventory.getItem(i)
        if (item.getMaxDamage() <= 0) continue
        let damgeValue = item.getDamageValue()
        if (repairValue > damgeValue) {
            repairValue = repairValue - damgeValue
            item.setDamageValue(0)
        } else {
            item.setDamageValue(damgeValue - repairValue)
            repairValue = 0
            break
        }
    }
    player.addItemCooldown(organItem, 20 * 60)
}
RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rose_quartz_dialyzer')
        .addOnlyStrategy('key_active', RoseQuartzDialyzerKeyActiveOnly)
)

// ===== rose_quartz_heart.js =====
RegistryOrgan('kubejs:rose_quartz_heart')
    .addScore('chestcavity:health', 1)
    .addScore('chestcavity:defense', -0.5)
    .addScore('kubejs:rosy', 1.0)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RoseQuartzHeartChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let rosyValue = chestCavity.getOrganScore('kubejs:rosy') * 0.5
    switch (slotType) {
        case 'rosy_explosion': {
            rosyValue = rosyValue + chestCavity.getOrganScore('chestcavity:health') * chestCavity.inventory.countNonEmpty() * 0.5
            break
        }
    }
    customData.maxHealth.addAttributeModifier(rosyValue, 'addition', 'base')
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rose_quartz_heart')
        .addStrategy('chest_cavity_update', RoseQuartzHeartChestCavityUpdate)
)

// ===== rose_quartz_liver.js =====
RegistryOrgan('kubejs:rose_quartz_liver')
    .addScore('chestcavity:detoxification', 1.0)
    .addScore('chestcavity:defense', -0.5)
    .addScore('kubejs:rosy', 0.5)

/**
 * @param {OrganChestCavityUpdateStrategyCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RoseQuartzLiverChestCavityUpdateOnly(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let rosyValue = chestCavity.getOrganScore('kubejs:rosy')
    switch (slotType) {
        case 'rosy_explosion': {
            rosyValue = rosyValue * 1.5 + chestCavity.getOrganScore('chestcavity:detoxification') * chestCavity.inventory.countNonEmpty() * 0.5
            break
        }
    }
    chestCavity.setOrganScore('kubejs:rosy', rosyValue)
}
RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rose_quartz_liver')
        .addOnlyStrategy('chest_cavity_update', RoseQuartzLiverChestCavityUpdateOnly)
)

// ===== rose_quartz_muscle.js =====
RegistryOrgan('kubejs:rose_quartz_muscle')
    .addScore('chestcavity:strength', 1.5)
    .addScore('chestcavity:defense', -0.5)
    .addScore('kubejs:rosy', 1.0)


/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RoseQuartzMuscleChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let rosyValue = chestCavity.getOrganScore('kubejs:rosy')
    switch (slotType) {
        case 'rosy_explosion': {
            rosyValue = rosyValue + chestCavity.getOrganScore('chestcavity:strength') * chestCavity.inventory.countNonEmpty()
            break
        }
    }
    customData.attackDamage.addAttributeModifier(rosyValue, 'addition', 'base')
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RoseQuartzMuscleTakeOn(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    OrganSkinAdd(entity, 'chest', 'secret/chestcavity/rose_arm')
}

/**
 * @param {OrganEventCustomData} customData
 * @param {Internal.EvaluateChestCavityJS} event 
 * @param {Internal.ItemStack} organItem
 * @param {number} organIndex
 * @param {string} slotType
 */
function RoseQuartzMuscleTakeOff(customData, event, organItem, organIndex, slotType) {
    const { entity } = event
    OrganSkinRemove(entity, 'chest', 'secret/chestcavity/rose_arm')
}


RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rose_quartz_muscle')
        .addStrategy('chest_cavity_update', RoseQuartzMuscleChestCavityUpdate)
        .addOnlyStrategy('organ_take_on', RoseQuartzMuscleTakeOn)
        .addOnlyStrategy('organ_take_off', RoseQuartzMuscleTakeOff)
)

// ===== rose_quartz_rib.js =====
RegistryOrgan('kubejs:rose_quartz_rib')
    .addScore('chestcavity:defense', 1.0)
    .addScore('kubejs:rosy', 1.0)

/**
* @param {OrganChestCavityUpdateStrategyCustomData} customData
* @param {Internal.EvaluateChestCavityJS} event 
* @param {Internal.ItemStack} organItem
* @param {number} organIndex
* @param {string} slotType
*/
function RoseQuartzRibChestCavityUpdate(customData, event, organItem, organIndex, slotType) {
    const chestCavity = event.chestCavity
    let rosyValue = chestCavity.getOrganScore('kubejs:rosy')
    switch (slotType) {
        case 'rosy_explosion': {
            rosyValue = rosyValue + chestCavity.getOrganScore('chestcavity:defense') * chestCavity.inventory.countNonEmpty() * 0.5
            break
        }
    }
    customData.armor.addAttributeModifier(rosyValue * 0.5, 'addition', 'base')
}
RegistryOrganStrategy(
    new OrganStrategyModel('kubejs:rose_quartz_rib')
        .addStrategy('chest_cavity_update', RoseQuartzRibChestCavityUpdate)
)

